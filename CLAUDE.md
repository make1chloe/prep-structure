# 클로이영어 새 앱(v2) — 먼저 읽을 것

규칙의 원본은 **`docs/규칙.md`** 다 — 번호로 부른다(`대전제-0` · `듣기-2`). 화면 명세는 **목업**(`docs/목업/`),
짓는 순서는 **`docs/목업-구현-프롬프트.md`**, 지금 어디까지 왔나는 **`docs/지금까지.md`**, 원장님 말씀은 `docs/원장님-정하실-것.md`.
2026-09-05 밤부터 **목업대로 처음부터** 짓는다 — 옛 v2 앱은 지웠다(git 역사에만 있다).
이 파일은 그 문을 가리킬 뿐이다 — 규칙을 여기에 두 벌로 적지 않는다(원칙-1).

## 절대 하면 안 되는 것 다섯 (인수인계 0절)

1. **`main`에 푸시하지 않는다** — 옛 앱이고 푸시가 곧 배포다(대전제-2)
2. 마이그레이션에서 **`auth.` · `storage.` · `public.`을 건드리지 않는다** (`scripts/check-v2only.mjs`)
3. **권한 기본값을 코드에 박지 않는다** — 원장님이 웹에서 정하신다
4. **검사 통과용으로 앱 코드를 고치지 않는다**
5. **재원생·학부모 진짜 비밀번호를 건드리지 않는다** — `zz_시험_` 계정 다섯뿐(대전제-12)
6. **`app/globals.css` 를 손으로 고치지 않는다** — 목업을 고치고 `node scripts/mockup-css.mjs`. `check-mockup` 이 둘이 같은지 잰다(디자인-3)

## 원장님 말을 듣는 법 — 듣기-1~5 (`docs/규칙.md`)

- 말씀을 그대로 시작하지 않는다. **앱 말로 바꿔 적고, 되짚어 보여드린 뒤** 시작한다
- **의도를 모르면 묻고, 세부가 비면 정해서 내민다.** 여쭙는 것은 되돌리기 비싼 **하나**뿐
- 검토·질문도 **끝이 목록인 명령**으로 — 범위 · 갈래 · 근거 · 경계
- 답을 받으면 **그날** `docs/원장님-정하실-것.md`에 옮긴다 — 세션은 잊고 문서만 기억한다

## 푸시 전에

`bash scripts/check-all.sh`(검사 + 빌드). 빨간 것은 앱이 틀린 것이다. DB 열쇠(`.env.local`)가 없는 자리는 DB 검사를 **건너뜀으로 센다** — 초록이 아니다.
눌러보기는 `bash scripts/e2e/run.sh`(로컬 Postgres·PostgREST·인증 흉내 + 앱 :3300 + 화면 검사).
PR은 안 만든다 — 작업 브랜치와 `v2` 둘 다에 푸시한다(원장님 허락). 되돌릴 안전망은 검사와 빌드뿐이다.
