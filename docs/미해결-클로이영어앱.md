---
tags: [클로이영어, 앱, 미해결]
생성: 2026-07-26
---

# 클로이영어 앱 — 아직 해결 안 된 것

> 이 파일은 **내가 결정해야 하거나, 클로드가 아직 못 한 일**만 모아둔 곳.
> 끝낸 일은 [[할일-클로이영어앱]], 회차별 정리는 [[2026-07-26-작업정리]] 참고.

---

## 🔴 내가 결정해야 하는 것

### 금요일 — 해결 (2026-07-26)
- [x] **금요일은 보강만 하는 날**. 정규수업이 아님
- [x] `설정 → 보강만 하는 요일` 에서 관리 (기본값 금요일). 고른 요일은
      **수강료 회차와 스케줄 알림에서 빠짐.** 그날 보강 잡는 건 그대로 됨
- [x] 2026 회차 점검에서 못 채운 평일 7일이 전부 금요일이었던 이유가 이것
- [ ] 토요일도 보강일인지 확인 필요 (4/11 토, 4/26 일에 수업 기록이 있었음)

### 출결 기록이 실제와 다를 수 있음
- [ ] 1~3월 데일리리포트 442건의 **출결이 비어 있음**. 4월부터 정상
- [ ] **지각이 전체의 35~40%** — 실제로 그렇게 많은지, 기본값처럼 눌린 건지 확인 필요
- [ ] 데일리리포트의 결석은 1년 5건뿐. 진짜 기록은 보강문자DB(291건)

### 노션 단원 데이터가 없다
- [ ] 노션 3교재DB 110권 전부 **전체페이지수·전체Unit수·커리큘럼 연결이 비어 있음** (확인 완료)
- [ ] 3공통진도DB는 **행이 0개**, 데일리리포트 `개별진도`도 전부 빈칸
- [ ] 즉 **단원은 새로 입력해야 한다.** 가져올 데이터가 없음
- [ ] 규칙적인 교재는 `⚡ 단원 한 번에 만들기` (Unit 1~20, 10초)
- [ ] 불규칙한 교재(내신·모의고사)만 엑셀

### 교재 안내 문자 발송 내역 (전체 기간) — 아직 못 함
- [ ] 노션에서 **교재 안내 문자를 보낸 기록이 있는 DB를 못 찾음**
- [ ] 하원숙제DB의 `발송완료`는 **숙제 문자**라 그건 이관됨
- [ ] 확인해야 할 것: 교재 안내 발송 기록이 어디 남아 있나?
	- Make 실행 로그?
	- 솔라피/바티 발송 내역?
	- 노션의 다른 DB나 페이지?
	- 아니면 기록을 따로 안 남겼나?
- [ ] 위치를 알려주면 `/import` 에 탭을 하나 더 붙일 수 있음
- [ ] **7월이 아니라 전체 기간**을 원함 → CSV로 내보낼 때 필터 없이 전체 내보내기

### 수강료 계산 규칙 — 확정됨 (2026-07-26)
- [x] 수업일이 기준보다 많은 달 → 정가 상한
- [x] 휴강으로 회차가 빠져도 **금액은 안 깎는다.** 보강으로 채운다
- [x] 대신 **보강 필요 횟수 + 차액**을 계산해서 보여준다 → 다음 달에 덜 청구할 수 있게
- [x] 달 중간 등원·퇴원은 그만큼만 청구 (안 온 수업이므로)
- [x] 결석은 차감하지 않음 (보강으로 처리)
- [ ] 형제 할인은 학생별 금액으로 넣게 되어 있음. 규칙으로 만들 필요 있나?
- [ ] 차액을 다음 달에 실제로 반영하는 기능은 없음 (지금은 금액만 보여줌)

### 노션 단어 점수 해석 — 해결 (2026-07-26)
- [x] `-단어T` 는 **틀린 개수**임을 확인
- [x] 이관 시 `전체 − 틀린 개수` 로 환산해서 맞은 개수로 저장

### 발송 수단
- [ ] 솔라피 가입 · **발신번호 사전등록** (1~2일 소요)
- [ ] 문자로 시작 → 문구 굳으면 알림톡 템플릿 심사
- [ ] 앱 알림(무료)로 옮길 것: 숙제 안내
- [ ] 문자로 남길 것: 데일리리포트, 중요 공지

---

## 📌 구조를 알아두면 좋은 것

### 일정이 여러 곳에 나뉘어 있다 — 지금 상태 (2026-07-26)
- [x] **할일 · 일정** (`tasks`) — 학사일정, 특강, 상담 예약, 직접 넣는 것
- [x] **시험 일정** (`exam_periods`) — 학교·학년별 기간 + 영어 시험일. `/schedule` 에서 관리
- [x] **휴강** (`holidays`) — `/schedule`, `/tuition` 에서 지정
- [x] **결석 예정** (`attendance.planned`) — `/plan`, 학생 요청에서
- [x] 세 가지가 **`/tasks` 맨 위에 "다른 화면에서 온 일정" 으로 같이 보임** (읽기만)
- [ ] 한 테이블로 합칠지는 나중에 결정. 지금은 각자 하는 일이 달라서 나눠 둠

### 공휴일 판단
- [x] 공휴일·대체공휴일·낀 날을 계산해서 **수업이 잡힌 날만** 알림 (자동 휴강 안 함)
- [x] 2026 검증: 3/2 삼일절 대체, 1/2·5/4 낀 날, 5/5, 2/16~18 설날 — 실제 기록과 전부 일치
- [ ] 음력 공휴일(설날·추석·석가탄신일)은 `lib/holidays.js` 의 `LUNAR` 에 해마다 적어야 함
      (2026·2027 까지 들어 있음. 2028 은 그때 세 줄 추가)

## 🟡 만들었지만 아직 안 써본 것

- [ ] `/plan` 다음 수업 준비 — 반/학생 고르고 숙제·결석예정·공지
- [ ] `/todo` 할일 — 분류를 직접 만들어 쓰는 구조
- [ ] `/tasks` 일정 — 날짜 정해진 것만
- [ ] `/tuition` 수강료 — 휴강 넣고 회차·금액 확인
- [ ] `/consult` 상담 + `/apply` 학부모 신청 양식 링크
- [ ] `/import` 노션 CSV 이관 (데일리리포트 · 하원숙제 · **일정/할일** · **결석/보강**)
      · 일정/할일 — 학사일정DB 또는 직접 만든 엑셀 (제목/종류/분류/날짜/끝날/메모)
      · 결석/보강 — 보강문자DB. 제목에서 결석일을 읽고, 없으면 생성일로 `추정` 표시
- [ ] `/me` 학생용 페이지 + 앱 알림 + 결석 알리기 + **댓글**
      · 학생·학부모가 리포트에 댓글 → 오늘 수업 학생 줄에 💬 표시, 대시보드에도 뜸
      · 선생님이 열면 읽음 처리. 자기가 쓴 댓글은 자기가 지울 수 있음
- [ ] 재원생 목록 `기록` 버튼 (교재 사용 이력·상담 이력)
- [ ] 설정 → 문자 문구 (인삿말·맺음말)

---

## 🟢 앞으로 만들 것 (ROADMAP 5~7단계)

- [ ] **출결 집계** — 월별 출결표, 무단결석 vs 사전연락 구분 통계
- [ ] **보강 관리** — 결석 → 보강 대상 자동 추출, 보강일 배정 화면
- [x] **2026 시험 일정 이관 완료** — `0022_exam_seed_2026.sql` (21개 학교·시험)
      · 1학기 기말은 영어 시험일까지 짝지어 넣음
      · 1학기 중간(4월)은 노션에 영어 시험일이 없어 비워 둠 → 지난 일이라 채울 필요 없음
      · 박문중 2회고사는 노션에 **기간이 없고 영어 시험일(7/7)만** 있어 하루로 넣음 → 확인 필요
      · 2학기는 신정중(10/28~30, 12/14~16)만 노션에 있음. 나머지 학교는 나오면 추가
- [x] **시험 일정** — 학교·학년별 기간(1차) → 영어 시험일(2차) 2단계 입력
      · 시험 기간 정규수업 = 결석 예상 → 반 전체 결석 예정 일괄 등록
      · 영어 시험 전날 = 정규수업이 아니어도 등원 → 등원 일정 자동 생성(전달사항 포함)
- [ ] **내신 트랙** — 학교별 시험 범위(교재 단원 연결), 내신 대비 기간 동안 숙제 전환
- [ ] **월말 리포트** — 한 달 진도·출결·점수 종합
- [ ] **성적 추이** — 단어/문장 점수 그래프
- [ ] **재시험 관리** — 미통과 학생 자동 추출
- [ ] **교재 소진 예상** — 남은 단원으로 언제 끝날지 계산
- [ ] **알림톡 연동** — 템플릿 승인 후
- [ ] **학부모 계정** — 지금은 학생 계정만. `parent_student` 테이블은 준비됨
- [ ] **강사·조교 권한 분리** — 지금은 원장/강사/조교 모두 거의 같은 권한

---

## 🔧 알고 있는 한계

- [ ] **단원 없는 교재**는 페이지로만 진도 기록 (`지금 84 / 160p`)
- [ ] **아이폰 앱 알림**은 홈 화면에 추가해야 켜짐 (애플 정책, 우회 불가)
- [ ] **앱 알림은 학생이 켜야** 감 → 문자를 완전히 대체하지 못함
- [ ] 노션 이관은 **학생 이름이 정확히 같아야** 매칭됨 (다른 건 건너뛰고 목록 표시)
- [ ] 발송 API 붙기 전까지는 **복사 → 문자 앱** 방식
- [x] 메뉴 정리함 — 늘 보이는 4개(대시보드·오늘 수업·수업 준비·발송) + 묶음 4개
- [x] **모바일 메뉴 고침** — 한 줄에 다 넣으니 글자가 세로로 쪼개졌음.
      900px 아래에서는 `☰ 메뉴` 하나로 접고, 480px 아래에서는 이름도 숨김

---

## 🧪 가상 시뮬레이션으로 찾은 것 (2026-07-26)

3명 × 8월 한 달을 실제 판정 로직으로 돌려봄.

- [x] **숙제 검사 사슬이 끊기는 버그** — 8/3 숙제 냄 → 8/5 결석(출결만 저장) → 8/10에
      검사 대상이 0개가 되어 **낸 숙제가 사라졌다.** 빈 리포트가 중간에 끼어도 같았음.
      → "배정이 있었던 가장 최근 리포트"를 찾고, 그 뒤에 검사된 항목만 제외하도록 고침
- [x] **결석 학생 처리** — 결석이면 검사할 숙제가 없으니 완료로 보고,
      오늘 수업에서 `결석 기록` 한 번으로 발송 목록(결석 안내)에 올라오게 함
- [x] **회차 특이사항 알림 — 3개월 누적으로 계산** (2026-07-26 수정)
      · 달마다 따로 판정하지 않는다. 8월 7회 + 9월 9회 → **"9월까지 합치면 딱 맞음.
        보강도 휴강도 하지 말고 그대로 수업하면 됩니다"** 를 알림 안에 같이 적어준다
      · 3개월을 다 합쳐도 남을 때만 → "그냥 하면 서비스, 쉬려면 휴강으로 지정" + 날짜 선택
      · 상쇄되는 달은 초록 `회차 맞음` 태그. 휴강 지정 버튼도 안 뜬다
      · 대시보드에도 상쇄 구간의 **첫 달에만** 한 줄로 뜬다 (같은 말 반복 방지)
      · 반 머리에 `3개월 합계 24회 / 기준 24회 — 딱 맞음`

## 🖐 업무 동선 시뮬레이션에서 나온 것 (2026-07-26)

`scripts/simulate-workflow.mjs` — 하루 흐름을 그대로 따라간 것.

- [x] 재시험·보강을 오늘 수업 화면에서 바로 잡게 함 (일정 화면으로 안 나감)
- [x] 숙제 검사 ○△✕ 를 한 번에, 점수는 틀린 개수로, ⟳ 지난번과 같게
- [x] 오늘 수업 맨 위 '수업 전에 볼 것' · 맨 아래 '발송하러 가기'
- [x] **반 전체 숙제 배정은 만들었다가 뺐다** — 전원 개별진도라 쓰면 안 됨
- [x] **채점 결과 입력칸도 뺐다** — 피드백은 수업 중에 말로 한다
- [ ] **단원 없는 교재**는 범위를 매번 손으로 친다 → 단원 입력이 답
- [ ] **시험 범위가 화면에 없다** — 내신 기간에 어디까지 낼지 판단 불가 (내신 트랙 필요)

## 🕐 시간대 — 고침 (2026-07-26)

- [x] **날짜가 하루씩 밀리고 있었다.** `new Date("2026-09-24T00:00:00+09:00").getDate()` 처럼
      쓰고 있었는데, Vercel 서버는 UTC 라 실제 시각이 9/23 15:00Z 가 되고
      `getDate()` 는 서버 기준이라 **23** 을 돌려줬다. 요일도 목→수로 밀렸다
- [x] 화면 날짜만 틀린 게 아니라 **공지 대상 학생이 잘못 잡혔다** (요일로 반을 고르므로)
- [x] `lib/day.js` 로 모아서 고침 — 날짜는 UTC 자정으로 파싱하고 UTC getter 로만 읽는다.
      "오늘"만 `Intl` 로 서울 날짜를 뽑는다. **어느 시간대 서버에서 돌든 같은 답**
- [x] `scripts/check-timezone.mjs` — TZ 를 바꿔가며 같은 값이 나오는지 확인하는 스크립트
      (UTC · 서울 · 뉴욕 · 키리바시 · 미드웨이 · 런던 · 시드니 전부 통과)

## 🐛 화면이 안 열리던 것 — 고침 (2026-07-26)

원장님이 "상담·오늘수업이 안 열린다" 고 알려주셔서 찾음. **빌드는 통과하는데 열면 터지는** 종류였다.

- [x] `/today` — 시간대 리팩터링에서 `target` 변수를 지웠는데 아래에서 아직 쓰고 있었다
- [x] `/today` — 보강 학생 목록에서 출결 행(`a`)을 잃어버리고 `a.makeup_of` 를 참조했다
- [x] `/consult` `/tasks` `/homework` — **`"use server"` 파일에서 상수를 export** 하고 있었다
      (`STATUS`, `CATEGORIES`). Next 는 use server 파일에서 **async 함수만** 내보낼 수 있다.
      → `app/consult/status.js`, `app/tasks/categories.js`, `app/homework/categories.js` 로 분리
- [x] `/plan` `/requests` — `dowOf` 를 안 가져와서 결석 예정 기간 등록이 터졌다
- [x] `/tasks` — 없는 함수(`rosterOf`)를 부르고 있어 전달사항 만들기가 터졌다
- [x] **`scripts/check-pages.sh`** 를 만들어 앞으로는 배포 전에 잡는다
      (선언 안 된 이름 · use server 잘못된 export · 빌드)

> 교훈: `next build` 통과 = 화면이 열린다 가 **아니다.**
> 빌드는 문법과 import 만 본다. 위 두 종류는 런타임에만 드러난다.

## 📣 공지 — 학생용 · 학부모용 (2026-07-26)

- [x] `notices.kind` 를 그대로 쓴다 (SQL 추가 없음)
      · `deliver` = **학생용** → 숙제 문자 + 수업 중 전달 체크
      · `notice`  = **학부모용** → 데일리리포트
- [x] 전에는 둘 다 리포트에만 들어가고 숙제 문자에 학부모용이 섞여 나갔다
- [x] 오늘 수업에서 '학생에게 말할 것' 을 **출결 바로 아래** 노란 칸으로 크게 표시.
      체크하면 흐려지고 취소선. 학생 줄에도 `말할 것 N` 표시

## ⚠️ 경고 · 오늘 마무리 (2026-07-26)

- [x] 경고 = 지각 / 숙제 미제출·미흡 / 단어시험 미통과. **하루 1회**. 3회 → 반성문
- [x] 경고는 저장 안 하고 리포트에서 매번 계산 (리포트 고치면 경고도 같이 맞게)
- [x] 저장하는 건 판단뿐 — 빼주기 / 반성문 씀 / **유예(봐준 이력이 남음)**
- [x] 기준은 `설정 → 경고 · 반성문 규칙` 에서 (몇 회, 단어 통과선 %, 셀 항목)
- [x] 늦귀가과제 이름 = **오늘 마무리** (`lib/reportText.js` 의 `STAY_LABEL` 한 줄)
- [x] △·✕ 찍으면 자동 제안 → 다 함 / 숙제로 / 넘어가기
- [x] 단어 기준 = **오답 10% 이내면 통과** (맞은 비율 아님). 0025 에서 정정
- [ ] 유예를 몇 번까지 봐줄지 규칙이 필요한지 (지금은 제한 없음, 이력만 남음)

## 🔤 단어시험 방식 (2026-07-26)

- [x] 네 가지를 합쳐 100% 배분 — 객뜻 / 주뜻 / 객단 / 주단 (0 가능)
- [x] 주관식 영단어는 **첫 글자 힌트 있음/없음**
- [x] (학생, 교재, 회독) 하나에 설정 한 줄. 교재 시작할 때 + 회독 올릴 때 정함
- [x] 오늘 수업 단어책 진도 아래 라벨 — `2회독 · 주뜻50 주단50(첫글자)`
- [x] `⟳ 다음 회독으로` → 회독 +1, 끝낸 단원 비움, 방식 다시 설정
- [ ] 회독을 올리면 진도를 비우는 게 맞는지 (지난 회독 기록을 남길지) — 써보고 결정

## 📝 쓰면서 발견한 것 (여기에 적기)

- [ ]
- [ ]
- [ ]
