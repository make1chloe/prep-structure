# 규칙에 어긋난 곳 — 2026-09-02

> 규칙 번호는 [`docs/규칙.md`](규칙.md) 를 따른다.
> **여기 오르는 것은 실물로 확인한 것뿐이다** (대전제-0). 의심만 한 것은 아래 「반박된 것」에 남긴다.
>
> ⚠️ 규칙 123개 중 **13개**를 이 문서가 다룬다. 나머지는 아직 안 훑었다.

---

## 어긋난 곳

### ① 규칙이 레포에 없었다 — ✅ 고침 (2026-09-02)
규칙이 **레포 밖 계획 파일과 코드 주석에만** 살았다. `v2` 에 `CLAUDE.md` 도 `PRINCIPLES.md` 도 없다
(`main` 에만 있다). 그래서 grep 이 안 되고, 검사가 못 걸고, 새로 시작하는 사람이 계획 파일을 찾아야 했다.
→ `docs/규칙.md` 신설. **규칙마다 번호**를 붙여 어긋난 곳을 번호로 가리킬 수 있게 했다.

### ② 원칙-1 · 표-9 — 아이의 「다 했어요」와 원장님 ○ 이 **같은 칸**이다 【치명】
`supabase/migrations/0016_rls_rest.sql:43` ↔ `app/me/actions.js:63`

`child_done` 정책이 아이에게 `day_item.status='done'` 을 허용하는데, **그 값은 원장님 ○ 과 같다.**
한 칸에 두 사실이다(원칙-1). 그리고 아이가 값을 쓰는 자리인데 「그 한 칸만 빼고 비교」가 아니다(표-9).

**재현했다** — 마감한 판에서 아이 계정으로 자기 숙제에 ○ 이 그대로 들어갔다.
진도까지는 **안 간다**(확인함). 문제는 **원장님 검사 목록에서 사라지는 것**이다.
정책이 `done` 만 허용해 **아이 손으로 되돌릴 수도 없다.**

⚠️ **지금은 안 터진다** — `day_sheet` 1,954판이 전부 마감 안 됨이라 `sheet_visible()` 이 늘 거짓이다.
**마감을 처음 켜는 날 문이 열린다.**

### ③ 0-3 — 낙관적 잠금이 **어디에도 없다** 【큼】
- 버전·잠금 칸: **없다**
- `UPDATE … where … updated_at = $` 로 견주는 자리: **0곳**
- 대신 있는 것은 `closed_at is null` 류 9곳 — 그건 「마감 뒤엔 못 고친다」이지 「내가 읽은 그 줄이 그대로일 때만」이 아니다

원장님은 **수업 중엔 폰, 자리에선 PC**를 쓰신다. 지금은 **나중 저장이 먼저 저장을 통째로 덮는다.**
계획이 예상 문제 18번으로 이미 적어 둔 자리다. **지키는 검사도 없다.**

### ④ 원칙-1 — 같은 규칙이 두 벌 【큼】
`lib/todo.js:622` ↔ `supabase/migrations/0009_rls_progress.sql:31`

「전국 시험이면 전원, 학교 시험이면 그 학교 아이만」이 **lib SQL 과 접근 규칙 두 곳**에 각각 적혀 있다.
한쪽만 고치면 화면과 DB 가 다른 답을 낸다.

### ⑤ 처음-9 — 이관값 **7표 800줄**이 「옮겨온 것」을 안 남긴다 【큼】
`makeup 368` · `todo 239` · `score_wrong 122` · `inquiry 53` · `class_schedule 10` · `notice 6` · `holiday 2`

`import_batch` 칸도 없고 `import_map` 도 안 덮는다. 그래서 **재적재가 무엇을 지울지 못 가린다** —
대전제-1 의 「몇 번을 돌려도 같은 결과인 전체 재적재」가 이 표들에서 깨진다.

### ⑥ 확정-㊲ — `v2.exam_word` 가 서 있는데 **아무도 안 쓴다** 【큼】
`supabase/migrations/0075_screens.sql:105` — 낱말 6개가 들어 있으나 `lib`·`app` 어디도 안 읽는다.
확정-㊲ 의 「이름으로 갈래를 판정하고 **그 낱말을 원장님이 고치신다**」가 **표만 서고 판정이 없다.**
화면도 스스로 밝히고 있다: `app/schedule/page.js:365` 「지금은 고칠 곳이 없습니다」.

### ⑦ 대전제-4 — `freezeDay` 를 화면이 안 불렀다 — ✅ 고침 (2026-09-02)
`lib/day.js` 에 한 벌이 있는데 화면이 아무도 안 불러 **하루 동선(②③)이 저장되지 않았다.**
오늘 단추를 달고 실물로 24줄이 서는 것까지 확인했다.

### ⑧ 검사-⑯ 이 없다 【작음】
「`excel`·`fixture` 줄이 재적재가 지우는 줄을 가리키면 실패」를 보는 검사가 **없다.**

### ⑨ 대전제-1 — 묶음 표시 없는 줄 1개 【작음】
`v2.student_book` 에 `import_batch` 가 빈 줄 하나 — `zz_시험_학생` × 「빈순삽함 실전 100」, 2026-09-02 02:48.
리허설이 만든 줄인데 `fixture` 로 안 찍혔다.

### ⑩ 뼈대-5 — 임계값 하나가 코드에 박혀 있다 【작음】
`lib/close.js:285` 의 `n >= 3` (확정-㊳ 「3회 연속 메모로만」). `v2.auto_rule` 은 **0줄**이라
원장님이 이 숫자를 못 바꾸신다.

---

## 지켜지고 있는 것 — 확인했다

| 규칙 | 확인 |
|---|---|
| **대전제-9** | `position:fixed`·`pushState`·`createPortal` — 주석을 지우고 세니 **0곳** |
| **대전제-10** | `alert`/`confirm` — **0곳** (걸린 둘은 「안 쓴다」고 적은 주석이었다) |
| **처음-1** | `fee_rule` 에 `from_date·to_date·amount·base_sessions·per_session` |
| **처음-4** | `push_sub` 에 `agreed_at`·`revoked_at` 둘 다 |
| **처음-5** | `class_member` PK 에 `from_date` · `student_book` 유일키에 `from_date` |
| **뼈대-2** | `auto_key` 에 **`base_date`** — 이것이 없으면 되풀이가 한 번만 생긴다 |
| **0-6** | `countDates` 가 `past`/`future` 를 갈라 준다 |
| **확정-㊺b** | `stop_rule` — elem 4주 · middle 4주 · high 6주 그대로 |
| **폰-2 · 폰-9** | `autoFocus` 0곳 · `opacity` 로 흐리게 0곳 |

## 의심했다가 **반박된 것** — 목록에 안 올린다

| 의심 | 왜 아닌가 |
|---|---|
| `progress` 2,963줄에 이관 표시가 없다 | **`last_by='import'` 로 전부 표시**돼 있다 |
| `day_item`·`units`·`books`·`score` 도 표시가 없다 | **`import_map` 12,015줄**이 덮는다 |
| 유일키 4개가 빈 칸을 허용하는데 `nulls not distinct` 가 아니다 | `students.profile_id`·`books.code` 는 **빈 값이 여럿이어야 정상**이고, `stop_rule.level`·`cc_planner.set_name` 은 **빈 줄이 0개**다 |
| `quiz.total`·`cut_pct`·`percentile`·`q_count` 가 원칙-5 위반 | 전부 **밖에서 오거나 원장님이 정하는 값**이다 — 세어 나오는 값이 아니다 |
| `lib/files.js` 의 `3`, `lib/routine.js` 의 `2` 가 뼈대-5 위반 | 학년도 계산(3월)과 이름 표시 규칙이다 — 임계값이 아니다 |

---

# 전수 훑기 결과 (2026-09-02, 26담당 · 반박 통과 8건)

> 앞의 ①~⑩ 과 **겹치지 않는** 것만 적는다. 각 줄은 반박을 한 번 이기고 살아남았다.
> ⚠️ **뿌리**: 규칙 원본이 git 에 없고, 검사 56종 중 그 파일을 읽는 것이 **0개**다.
> 8건 중 5건은 지키는 검사가 아예 없고, **3건은 검사가 초록인 채로** 지나갔다.

## ⑪ 0-10 — 메뉴 한 벌이 **어디에도 안 붙었다** 【큼 · 오늘 아픈 유일한 자리】
`app/nav.js:17-47`(아무도 안 부름) · `app/layout.js:21`

`grep -rn '<Nav' app` → **0건.** `layout.js` 는 `<body>{children}</body>` 그대로다.
- **대시보드 `/` 에 밖으로 나가는 링크가 0개** — 원장님이 로그인해 서는 첫 화면에서 아무 데도 못 간다
- `/today`·`/schedule` 은 링크가 전부 자기 자신 (두 화면 1,108줄, 매일 여는 화면)
- **퀵메모가 10개 화면 어디에도 없다.** `app/nav.js:35` 가 `{onQuick && …}` 라 Nav 를 붙여도 **단추가 안 그려진다**
- 로그아웃은 10개 중 5개에만

`app/globals.css:509-532` 에 배색이 다 서 있다 — **만들어 놓고 안 걸었다.**
⚠️ **`scripts/check-menu.mjs` 는 18건 전부 ✅ 를 준다** — 상수와 파일 존재만 본다. **거짓 초록.**

## ⑫ 대전제-0 — 화면이 **없는 제약을 있다고** 말한다 — ✅ 고침 (2026-09-02) 【큼 · 내 잘못】
`app/today/page.js:634-639` · 베낀 `scripts/check-screen-today.mjs:530` · 반대로 과장한 `docs/원장님-정하실-것.md:134`

오늘 화면이 「영역 메모를 담을 칸이 **DB 에 없습니다**」를 사실처럼 띄운다. **표는 있다** —
`v2.day_area_memo`(0079) 이고 `lib/day.js:390·404` 에 판단 한 벌이 있다.
반대편에서 `docs/원장님-정하실-것.md:134` 는 「✅ **됩니다**」라고 적는다.
**둘 다 틀렸다.** 참인 문장은 하나뿐이다 — **「표도 판단도 있는데 입력칸을 아직 안 붙였다」**
(`grep -rn 'areaMemos|putAreaMemos' app/` → **0건**).

## ⑬ 대전제-6 — `lib/day.js:386` 의 `delete` 는 **화면에서 부르면 터진다** — ✅ 고침 (2026-09-02) 【잠복 · 내 잘못】
`lib`·`app` 통틀어 `delete from v2.` 는 **이 한 줄뿐**인데,
`authenticated` 가 DELETE 를 가진 v2 표는 **0개**다(전수 확인). 화면은 `authenticated` 로 돈다.
→ 화면이 영역 메모를 붙이는 날 **첫 저장에서 permission denied.**

⚠️ **`scripts/check-areamemo.mjs` 가 초록인 까닭은 그 검사가 `postgres`(표 주인)로 돌기 때문이다.**
같은 검사가 「지우기는 안 준다(대전제-6)」와 「빈 줄은 지운다」를 **동시에 초록**으로 준다 — 서로 반대인데.

## ⑭ 0-4 — 검사 ○△✕ 에 **트랜잭션이 없고, 실패해도 「저장됐다」고 한다** 【큼】
`app/today/actions.js:64-92` · 짝 `app/today/ui.js:41-43`

한 번 누르면 `day_item` · `progress_part` · `progress` 셋을 건드리는데 **`begin` 이 없다**(`{tx:false}`, 까닭 주석 없음).
더 아픈 쪽 — `fromCheck` 가 실패해도 `:90-91` 이 `ok:true` 를 돌려주고 화면은 `!r?.ok` 만 본다.
**진도가 하나도 안 올라가도 ○ 이 켜진 채 아무 말도 안 뜬다.**
같은 함수 `:75-78` 은 `day_item` 0줄을 검사-⑪ 대로 실패로 되돌린다 — **한 함수 안에 잣대가 둘.**

## ⑮ 속도-4 — 「읽음」이 **전 학생 공용 200줄**로 잘린다 — ✅ 고침 (2026-09-02) 【중간】
`app/send/sql.js:116` · 화면 `app/send/screen.js:152·380·394`

접기 머리는 **안 자른 총수**를 적고 몸통은 200줄만 본다 — 넘는 순간 갈린다.
「안 읽은 집만 보기」가 그 200줄 안에서만 걸러 **「안 읽은 줄이 없습니다」를 오류 없이** 띄운다.
자른 사실을 밝히는 자리·더 보기·페이징 **0곳.**
⚠️ 지금 `notify_log` 0줄이라 안 문다. 데일리 한 번에 ~21줄 → **열흘 남짓에 200을 넘는다.**

## ⑯ 처음-3 — **「파기한 날」 칸이 v2 어디에도 없다** — ✅ 고침 (2026-09-03) 【중간】
넷 중 셋은 있다(보관 기한 `purge_map.after_days` · 파기 예정일 `file.purge_on` · 파기 목록 표 69줄).
`lib/purge.js` 의 `planFor` 가 만드는 문장은 `set 칸=null|mask` 와 `set state='purged'` 뿐 — **날짜를 찍는 걸음이 없다.**
⚠️ 파기가 아직 한 번도 안 돌았다. **첫 파기가 도는 날 이전**이 마지막 기회다.

## ⑰ 검사-⑱ — 매핑표에 **짝 없는 19줄** 【중간】
`supabase/migrations/0048_import_day.sql:49-51`

데이터 insert 는 `where d.import_batch='import'` 로 좁히는데 **매핑 insert 는 안 좁혔다.**
→ 안 옮긴 옛 줄에도 「새 줄 X 가 되었다」가 적혔다. 실측 **19줄**(전부 `new_id`=옛 id, `skip_why` null).
⚠️ `0049_verify_day` 의 줄수 대조는 **양쪽을 같은 조건으로 좁혀** 4,131=4,131 로 초록이 된다 — 원리적으로 못 잡는다.

## ⑱ 문서 규약 — **규칙 원본이 커밋돼 있지 않다** 【메타 · 뿌리】
`git status docs/` → `?? docs/규칙.md` · `?? docs/규칙-어긋난곳.md` — **이 맥 안에만 있다.**
검사 56종 중 이 파일을 읽는 것 **0개**. 규칙 옆에 검사 이름이 적힌 줄도 **0개**(204줄이 그걸 요구하는데도).
그 결과 속도 상한이 **두 벌**로 산다 — `docs/규칙.md:110` 과 `scripts/check-fast.mjs:33-37`
(그 두 번째 벌의 `/report` 는 발송 화면의 **옛 이름**이라 숫자는 맞고 이름표만 낡았다).

---

# ⚠️ 검사가 있는데 **거짓 초록**인 자리 셋

| 검사 | 왜 초록인가 |
|---|---|
| ~~`check-areamemo`~~ | ✅ **고침** — 이제 화면과 같은 손(`set role authenticated`)으로 돈다. delete 로 되돌려 보니 **빨개진다** |
| `check-menu` | 상수와 파일 존재만 본다. **Nav 가 죽은 채로 18/18 ✅** |
| `0049_verify_day` | 옛·새 **양쪽을 같은 조건으로 좁혀** 늘 같은 수가 나온다 |

> 폰-5 가 경고한 그 꼴이다 — 「**일부러 어기는 본보기를 두고 검사가 잡는지 확인한다**」.
> 셋 다 그 확인을 안 했다.

# 검사가 아예 없는 규칙

- **규칙 123줄 전체** — `docs/규칙.md` 를 읽는 검사 0개 (**여기가 뿌리**)
- **0-4** 트랜잭션 경계 — `markCheck` 를 보는 검사 0건. `check-progress` 는 `{tx:true}` 를 직접 넣어 통과시켜 **부르는 쪽을 안 본다**
- **0-10** 늘 손에 닿는 것 — `nav.js`·`<Nav>` 를 읽는 검사 0건
- **처음-3** 「파기한 날」을 요구하는 검사 0건
- **속도-4** 화면 SQL 에 줄 수 상한이 있나를 보는 검사 없음
- **「화면이 DB 에 대해 하는 서술이 스키마와 맞나」** — 한 종도 없다
- **「lib 의 `delete from` 이 그 표의 `authenticated` 권한과 맞나」** — 없다
- **「매핑표의 `new_id` 가 가리키는 줄이 진짜 있나」** — 없다
- **속도 상한 `/send`·`/schedule`** — `check-fast` 표에 아예 없다
