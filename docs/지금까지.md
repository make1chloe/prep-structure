# v2 — 지금까지 (2026-09-05 밤 · 목업대로 처음부터 — 0단계 ✅ · 1단계 ✅ · 2단계 오늘 수업 **1차 ✅ · 2차-1 루틴 ✅ · 2차-2 시험 ✅ · 2차-3 조절 ✅ · 2차-4 경고 ✅ · 2차-5 진도 체크 ✅**)

> 원장님 9/5: 「이미 구현된 거 다 버리고 … 목업을 그대로 혹은 더 고도화해서」 → 「`docs/목업-구현-프롬프트.md` 대로. 0단계부터.」
> 세션은 잊고 문서만 기억한다 — 다음 세션은 이 문서를 읽고 **3단계 「달·학기」의 다음 화면**(아래 「2차에 남긴 것」 19 다음 — 교재 15·엑셀 15b → 보드 04 · 로드맵 08 · 할 일 06 · 표 06c → 학생 14 · 신규 상담 18 → 자료함 20 · 영상 19)를 잇는다. 끝날 때 이 문서를 갱신한다.
> 원장님 9/5 밤: 「목업 수정때 학생학부모앱은 우선순위에서 밀렸어서 구현시 좀더 고도화필요」 — **학생·학부모 화면(07·08·09·09b·16·19·20)은 목업보다 고도화해서 짓는다.** 차례가 오면 아이·학부모 동선으로 다시 걸어 빠진 것 목록을 먼저 보이고 확인받는다(프롬프트 「정해 둔 것」 표).
> 원장님 9/5 밤: 「지금 옛앱자체를 학생학부모에게 배포하지않았어 그냥 바로 제대로 만들어야 배포를할수가있어」 — **옛 앱은 원장님(·강사)만 쓴다.** 학생·학부모에게는 새 앱이 첫 배포다(인수인계 §10 「전환이 아니라 첫 배포」). 그러니 할 일은 하나 — 27화면을 목업대로 제대로 지어 끝의 정의(프롬프트)를 채우는 것.

## 어디까지 — 0단계 「겉 한 벌」 끝 (2026-09-05)

| 한 것 | 어디 | 확인 |
|---|---|---|
| 옛 v2 앱 지움 — `app/` `lib/` `middleware.js` · 옛 화면·판단에 묶인 검사 51개 (151 파일) | git 역사(11e8b46 이전)에만 남아 있다. 베끼지 않는다(대전제-11) | — |
| **겉 한 벌** — 목업 `<style>` 930규칙 → `app/globals.css` 877 · 겉껍질 53은 `docs/목업/chrome.css` | `node scripts/mockup-css.mjs` 가 브라우저에 물어 갈라낸다. **손으로 안 고친다**(디자인-3) | `check-mockup`: 토큰 312 같음 · 27화면 × PC·폰 화소 같음 |
| 빈 뼈대 — `app/layout.js`(글꼴 하나 · 배색 되살림 `chloe-skin`) · `app/page.js`(자리표시) | | `next build` 통과 (`/` 하나) |
| 검사 다섯 — `check-palette`(토큰 6자리 · 갈색 0 · 대비 174쌍 · 어두움 두 벌 같음) · `check-mockup` · `check-sizes`(높이 한 벌·넘침·겹침) · `check-fonts`(글꼴 1·열 단계·입력칸=본문) · `check-contrast`(배색 5 × 밝음·어두움 열 자리) | `scripts/` — 화면이 생기면 `CHECK_URLS` 로 앱 화면도 본다 | 전부 초록 |
| 남긴 검사 — DB 지킴 8(`v2only` `migrations` `schema` `grants` `sql` `v2-rls` `tables` `residue`) · `sw` · `tz` | `check-all.sh` 는 DB 열쇠(`.env.local`)가 없으면 DB 검사를 **⏭ 건너뜀으로 센다** — 초록이 아니다 | |
| **e2e 틀** — Postgres :55440 · PostgREST :55441 · 인증 흉내 :55442 · 앱 :3300 | `scripts/e2e/` (`fetch` `up` `down` `run` `auth` `token` `shim` `seed`) — 옛 앱 것을 옮겼다 | 옛 앱 표(public, git main) + 마이그레이션 0001~0088 (0081 은 실제 데이터 단언이라 ⏭ · 9000·9001 전환일은 안 돌림) · 리허설 계정 5 · 앱 `/` 뜸 · 화면 검사 셋 통과 |
| 검사가 목업을 고치게 한 것 — 테두리 `--edge` 기본·종이 (부품 3:1) | `docs/목업보다-나아진-것.md` | 아티팩트도 다시 올렸다 |
| ❌ **`check-sw` 는 빨강이다** — 폰에 박힌 옛 서비스워커가 가리키는 길 넷(`/me` `/parent` `/api/push/seen` `/api/icon/192`)이 아직 없다 | 학생·학부모·발송 화면을 지으면 초록이 된다. 검사 통과용으로 길을 지어내지 않는다(CLAUDE.md 4) | **그때까지 v2 를 배포하지 않는다** |

## 1단계 「뼈대」 끝 (2026-09-05 밤)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표는 v2 그대로** — 사람·권한 32칸·교재·진도·시험·큐(job_queue)·「오늘 돌았나」(day_ran)가 다 있다. 0100 은 셋만 더한다: `profiles.must_change_pw` + `password_changed()` · `v2.rule`(임계값) · `v2.enqueue()` | `supabase/migrations/0100_new_app_skeleton.sql` (멱등) | check-tables · check-grants · check-schema 초록(눌러보기 DB) |
| ⚠️ 9/5 밤 정정 — 처음엔 스키마를 하나 더(v3) 세웠다가 걷었다. v2 에 같은 표(profiles·role_access·audit·job_queue·day_ran)가 이미 있어 **두 벌**이 됐다(원칙-1). 실제 DB 에는 닿기 전이었다 | | |
| **로그인 00** — 카드 셋(학생 chloe+4자리 · 학부모 전화번호 · 원장 강사 조교 이메일), 꼬리 도메인은 화면에 없음, 틀리면 그 카드에만 · 비밀번호는 주소에 안 남음 | `app/login` · `lib/roles.js` toLoginEmail | e2e/screens |
| **처음 비밀번호 바꾸기** — must_change_pw 면 /password 로 보내고 안 바꾸면 못 넘어감 · 0000·짧은 것 거절(최소 글자는 v2.rule) | `app/password` · `v2.password_changed()` | e2e/screens |
| **세션 한 벌** — sessionUser(쿠키) · whoami · guard. `auth.getUser()` 0 | `lib/session.js` · `lib/supabase.js`(스키마 v2 한 곳) | check-session |
| **권한** — 열쇠 22개·32칸은 `lib/perm.js` 한 벌, 값은 v2.role_access, 기본값은 코드에 없음. 「누가 무엇을 보나」 화면(원장만) 켬·끔·안 정함 | `app/settings/access` · `v2.can` | check-perm · e2e/screens(강사에게 대시보드 켜기 → 메뉴에 뜸) |
| **메뉴·껍질** — 상단바(이름·역할·메뉴·배색·로그아웃) sticky, 지은 화면만 메뉴에(BUILT) | `app/_shell` · `lib/menu.js` | e2e/screens(로그아웃 있음 · 켠 만큼만) |
| **배색 고르기** — 목업 .skins 그대로, chloe-skin. 상단바가 아니라 **설정 화면**에 둔다(폰 상단바가 세 줄이 되던 것) | `app/_shell/skins.js` · `app/settings` | e2e 캡처 |
| **큐 뼈대** — v2.job_queue(0012) 위에 lib/queue.js: 집기(locked_at 자물쇠 · 읽은 그대로일 때만) · 실패 backoff · 상한은 v2.rule · 넣기는 v2.enqueue() · 크론은 오늘(v2.today)을 받고 day_ran 을 보며 runDue 만 | `lib/queue.js` · `lib/rule.js` · `app/api/cron` | check-queue |
| **눌러보기 확장** — 마이그레이션은 `_ap.mjs`(실제 길, v2.migration 기록) · SSL 켬 · DB 검사를 `DATABASE_URL` 로 여기서도 · 화면 걷기 `scripts/e2e/screens.mjs` 22건 · 로그인한 채로 치수·글꼴·대비 검사(CHECK_STATE) | `scripts/e2e/` | run.sh |
| 답 없던 여덟 | **원장님이 9/5 밤에 다 답하심** — 확정-㊾~56 (①③④⑤ A · ② 앱에서 고르기 · ⑥ 옮겨 쓰기 · ⑦ 그때그때 · ⑧ 받음). 답 없는 자리 0 | |

⚠️ `check-all`(눌러보기 DB 로): 26 중 **빨강 둘** — `check-migrations`(0081 실제 데이터 단언을 로컬에서 안 돌려서 · 실제 DB 에선 초록) · `check-sw`(학생·학부모·발송 길이 아직 없다 · 그 화면을 지으면 초록). 둘 다 지어내서 초록으로 만들지 않는다.

## 2단계 「하루 동선」 — 오늘 수업 **1차** 끝 (2026-09-05 밤, 목업 01 + 03 폰)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 둘** — 0101: `day_sheet.attend` CHECK 를 목업의 다섯(왔음·지각·결석·조퇴·온라인 + 휴강)으로 · 색인 둘(판→항목, 아이→날짜) / 0102: 「한 자리에 항목 하나」 제약(0052)을 걷고 부분 유일 색인 둘(교재·단원 줄 / 글 줄) — 글로 적는 항목이 한 자리에 둘 이상 들어가게 | `supabase/migrations/0101_today.sql` · `0102_day_item_free.sql` (멱등) | check-sql · check-tables · check-schema |
| **판단 lib 한 벌 다섯** — `day.js`(오늘 · 반·아이 · 판 세우기 · 마감 · assertOpen) · `attend.js`(출결 쓰는 길 **하나**, 값을 거른다) · `homework.js`(○△✕ · 어디까지 시작만/절반/거의 다 · 나머지 → 오늘 학습으로/다음 숙제로/남아서 · 항목 더하기 · 미루기) · `late.js`(늦귀가 사유·예상 귀가 +20/+40/+1시간 · 보내기 = 큐 `late_notice`) · `status.js`(「검사 안 본 것」 = status 없음/none) | `lib/` | check-attend(검사-②) · check-close(검사-⑤) · check-fast(검사-㉝) |
| **화면 01** — 상단 알약(날짜 · 반 시작–끝 · N명 · 검사 안 본 것 N) · 학생 줄(이름·학년 · **출결 세그먼트 낙관적** · 학원 N·숙제 N · 상태 알약 마감됨/결석·보강 안 잡힘/검사 n/m 남음 · 펴기) · 카드 **1 숙제 검사**(○△✕ → 어디까지 → 나머지는) · **2+3 오늘 학습·오늘 숙제**(좌우, 폰은 위아래 · 미루기 · 항목 더하기) · **3b 늦귀가**(적어 두기 · 📨 지금 보내기 · 보냄/보내야 함) · **✉️ 부모님께 글**(임시 저장 · 저장하고 마감) · 마감 뒤 읽기만 · 오늘 수업 없음 · 아이 없는 반 | `app/today/page.js`(그리기만) · `row.js`(클라이언트) · `actions.js`(손 — lib 만 부른다, 실패는 그 자리에서 말한다) · 메뉴 `lib/menu.js` BUILT | e2e/today **18건**(검사-㉜) · 치수·글꼴·대비 검사에 `/today` · 캡처 `.tmp/e2e-today-{1280,390}.png` |
| **판이 서는 때** — 화면을 열면 오늘 오는 아이의 판이 선다(있으면 그대로). 설 때 「아직 검사 안 한 지난 숙제 전부」(30일 안, 원장님 9/3 ②)가 검사 줄로 온다 — 조각이 원본을 가리킨다(`carry_of`) | `lib/day.js` roster → openSheets | e2e: 어제 숙제 둘이 검사 줄로 · 「검사 안 본 것 2」 |
| **속도** — **4단**(로그인 확인 → 오늘 → 반·아이 **한 조회**(중첩 embed) → 판 **한 조회**) · 조회 **다시 열기 4 · 처음 열기 8**(e2e 가 PostgREST 요청 로그로 센다, 상한 20) · 파도는 lib 안(세우기 ∥ 지난 숙제 ∥ 검사한 것) | `lib/day.js` · `scripts/check-fast.mjs` · `scripts/e2e/up.sh`(PostgREST `log-level = info`) | check-fast · e2e/today |
| **눌러보기 손질** — `_ap.mjs` 가 0051 전 파일을 기록 표가 생긴 뒤에 적는다(빈 DB 에선 check-migrations 가 0001~0050 을 「안 돌린 것」으로 세고 있었다 — 이제 0081 하나만) · `check-v2-rls` 「강사가 아이를 다 보는가」 기대값을 박힌 2 대신 센 것으로(seed 학생이 fixture 가 되며 3이었다) · PostgREST 요청 로그 켬 | `scripts/_ap.mjs` · `scripts/check-v2-rls.mjs` · `scripts/e2e/up.sh` | check-all 21 중 초록 19 |
| ⚠️ 9/5 밤 정정 — 처음엔 반→아이 두 조회 · 판을 세운 뒤 다시 읽어 **6단(처음 9단)** 이었다(같은 조회를 두 번 하면 React 요청 메모가 첫 답을 되주는 것을 순서로 피하고 있었다). 반·아이를 한 조회로, 「있나」와 판 읽기를 하나로, 세운 판은 다시 안 읽고 손에 든 것으로 그린다 → 4단 | | |

## 2차-1 「루틴 자동 깔기」 끝 (2026-09-05 밤 — 확정-⑨: ① 숙제 검사가 방아쇠, ②③ 은 저절로)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0103** — `v2.sheet_book`(판×교재: 깔린 때 `laid_at` · 메모 둘 · 회차 눈금 `waves`) · `day_item.off`(뺀 줄 — 지우지 않고 내린다, 대전제-6 · 검사-⑩) · `day_sheet.load_mode`(그대로·필수만) · `v2.todo_units()`(안 한 소단원 차례 — `cursor_of` 를 이 위에 다시 세워 CTE 두 벌을 막음) · PostgREST 계산 칸 `units.label/short/code`(이름은 `unit_label` 한 곳) | `supabase/migrations/0103_routine_lay.sql` (멱등, 3번 돌림) | check-sql · check-tables(표-유도 88) · check-schema |
| **판단** — `lib/routine-plan.js`(순수: 덩어리는 같은 대단원 안 per_session 개 · 자리마다 줄 · 필수만은 묶음이 비면 줄이지 않음 · 교재 상태 셋 · 회차 셋씩 · off) · `lib/routine.js`(읽고 넣기: 학생루틴 있으면 그것, 없으면 영역루틴(확정-㉒) · 한 번 깔린 교재는 다시 안 깖 · 줄이기·교재 상태·회차·메모 손) | `lib/` | **check-routine 24건**(검사-㉞) · check-close 에 routine.js |
| **언제 까나** — 검사 줄이 다 검사되면(`lib/homework.js checkItem` 끝에서) · 검사 줄이 없는 새 판은 설 때(`lib/day.js openSheets`) | | e2e/today: 검사 하나 남았을 땐 「검사 끝나면 채워집니다」, 둘 다 검사하면 학원 3·숙제 2 |
| **화면 카드 2+3** — 분량 띠(학원 N · 90분이면 한 항목에 X분 · 숙제 N · 줄이기 그대로/필수만) → 교재마다 머리(이름 · N회독 · 대단원 · 진행중/숙제멈춤/교재멈춤) + 좌우(폰은 위아래) 학습·숙제: 회차 세그먼트(다시·오늘·하나 더 / 복습·하나 더·다음만) · 줄(항목 › 대단원 › 소단원 · 쪽 · 문항) · 교재마다 메모 둘(아이 화면에 그대로) · 숙제멈춤이면 「숙제 없음」 · 교재멈춤이면 「N 에 저절로 풀립니다」 → 「그 밖에」(손으로 더한 줄 · 나머지 줄) | `app/today/row.js` WorkCard·BookBlock·Half · `actions.js` mode·stop·wave·memo | e2e/today **31건** · 캡처 |
| **검사가 잡은 것 셋** — ① 회차 세그먼트 글씨가 잘리는데 `check-sizes` 가 초록 → 단추 글씨를 직접 재는 「잘림」 규칙(검사-㉕) · 눈금은 짧은 부호 `units.code`(1-4 · R02) ② 그 규칙이 목업도 잡았다(폰: 늦귀가 보내기 단추 · 조절 모달 소단원 칩) → 목업 CSS 고침(`docs/목업보다-나아진-것.md`) ③ `check-v2-rls` 가 audit_row 안에서 죽음 — 눌러보기 `auth.uid()` 흉내가 빈 claims(`''`)를 json 으로 읽던 것 → 진짜 Supabase 처럼 nullif (seed 에 단원이 생기며 진도 검사 8건이 처음 돌았다 — 통과 26) | `scripts/check-sizes.mjs` · `docs/목업/…html` · `scripts/e2e/supabase-shim.sql` | check-sizes · check-v2-rls |
| **속도** — 다시 열기 5 조회 · 4단 그대로(판 읽기에 단원·항목·판×교재를 붙이고 배정 교재를 파도에 태움) · 처음 열기 9 · 깔기는 검사가 끝나는 그 한 번 5단 | `lib/day.js` · `lib/routine.js` | e2e/today · check-fast |
| **여쭐 것 하나 — 답하심** — 「예습」 줄(㉔) → **B**: 루틴 항목마다 「예습」 표시 칸, 원장님이 켠다(확정-57, 원장님 「b」). 루틴 11 을 지을 때 칸을 만들고, 그때까지 숙제 줄은 전부 복습 | `docs/원장님-정하실-것.md` ✅ | |

## 2차-2 「시험 카드」 끝 (2026-09-05 밤 — 목업 01 🔤 시험 · 📝 다음 시간 시험)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표는 0038~0041 그대로**(`v2.quiz` · `quiz_style` · `quiz_passed` · `style_for` · `quiz_for_report` · `quiz_failed_today` · `quiz_guard`) — 0104 는 손 권한(insert·update — 0038 은 select 뿐이었다)과 계산 칸 셋(`quiz.passed` `quiz.pct` `quiz_style.text`)만 더한다 | `supabase/migrations/0104_quiz_card.sql` | check-quiz 15건(검사-㉟) |
| **판단** — `lib/quiz-plan.js`(순수: 오늘 볼 것/다음 시간 가르기 · 범위 글 · 사유 꼬리표) · `lib/quiz.js`(내기 · 고치기 · 보기 → 상태는 SQL 판정으로 · 못 넘으면 재시험 줄(더 어렵게, 다음 회독 방식) + 늦귀가 사유 · 건너뜀 · 통과하면 걷기) · `lib/late.js tagReason`(사유 한 줄에 꼬리표 붙였다 떼기 — 사유는 이 줄 하나가 원본) | `lib/` | check-close 에 quiz.js · check-quiz |
| **화면** — 🔤 **시험 · 시작하자마자**(지난 시간에 낸 것 · 오늘 본 것 · 재시험: 틀린 개수·전체 → 「18/20 · 90% · 통과」 · 회독·방식·통과선 · 미통과 알약 · 📄 재시험지 만들기 → 「재시험 대상 ✓」 · ⏭ 오늘은 재시험 건너뜀) · 📝 **다음 시간 시험**(첫 교재의 숙제 반쪽에: 갈래 · 범위(교재 = 오늘 학습 소단원 / 직접) · 전체 개수 · 통과선 · 「리포트에 안 나갑니다」 경고 · ⚙️ 방식 줄 · + 시험 더하기) | `app/today/row.js` QuizCard·NextQuiz · `actions.js` quizAdd·Set·Take·Retest·Skip | e2e/today **43건** |
| **속도** — 시험 읽기를 판 파도에 태움(다시 열기 6 조회 · 4단 그대로) | `lib/day.js` | e2e/today |

## 2차-3 「조절 모달 02」 끝 (2026-09-05 밤 — 목업 02 · 9/5 ④)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0105** — `sheet_book.tuned_at`(조절한 때 → 「같은 조절 N번째」) · 규칙 줄 둘 `tune.ask_after`=3 · `chunk.split_from`=40(뼈대-5) · `v2.today_load` 권한(0017 「모든 함수」가 0018 보다 먼저라 빠져 있었다) | `supabase/migrations/0105_tune.sql` | check-sql · check-schema |
| **판단** — `routine-plan.js` tuneUnits(안 한 차례에서 n 개, 뺀 칩 건너뜀, 대단원 안) · loadOf(문항·쪽 합계) · splitPresets(62 → 1-20번·1-31번·전체) · `routine.js` tunePool(읽기: 안 한 소단원·지금 나가는 것·이번에·도는 차례·오늘 분량(today_load 한 곳)·조절 몇 번째·규칙 둘·메모) · applyTune(학습·숙제 둘 다 고른 소단원으로(회차와 같은 손) · 「이번에」는 그 줄의 range_note · 메모 둘 · tuned_at · off 다시 매김) | `lib/` | check-routine **30건** |
| **화면 02** — 교재 머리 「조절 ↗」 → 겹덮개 모달: 소단원 갯수(칩 목록 + 스테퍼 −/직접 입력/+) · 📐 「N개면 오늘 Q문항 · P쪽 — 교재 N권 합치면」 · 🔀 도는 차례(읽기만) · 긴 줄(≥ split_from 문항)의 「이번에」 눈금 셋 + 직접 · 메모 둘 · 적용/닫기 · 「같은 조절 N번째 — 루틴을 고칠까요?」 알약 | `app/today/row.js` TuneModal · `actions.js` tuneOpen·tuneApply · 겹덮개 `.mdlov`(목업 style 에 둠 — 목업은 액자 `.mdlwrap`) | e2e/today **48건** |

## 2차-4 「경고 · 반성문」 끝 (2026-09-05 밤 — 확정-㊼ · 목업 01 ㉗)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0106** — 경고는 저장하지 않는다(대전제-5): `v2.warn_days`(하루 1회 — 지각 · 미제출 1건 · 미흡 N건부터(규칙 `warn.weak_from`=2) · 단어 미통과) · `warn_since`(마지막 「정리」 달의 1일) · `warn_states`(아이마다 횟수·오늘 까닭·최근 사흘·due·유예·오늘 처분) · `warn_band`(월초 띠). 저장하는 것 둘: `v2.reflection`(반성문 처분 — 다음 시간 숙제 · 오늘 남아서 · 유예) · `v2.warn_reset`(달 정리 — reset/keep, 전원 또는 아이, 달의 1일만) · 규칙 `warn.report_at`=3 | `supabase/migrations/0106_warn.sql` | **check-warn 13건**(검사-㊱) · check-tables(표 90) |
| **판단** — `lib/warn.js` warnStates(rpc 한 번) · warnBand · reflect(처분: 남아서면 늦귀가 사유 꼬리표 · 숙제면 「반성문 쓰기」 숙제 줄 · 유예면 다음 경고에 다시 · 바꾸면 되돌린다) · resetWarnings(전원) · `lib/warn-plan.js` DISPOSAL | `lib/` | check-close 에 warn.js |
| **화면** — 학생 줄 알약 「경고 N」/「경고 N · 반성문」 · 3b 늦귀가 카드에 「경고 N회째 — 반성문」(최근 날들) + 처분 세그먼트(다음 시간 숙제 · 오늘 남아서 쓰기 · 유예) · 맨 위 월초 띠 「N월이 시작됐습니다 — N월 경고를 정리할까요?」(전원 정리하기 / 이번 달은 그냥 두기) | `app/today/row.js` LateCard · `band.js` · `actions.js` reflectAs·warnReset | e2e/today **54건** |
| **속도** — 경고 상태를 판 파도에(rpc 한 번) · 월초 띠는 판과 나란히 → 다시 열기 8 조회 · 4단 그대로 | `lib/day.js` · `app/today/page.js` | e2e/today |
| **여쭐 것 하나 — 답하심(㉕ → 확정-63)** — 기준은 학원 기본 3회, **아이마다 따로**(`students.warn_report_at`, 3b 스테퍼) · **한 번 쓰면 그 뒤로 다시 N회를 센 뒤** 또 한 번(0108 — `warn_states.since_written`). 유예는 쓴 것이 아니다 | `supabase/migrations/0108_warn_per_student.sql` · `lib/warn.js setLimit` | check-warn 16건 |

## 2차-5 「진도 체크 02b」 끝 (2026-09-05 밤 — 확정-⑳·㊳·㊶·51 · 검사-⑭)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0107** — 학원 사람의 진도 쓰기 문(`progress`·`progress_part` staff 정책 — 0009·0052 는 아이가 찍는 세 겹 문만 있었다, 권한 0017 은 있었다) | `supabase/migrations/0107_progress_staff.sql` | check-v2-rls · 강사로 넣어 봄 |
| **판단** — `lib/progress-plan.js`(순수: ○ done · △ doing · ✕ 그대로 · 예습·조각은 완료 아님 · done·skip 은 검사로 안 내려감 · 마감 메모 자동 ○ 은 학습 줄만 · 대단원 요약) · `lib/progress.js`(검사 → 진도 `markFromCheck`(checkItem 이 부름) · 02b 나무 `tree` · 찍기·되돌리기 `setUnit` · `skipChapter` · 마감 `autoDoneOnClose`(closeSheet 이 부름, 그 교재만)) | `lib/` | **check-progress 12건**(검사-㊲) · check-close 에 progress.js |
| **화면 02b** — 교재 머리 「진도 체크 ↗」 → 모달: 끝낸 대단원 n/m · 지금 · 안 끝난 소단원 · 대단원 접기(끝냄/지금/건너뜀 꼬리표) · 소단원 ○◐· (찍으면 바로 저장 · 마감된 판은 읽기만) · ✍ 메모로 자동 ○ 후보(오늘 학습 소단원, 01 의 학습 메모가 있으면) · 「교재 없이 한 날」은 01 메모와 같은 값(읽기만) · 이 대단원 건너뛰기 | `app/today/row.js` ProgressModal · `actions.js` progressOpen·Set·Skip | e2e/today **61건** |
| **진도 나무는 표 하나(확정-51)** — 01 의 검사 △ 가 02b 에 ◐ 로, 마감의 자동 ○ 이 02b 에 ○ 로 — 같은 `progress` 표 | | e2e/today |

## 2차-6 「결석·지각 예정 02c」 끝 (2026-09-05 밤 — 확정-㉔ · 목업 02c)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0109** — 결석 예정은 **보강 줄 한 벌**(`v2.makeup`, `of_date` = 빠지는 날 · state todo/set/waived · 물리면 cancelled — 지우지 않는다, 대전제-6) + `reason`·`notified_at`·`created_by` · 한 아이 한 날 하나(부분 유일 색인) · 지각 예정은 새 표 `v2.late_plan`(분 · 사유 · 알림 때 · 물림 때) · **아이의 수업일은 SQL 한 곳 `v2.student_days`**(반 요일 × 소속·시간표 기간 − 휴강 + 잡힌 보강 — 02c 달력·12 일정·회차가 이것을 센다) | `supabase/migrations/0109_attend_plan.sql` | check-sql · check-tables(표 91) · check-v2-rls |
| ⚠️ **0101 정정** — 0101 이 `day_sheet.attend` CHECK 에서 `makeup`(보강으로 온 날)을 빠뜨렸다. 빈 눌러보기 DB 에선 안 잡혔지만 **실제 DB 엔 옛 앱에서 옮긴 보강 출결 368줄**이 있어 거기서 멈춘다 — **실제 DB 에 돌기 전이라 0101 을 제자리에서 고쳤다**(0109 가 CHECK 를 다시 걸지 않는다 — 한 벌, 검사-②에 적음) | `0101` · `lib/attend.js`(makeup 은 앱이 채운다) | check-attend |
| **판단** — `lib/plan-plan.js`(순수: 달력 월요일부터 42칸 · 날마다 표시의 우선 휴강>결석>지각>보강>수업 · 물린 것은 안 뜸 · 보강 글) · `lib/plan.js`(오늘 명단이 읽는 `plansToday` — 빠지는 아이·보강으로 오는 아이·지각 예정 셋을 한 파도 · 02c 가 여는 `planOpen` · 저장 `planSave`(수업일만 · 결석/지각/없음) · 📨 `planNotify` = 큐 `attend_plan_notice` + 알림 때) | `lib/` | **check-plan 12건**(검사-㊳) · check-close 에 plan.js |
| **명단(01)** — 오늘 결석 예정인 아이는 **판이 안 선다**(숙제가 안 나간다) · 줄에 「결석 예정 · 보강 10/18 14:00 / 안 잡음 / 안 잡힘」 · 출결 기본 결석 · 반 알약 「결석 예정 N명/없음」 · 지각 예정은 「지각 예정 N분」 · 출결 기본 지각 · **보강으로 오는 아이는 「보강」 묶음**(반이 없어도 선다 · 출결 makeup) | `lib/day.js` rosterPeople·roster·missing · `app/today/page.js` | e2e/today(학생둘 — 오늘 결석 예정) |
| **화면 02c** — 학생 줄 「📅 예정」 → 모달: 아이 이름 · 반 요일·시각 · ◂ 달 ▸ · **수업일만 고를 수 있는 좁은 달력**(· 수업일 ✕ 결석 ⏰ 지각 ↻ 보강 🚫 휴강) · 고른 날의 **결석**(사유 · 보강 날짜·시각 직접 · 📅 달력에서 고르기 — 아무 날이나, 시각 눈금 10·11·14·16시, 「칸이 차 있어도 넣을 수 있습니다」 · 안 잡음) / **지각**(얼마나 10·20·30분·1시간 · 사유) / **없음**(물림 — 「지우는 것이 아니라 온다로」) · 📨 학부모께 알림(보냄 시각이 남는다) · 범례 | `app/today/row.js` PlanModal · `actions.js` planView·planPut·planSend | e2e/today **81건** · 모달 캡처 |
| **속도** — 오늘 예정 셋을 반·아이 조회와 **같은 파도**에 → 4단 그대로 · 조회 다시 열기 11 · 처음 열기 15 | `lib/day.js` | check-fast · e2e/today |
| **걷기가 잡은 것 셋** — ③ **요일이 서버 시간대에 기댔다**: `lib/day.js weekday` 와 02c 머리의 요일이 `new Date(d+"T00:00:00+09:00").getDay()` 라 UTC(Vercel · 이 눌러보기)에선 **하루 전 요일** — 오늘 수업이 전날 요일의 반을 불렀을 것이다(리허설 반이 매일이라 걷기엔 안 보였고, 02c 캡처의 「9월 6일 **토**」가 잡음). 요일·며칠 뒤를 `lib/day-plan.js`(순수 · UTC 메서드) 한 벌로 · check-tz 가 UTC 로 돌려 본다(검사-㊴). ① 보강 달력(`.mkcal`)은 생성 CSS 에서 `.open` 이 있어야 보인다 — 화면엔 안 보이고 있었다. ② **물린 날을 다시 못 잡았다**: 0043 의 유일키 `makeup_key`(학생·보강 날·빠지는 날, 이관 멱등용)에 물린 줄(cancelled)도 걸려 `duplicate key`. → **물린 줄은 유일성에서 빠진다** 한 규칙으로: `makeup_key` 를 살아 있는 줄만 세는 부분 유일 색인으로(이름 그대로) · `late_plan` 도 같은 꼴(`late_plan_one_live`) · 이관 ⑤ 의 충돌 대상은 **0110** 이 같은 조건으로 맞춘다(부분 색인은 on conflict 에 같은 where 를 적어야 잡힌다 — 빈 DB 에서 `import_rest()` 를 돌려 되돌려 봄) | `lib/day-plan.js` · `lib/day.js` · `row.js` · `0109` · `supabase/migrations/0110_import_makeup_key.sql`(0042 그대로, 한 줄) · `scripts/check-tz.mjs` | check-tz · e2e/today(고른 날 머리의 요일 · 물린 날 다시 결석 · 지각) |

## 2차-7 「✉️ 부모님께 글 — 갈래 다섯 · 길이 · 브리핑 · 미리보기」 끝 (2026-09-05 밤 — 목업 01 ✉️ · 03 폰 · 확정-64)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0111** — 글은 `day_sheet.comment` 그대로(한 벌). 곁에 넷: `comment_kind`(갈래 다섯) · `comment_cap`(50·100·200·300) · `comment_keys`(키워드) · `comment_ai`(AI 초안 원문). 규칙 값 일곱 `comment.cap.*`(보통 100 · 숙제안함 200 · 시험전 100 · 시험후 300 · 늦은밤 50) · `comment.late_from`=21 · `comment.retry`=2 | `supabase/migrations/0111_comment.sql` | check-sql · check-tables |
| **판단** — `lib/comment-plan.js`(순수: 갈래·길이 · **상황이 길이를 먼저 고른다** · 그날 상태에서 갈래가 저절로(늦은밤 › 시험후 › 시험전 › 숙제안함 › 보통 — 시험 전후는 시험 06 뒤에 채우는 자리) · 글자 세기 · 넘으면 문장 끝에서 자르기 · 글 밑에 붙는 줄 · 미리보기 · AI 초안 그대로인가 · AI 재료(사실만)·지시문) · `lib/comment.js`(규칙 값 한 조회 `commentRules` · ✨ `draftComment`: 열쇠는 `v2.integration` anthropic 줄을 서버에서만 · 말투 본보기 `v2.comment_sample` 40줄 + 원장님 조건 `ai_rules` · 넘으면 다시 시킴(최대 2) → 그래도 넘으면 문장 끝에서 자름 · 초안은 `comment_ai` 에, 글은 **비어 있거나 지난 초안 그대로일 때만** 바꾼다) · `lib/day-plan.js seoulHour` | `lib/` | **check-comment 21건**(검사-㊵) · check-close 에 comment.js · check-tz(서울 시) |
| **AI 부르기** — 공식 SDK `@anthropic-ai/sdk`(package.json 에 더함) · 모델은 연동 줄의 `config.model`, 없으면 `claude-opus-5` · 고정 지시문(규칙·본보기)은 프롬프트 캐시 · 되돌아온 오류는 그 자리에서 우리말로(열쇠 틀림 · 모델 이름 · 바쁨 · 못 부름) · 거절이면 「직접 적어 주세요」. 안전 거절 대체(fallbacks)는 **안 켰다** — 학부모 글엔 일어날 일이 없고, 못 시험해 본 베타 인자로 유일한 AI 기능이 막히는 쪽이 더 위험하다 | `lib/comment.js` | e2e/today(열쇠 없을 때의 말) |
| **화면 01 ✉️** — 글 밑에 저절로 붙는 줄(개수 있는 시험만 · 「개수를 안 적어 안 붙습니다」 · 귀가 예정 · 반성문 처분 · 「따로 안 씀」) · 키워드 칸 · 상황 세그먼트(저절로 고른 것엔 「오늘 상태에서 저절로」) · 길이 세그먼트(상황을 누르면 따라온다) · ✨ 브리핑 만들기 · 「N / 100자」(넘으면 빨강) · 글 · 초안이 나왔는데 글이 있으면 「이 초안으로 바꾸기」로 내민다 · 👁 미리보기(학부모 화면 그대로) · 마감 때 AI 초안 그대로면 「그대로 보낼까요?」(그대로 마감 / 고치기) · 마감 뒤 꼬리표 「300자 · 숙제안함」(+「AI 초안 그대로」) | `app/today/row.js` CommentCard · `actions.js` comment·close(값 꾸러미)·commentDraft · `page.js`(글 규칙을 판 파도에) | e2e/today **92건** · 4단 그대로 · 조회 다시 열기 12 · 처음 열기 16 |
| **캡처가 잡은 것** — 폰(390)에서 「상황」「길이」 이름이 **세로로**(상/황) 섰다 — 30px 고정 너비. `white-space: nowrap` 로 고치고, check-sizes 에 **세로 글자** 규칙(띄어쓰기 없는 짧은 낱말의 줄 조각이 둘 이상)을 더해 옛 빌드에서 빨개지는 것을 보고 새 빌드에서 초록을 봤다(폰-5) | `app/today/row.js` · `scripts/check-sizes.mjs` | check-sizes(검사-㉓ 지킴) |

## 2차-8 「학생 줄 머리 · 🗺 영역별 메모 · 📝 단원평가」 끝 (2026-09-05 밤 — 목업 01 남긴 것 6)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0112** — 표는 다 있었다(`day_area_memo` 0079 · `unit_test` 0011 · `schools` 0006). 규칙 값 `unit_test.pass_pct`=80 · 권한(영역 메모 넣기·고치기·거두기 · 단원평가 채점 · 학교·문법 분류 읽기) | `supabase/migrations/0112_row_head.sql` | check-sql · check-grants |
| **판단** — `lib/roster-plan.js`(순수: 「중2 · 신정중」 — 급+학년 · 학교 이름 줄임(중학교→중 · 여자고등학교→여고) · 진도 점(오늘 검사 줄 하나에 점 하나 ○△✕·) · 「N회독째」(2부터, 첫 교재) · 「N일째 안 봄」(안 본 줄 중 가장 오래된 숙제가 며칠 전 것인가 — 낸 다음 날은 아직 아니다) · 🗺 오늘 한 단원(검사 결과대로 ○◐✕ · 오늘 학습 ◐) · 영역 네 칸 · 단원평가 결과) · `lib/area-memo.js`(칸 하나 저장 — 비우면 줄을 거둔다) · `lib/unit-test.js`(오늘 명단의 단원평가 · 맞은 개수 → scored) · `lib/rule.js ruleMap`(접두어로 규칙 여러 개를 **한 조회**) · `lib/day.js todayCfg`(글 규칙 + 단원평가 통과선 한 조회 · 판 조회 모양 `SHEET_SEL` 한 벌 — 영역 메모 embed · 조각의 원본 판 날짜 `carry:carry_of(day_sheet(date))`) | `lib/` | **check-roster 10건**(검사-㊶) · check-close 에 area-memo.js |
| **명단 머리** — 이름 · 「중2 · zz_시험_중」 · 진도 점(검사하면 따라 바뀐다) · 출결 · 「학원 N · 숙제 N」 · 「3회독째」 · 경고 · 상태 말(「N일째 안 봄」이 「검사 n/m 남음」보다 앞선다) | `app/today/row.js` · `lib/day.js`(학생에 `schools(name,level)` 같이) | e2e/today |
| **🗺 진도 · 영역별 메모** — 오늘 한 단원 꼬리표 · 단어·문법·독해·영작 한 마디(칸을 떠나면 저장 · 아이에게 그대로 나가고 ✨ 브리핑 재료 — `facts()` 에 든다) | `row.js` AreaMemoCard · `actions.js` areaMemo | e2e/today · check-comment(재료) |
| **📝 단원평가** — 이 아이에게 출제한 것이 있을 때만 선다(없으면 안 엶) · 문법 분류 이름 · 원장님이 출제한 N문항 · 낸 날 · 통과선 · 맞은 개수 스테퍼 → 「21 / 25 · 84% 통과」 · 채점하면 scored(오늘 것은 그날 보인다) | `row.js` UnitTestCard · `actions.js` unitScore · `lib/unit-test.js` | e2e/today **99건** · 조회 다시 열기 13 · 처음 열기 17 · 4단 |
| **검사가 잡은 것** — ① `day_area_memo` 에 지울 권한을 줬다가 check-grants 가 잡음(대전제-6) → 비운 메모는 빈 글로 남긴다. ② 「세로 글자」 규칙(2차-7)을 목업에도 돌리니 목업 세 곳이 접혀 있었다(학생 화면 단계 이름 「루틴」 · 자료 카드 「마지막」 · 01 의 「상황·길이」) — 목업 CSS 를 고치고 앱 CSS 를 다시 만듦 · 규칙은 자식 없는 글자 상자만 · 표 칸(td·th)과 세운 글(writing-mode)은 뺀다 | `docs/목업/…html` · `app/globals.css`(생성) · `scripts/check-sizes.mjs` | check-grants · check-sizes(목업 · 앱) |

## 2차-9 「늦귀가 나머지 — 실제 하원 · 되풀이 · 안 보낸 채 마감」 끝 (2026-09-05 밤 — 확정-⑭ · 목업 01 3b · 남긴 것 8)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0113** — 규칙 `late.repeat_count`=3 · `late.repeat_days`=21(「3주에 세 번」, 뼈대-5) · `v2.left_at(학생, 날)` 실제 하원 한 벌(등원 걸음 4 · 서울 시각 time) · `v2.late_states(학생들, 날)` 오늘 화면 한 줄씩(실제 하원 · 오늘을 넣어 21일 안 약속(until_at)이 있던 날 수 · 최근 3일 · 묻나) · ⚠️ **학부모 쪽 `late_for_family()`(0071)가 0083 이 걷어낸 `left_at` 칸을 그대로 읽고 있었다** — 부르면 「column l.left_at does not exist」(실측) → 같은 모양으로 다시 짓고 실제 하원은 `left_at()` 에서(09 「실제 하원을 찍으면 여기가 바뀝니다」) | `supabase/migrations/0113_late_rest.sql` | check-sql · check-sql-funcs(새) · 표-유도 |
| **판단** — `lib/late-plan.js`(순수: 차이 분·말 · 「실제 하원 22:05 · 예상보다 25분 늦게」 · 되풀이 띠 문구 · 안 보낸 채 · 마감 전에 묻는 것 한 상자) · `lib/day-plan.js seoulTime·seoulStamp`(시간대 무관) · `lib/late.js`(`sendLate` 가 큐에 넣고 **`sent_at` 을 누른 때**로 찍는다 — 학부모 화면은 이때부터, 푸시는 큐가 뒤에서(10) · `lateStates` rpc · `setLeft` 실제 하원 = arrival 걸음 4 덮어쓰기 — 판이 아니라 **마감과 무관**) · `lib/day.js` 파도에 `lateStates` 한 조회(+1, 4단 그대로) | `lib/` | check-late(검사-㊷ 15건) · check-close(setLeft 예외 — 등원 표) · check-tz |
| **화면 3b** — 되풀이 띠 「🌙 3주 안 3번째 남습니다 — 숙제량을 볼까요?」 + 날들 + 교재마다 「조절 ↗」(02 모달을 그 자리에서) · 「실제 하원」 칸(찍기 → 「실제 하원 22:05 · 예상보다 25분 늦게」 · 마감 뒤에도 찍는다) · 「보냄 21:41」 · ✉️ 마감 전 **한 상자**(「AI 초안 그대로?」 + 「늦귀가를 아직 안 보냈습니다 — 학부모는 모른 채 기다립니다」 — 📨 보내고 마감 / 그대로 마감 / 돌아가기 · 막지 않는다) | `app/today/row.js` LateCard · CommentCard · `actions.js lateLeft` | e2e/today 106건(+7) |
| **검사가 잡은 것** — `check-sql-funcs`(새, 검사-㊸: 읽기만 하는 SQL 함수를 null 인자로 다 불러 본다)가 `late_for_family` 를 잡는다 — 0071→0083 순서라 check-sql 은 못 본 것. 새 앱 DB 에는 아직 안 돌렸으니 실제 사고는 없었다(㉖) | `scripts/check-sql-funcs.mjs` | check-sql-funcs |

## 2차-10 「폰 03 — 줄 끝 저장줄」 끝 (2026-09-05 밤 — 목업 01·03 · 폰-6 · 남긴 것 9)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **줄 끝 저장줄 한 벌** — 목업 01·03 대로 「저장하고 마감 · 임시 저장 · 닫기」가 ✉️ 카드 안이 아니라 **줄(.row)의 끝**에 선다. ✉️ 카드가 단추를 portal 로 내보내고(상태는 카드에 그대로), 줄의 직접 자식이라 **sticky 가 줄 안에서 화면 아래에 붙는다** — 판 끝까지 안 내려가도 마감한다(PC 도 같다). 폰은 `padding-bottom: calc(8px + env(safe-area-inset-bottom))` 로 홈 표시줄 위 · 「닫기」는 폰에서만(PC 는 머리의 닫기) · 묻는 상자(AI 초안 그대로 · 안 보낸 늦귀가)가 눈 밖이면 scrollIntoView 로 데려온다 · 「👁 미리보기」는 글 밑에 남는다 | `app/today/row.js` Row(barHost) · CommentCard(createPortal) · 목업 CSS `.rowbar` → `app/globals.css`(생성) | e2e/today +4(폰 390 sticky · env · 닫기 보임/숨음) · check-mockup |
| **검사가 잡은 것** — 걷기의 폰 검사가 첫 판에서 잡았다: `position` 은 sticky 인데 **안 붙었다**(rect 아래 3184 ≠ 700) — 페이지 틀 `.frame{overflow:hidden}` 이 스크롤 상자가 돼 있었다(폰-3 그대로). 목업 CSS 를 `overflow:clip` 으로(모서리만 자르고 스크롤 상자가 아니다) → 앱 CSS 다시 갈라냄. 「position 이 sticky 인가」만 봤으면 헛통과(폰-5) · check-sizes 「형제 겹침」에서 sticky 를 뺀다(붙는 줄은 겹치라고 있는 것) · check-mockup 은 화소 비교 전에 sticky 를 풀어 둔다(붙는 자리가 캡처 스크롤에 따라 달라 #s1 폰 1.29% 가 헛잡혔다) | `docs/목업/…html` · `app/globals.css`(생성) · `scripts/check-sizes.mjs` | e2e/today · check-sizes(목업·앱 초록) |

## 2차-11 「🏠 대시보드 17 — 빵꾸 막이가 맨 위」 끝 (2026-09-06 — 목업 17 · 속도-상한 대시보드 20 · 5단)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0114** — `v2.todo_counts(학생들, 날)`(교재마다 안 한 소단원 수 — `todo_units` 위에서, 원칙-1) · `v2.dash_ops(날)`(하루 정리 마지막 돈 날 · 큐 실패·대기 · 클래스카드 마지막 수신 · 오늘 마감한 판 수 — 큐는 사람이 못 읽어 definer, 학원 사람에게만 집계 숫자) · `v2.routine_areas(학생들)`(학생·영역 루틴이 있는 영역 한 조회). ⚠️ 첫 판이 **조회 21** 로 상한을 넘어 둘을 접었다 — 상한을 올리지 않았다 | `supabase/migrations/0114_dash.sql` | check-sql · check-sql-funcs · 표-유도 |
| **판단** — `lib/dash-plan.js`(순수: 빵꾸 막이 세 까닭 · 날짜 글 · D-day · 시험 줄 · 클래스카드 수신 · 하루 정리 · 「어제 21:40」 · 반 줄 `classLabel` — 오늘 수업 알약도 이것) · `lib/dash.js dashboard()`(반·아이 한 파도 → 나머지 13 조회 한 파도: 교재 · 루틴 있는 영역 · 안 한 소단원 수 · 늦귀가 · 경고 · 판 · 단원평가 · 재시험 · 안 돌고 있는 것 · 보강 · 시험 · 남기실 말 · 신규 문의 · 권한) | `lib/` | check-dash(검사-㊹ 14건) · check-fast(`app/page.js` 3단 + dash 파도) |
| **화면 `/`** — 알약(날짜 · 🚨 빌 아이 N · 오늘 N명 · 시각) · 원장에겐 「누가 무엇을 보나 안 정한 것 N」 카드 그대로 · **🚨 빵꾸 막이**(아이·교재 · 까닭 · 꼬리표 · 같은 영역 교재 N권 · 결석 예정 표시 · 「나머지 N명은 다 차 있다」 · 오늘 수업 열기 ↗) · 카드 여섯: 📚 오늘 수업(반 줄 + 열기 · 늦귀가 예정 N · 보내야 함 · 반성문 N) · 📨 발송(데일리리포트 마감 n / N) · 🔥 오늘 안(단원평가 출제 · 재시험) · ⚠️ 안 돌고 있는 것(클래스카드 · 하루 정리) · 📅 이 달(보강 안 잡힘 + 잡기 · 시험 임박 D-N · 영어일 없는 학교) · 💬 답할 것(남기실 말 · 신규 상담). 단추는 지은 화면(오늘 수업)으로만 — 발송·루틴·시험·할 일 화면은 「아직」이라고 말한다 | `app/page.js`(그리기만 · 표 직접 읽기 0) | e2e/today 대시보드 9건(조회 19) · 치수·글꼴·대비 검사에 `/` |
| **씨앗** — 학생둘에게 독해책(독해 루틴 없음 → 빌 아이 1) · 남기실 말 1 · 신규 문의 1 | `scripts/e2e/seed.sql` | e2e |

## 2차-12 「🎒 아이 화면 07-1 — 등원·하원 · 오늘 · 다 했어요」 끝 (2026-09-06 — 목업 07 · 답 ⑧⑨⑩⑪⑫⑯ · 9/5 ⑬ · 고도화는 목록으로 여쭘)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0115** — `makeup_own`(아이·학부모가 제 결석·보강을 읽는다 — 07 「앞으로」·09) · `v2.last_area_memos(학생)`(메모 있는 마지막 마감 판의 영역 메모 — 「선생님 한 마디」, own_read_dam 위에서) · ⚠️ **「다 했어요」 무르기** — 원장님 답 ⑧(9/3 「가능」)인데 0082·0084 문지기가 「한 번 누르면 못 내린다」로 막고 있었다 → 그 걸음만 뺀 문지기로 갈아 끼움(내 판만 · 다른 칸 못 바꿈 · 시각은 서버가는 그대로) | `supabase/migrations/0115_me.sql` | check-sql · check-sql-funcs · e2e(무르기) |
| **판단** — `lib/arrival-plan.js`(순수: 걸음 셋 + 집에 가요 · 도착 = 가장 이른 등원 걸음 · 지각 분 · 학원 회선 IPv4/IPv6 · 요청 주소 · 반 고르기 · 「앞으로」 줄 · 학원 줄 차례) · `lib/arrival.js`(관문 → 등원 표 한 줄은 **아이 자격**(시각·날짜는 DB 문지기가 서버 시계로, 표-10) → 출석이면 판 세우기·출결은 **서버 자신(service role)이 원장 손과 같은 길**(`ensureSheet`·`attendanceWrite`) — 아이 자격은 판을 못 깐다 · 알림은 큐 `arrival_notice`·`leave_notice`(손은 10) · `allowIp` 학원 회선 더하기) · `lib/me.js meDay`(제 학생 줄 → 한 파도 12 조회, 4단 · 전부 아이 자격 RLS) · `lib/day.js` SHEET_SEL·shape 내보냄 · ensureSheet 가 반 없는 날(보강)도 | `lib/` | check-arrival(검사-㊺ 10건) · check-fast(`app/me/page.js` 3단) |
| **화면 `/me`** — 아이는 `/` 에서 `/me` 로(메뉴 없음 · 카드는 원장님이 켠 me.* 만) · 🕘 등원·하원(걸음 셋 칩 · 반이 둘이면 고르기 · 「HH:MM 왔음」 · 🏠 집에 가요 → 「HH:MM 집에 감」 · 와이파이 밖·두 번·수업 없음·휴강은 그 자리에서 말한다) · 📋 **오늘 할 것**(학원 줄은 차례대로 「다 했어요」 → 다음이 열림 · 숙제 줄 「다 했어요 / 무르기」 · ✎ 선생님 메모 · 판 없으면 「열면 뜹니다」, 검사 전이면 「검사하면 뜹니다」) · 📘 오늘 낼 숙제(판이 섰으면 검사 줄, **등원 전엔 지난 판의 숙제** — 아침에 집에서 본다 · 검사 결과 ○△✕) · 🔤 시험(오늘·다음 시간 · 결과 %) · 🌙 오늘은 남아서(HH:MM 예정 · 사유) · 📅 앞으로(결석 예정·보강 · 지각 예정) · 🗺 내 교재(회독 · 남은 소단원 · 멈춤) · 💬 선생님 한 마디(마감해야) | `app/me/page.js`(그리기만) · `cards.js`(등원 · 다 했어요) · `actions.js` · `app/page.js`(아이 → /me) | e2e/screens +7 · e2e/today +7 · 치수·글꼴·대비 검사에 `/me`(아이 자격) |
| **설정** — 🏫 학원 회선 카드(원장): 주소 목록 + 「지금 이 자리의 주소 더하기」(답 ⑨ — 그 자리의 주소를 서버가 읽어 `integration arrival.ips` 에) · 비어 있으면 아무도 못 찍는다고 말한다 | `app/settings/page.js` · `ipcard.js` · `actions.js` | e2e(설정 화면 열림) |
| **씨앗** — 학원 회선에 127.0.0.1 · 아이 화면 카드 넷 켬(role_access) | `scripts/e2e/seed.sql` | e2e |
| **검사가 잡은 것** — ① 아이가 「출석」을 찍자 「permission denied for table integration」: **서버 자신(service_role)에 v2 표 권한이 한 줄도 없었다**(0005 는 usage 만 — bypassrls 는 규칙을 지나칠 뿐 권한이 아니다). 크론도 같은 자리에서 죽게 되어 있었다 → **0116** 이 select·insert·update(delete 는 대전제-6)를 주고 기본 권한도 정한다 · check-grants 에 「서버 자신이 못 쓰는 표 없음 · 지울 권한 없음」 ② check-fonts 가 오늘 수업의 시험 추가 `<select class="sel">` 12px 를 잡음(목업의 `.sel` 은 가짜 칩) → 진짜 select 는 입력칸 글씨(15/16)로 ③ 비밀번호 바꾼 뒤·로그인 뒤 첫 화면이 `/` 였다 → 역할마다 제 첫 화면(`homeFor`)으로 바로 | `supabase/migrations/0116_service_role.sql` · `scripts/check-grants.mjs` · `app/today/row.js` · `app/login·password/actions.js` | check-grants · check-fonts · e2e |

## 2차-13 「📅 아이 달력 09b」 끝 (2026-09-06 — 목업 09b · 07-2 의 ③ · 답 ①)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **판단** — `lib/cal-plan.js`(순수: 달 범위 · 날마다 표시 우선 · 날 하나의 줄 출·수·숙·시·휴 — 학생·학부모가 같은 한 벌) · `lib/cal.js calendar()`(제 학생 줄 → 한 파도 9 조회: 수업일 · 결석·보강 · 지각 예정 · 등원·하원 · 판(숙제·늦귀가·수업일지) · 시험 · 학교 시험 · 휴강 · 권한 — 4단, 전부 아이 자격 RLS) | `lib/` | check-cal(검사-㊻ 10건) · check-fast(`app/me/cal/page.js` 5단) |
| **화면 `/me/cal`** — ◂ 달 ▸ · 월요일부터 42칸(오늘 테두리 · 앞날은 흐리게 · 시험일 빨간 테두리) · 칸마다 표시(✓ ⏰ ✕↻ 📘 📝 · –) · 범례 · 칸을 누르면 아래에 그날의 줄(「17:02 등원 · 22:25 하원」+늦귀가 사유 · 수업일지 · 숙제 N개 · 단어 시험 17/20 · 85% · 결석 · 보강 · 휴강 사유) — 링크만으로 움직인다(?m · ?d) · 「나」 화면에 📅 달력 카드 | `app/me/cal/page.js` · `app/me/page.js` | e2e/today +4 · 치수·글꼴·대비 검사에 `/me/cal`(아이 자격) |

## 2차-14 「📚 받을 교재·학습지 · 💬 남기실 말 — 07-2 ①⑤」 끝 (2026-09-06 — 원장님 「계획대로 해」 → 추천대로)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0117** — `material_give` 의 아이 문지기(표-9·표-10: 내 것만 · stage·due_on 만 · got_at 은 서버가) · `child_got` 을 아이 본인만으로(학부모는 못 고침 — 0084 ㉑ 과 같은 결) | `supabase/migrations/0117_material_child.sql` | check-sql · e2e |
| **판단** — `lib/material-plan.js`(순수: 단계 넷 · 갈래별 묶음 · 끝낸 것 · 마감 글) · `lib/material.js`(단계 · 마감 쓰기) · `lib/request.js`(남기실 말 — 갈래 넷 · 500자) · `lib/me.js` 파도 +2 · `lib/cal-plan.js`·`cal.js` 에 🚩 내가 정한 마감(+1 조회) | `lib/` | check-material(검사-㊼ 4건) · check-cal(🚩) |
| **화면 `/me`** — 📚 받을 교재·학습지(갈래별 · 단계 세그먼트 · 내가 정한 마감 날짜 → 「M/D까지」 · 끝낸 것 접힘 · 「N/전체」) · 💬 선생님께 남기실 말(보내기 → 「보냄 · 답 기다리는 중」 · 답이 오면 그 밑에) → 대시보드 「답할 것」에 뜬다 · 달력에 🚩 | `app/me/cards.js` · `page.js` · `actions.js` | e2e/today +4 · 대시보드 「남기실 말 2」 |
| **씨앗** — 자료 종류 둘 · 나눠 준 학습지 셋(하나는 끝냄) | `scripts/e2e/seed.sql` | e2e |

## 2차-15 「👨‍👩‍👧 학부모 화면 09 · 학부모 달력」 끝 (2026-09-06 — 목업 09 · 확정-⑮·⑯ · 사고 #7)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **판단** — `lib/parent-plan.js`(순수: 숙제 % · 수업일지 꼬리표 · 오늘 늦귀가(보낸 것만 · 실제 하원) · 보낸 것 · 오늘 등원 · 다음 시간 시험) · `lib/parent.js`(내 아이들 → 고른 아이의 한 파도 10 조회: 늦귀가 안내 rpc · 등원 · 마감한 최근 판 · 오늘 판 · 결석·지각 예정 · 영역 메모 · 남기실 말 · 권한 · 오늘 수업일 — 전부 **학부모 자격 RLS: 마감한 판만**) · `lib/cal.js calendar(…, role)` 에 자격 인자 + ◂▸ 한계(재원 시작 ~ 다음 달, 확정-⑯) | `lib/` | check-parent(검사-㊽ 8건) · check-cal(10건) · check-fast(`app/parent` 4단 · `app/parent/cal` 4단) |
| **화면 `/parent`** — 형제면 위에 고르기(`?s=`) · 🌙 오늘은 늦게 갑니다(보낸 것만 · 「HH:MM 에 갔습니다」) · 🕘 오늘(아이가 찍은 등원·하원 — 마감 전에도) · 📋 오늘/M·D 수업(마감한 수업일지 + 꼬리표) · 📘 다음 숙제 · 📝 다음 시간 시험 · 📅 앞으로 · 💬 선생님 한 마디 · 📅 달력 · 🎒 아이 · 📨 보낸 것 · 💬 남기실 말 — 권한 `PARENT.*` 로 가리고 **빈 카드는 숨긴다**(확정-⑮) · 메뉴 없음 | `app/parent/page.js` · `actions.js` | e2e/screens +5(아침) · e2e/today +6(마감 뒤) · 치수·글꼴·대비 검사에 `/parent`·`/parent/cal`(학부모 자격) |
| **달력 `/parent/cal`** — 아이 달력과 **같은 한 벌**(`app/_shell/calview.js` 를 둘이 쓴다 · 판단은 `lib/cal-plan.js` 하나) · 학부모 자격이라 마감 안 한 날은 자연히 「수업함·정리 중」 | `app/parent/cal/page.js` · `app/_shell/calview.js` · `app/_shell/askcard.js`(남기실 말도 아이·학부모 한 벌) | e2e/today(같은 그림 · 다음 달 ▸ 한계) |
| **씨앗** — 학부모 ↔ 학생 1(어머니) · 학부모 권한 줄 다섯(intro·recent·homework·next·sent) | `scripts/e2e/seed.sql` | e2e |

## 2차-16 「📨 발송 10 — 나가는 길 한 곳 · 손 다섯 · 고르고 한 번에 예약」 끝 (2026-09-06 — 목업 10 · 확정-㉕·㊿ · 대전제-7 · 뼈대-9·11 · 속도-3)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0118** — 자취(notify_log)에 why·sheet_id·job_id(뼈대-3 · 다시 시도해도 자취가 안 는다) · 규칙 줄 넷(방해금지 23:00~09:00 — 원장님 2026-08-07 옛 답 그대로 · 예약 단추 시각 21:00·09:00) · 공개키 문 `push_public_key`(아이·학부모가 알림을 켤 때, 비밀키는 안 나간다) · 발송 판이 읽는 한 벌(`send_board` = `send_jobs` · `sent_log` · 예약 · 닿는 길 · 규칙 — 아이 이름을 붙여서, 학원 사람만) · 자취 `sent_at` 의 기본값 now() 걷기(0012 의 함정 — 그대로면 자취를 남기는 순간 「보냈다」가 된다) · 예약 표 쓰기 권한 · 자취 제 것 읽기 | `supabase/migrations/0118_send.sql` | check-sql · check-sql-funcs · check-grants · e2e |
| **나가는 길 한 곳** — `lib/notify.js notify()`(자취 먼저 → 스위치 `NOTIFY_SINK` off(기본)·self·live → 학부모 기기에 웹 푸시 → 자취에 보냄·못 보냄 · 끈 기기는 revoked_at) · `lib/push.js`(web-push 를 부르는 유일한 자리 · 404·410 은 끈 기기 · 실패를 삼키지 않는다) · `lib/notify-plan.js`(순수: 갈래 이름 여덟 · 잠금화면 문구 · 스위치 · 기기 고르기 · 방해금지) | `lib/` | check-notify(검사-①·⑦ · 대전제-7) · check-send |
| **손 다섯** — `lib/send.js`: 데일리리포트(마감한 판만 · 치환 자리 `{{` 면 되돌린다, 뼈대-11) · 늦귀가 · 등원 · 하원 · 결석·지각 예정 — 전부 방해금지면 미룸(큐 next_at, 시도 횟수 안 셈) · 「예약 → 큐」(`promoteScheduled`, 한 바퀴 앞에) · 백스톱(`backstop`, 화면이 열릴 때 렌더 뒤 `after`) · 크론이 손을 들여온다(뼈대-9) · `lib/queue.js` 에 `beforeRun`·`defer` | `lib/send.js` · `lib/queue.js` · `app/api/cron/route.js` | check-queue · check-notify · e2e/today(크론 한 바퀴 실패 0) |
| **화면 `/send`** — 파도 2(오늘 판 · `send_board` 한 벌: 큐·자취·예약·닿는 길·규칙) · 알약(🌙 지금 보낼 것 · 선택 N건 · 앱 알림만/🧪 리허설 · 닿는 길 `parent_reach`) · 🕘 지금(늦귀가 — **오늘 카드에서 보냄 표시만**, 확정-㊿ · 사유 빈 것은 ⚠️) · 📨 마감하면 나갑니다(마감한 판만 「보낼 것」 기본 고름 · 마감 전은 ⏳ · 치환 자리 빈 것은 ⚠️ 고치기 ↗ · 예약됨 취소 · 나간 것 ✅ 다시 보내기) · 🔔 저절로 나가는 것(등원·하원·예정 알림 중 안 끝난 일) · 📢 예약된 것(표 + 방해금지로 미뤄진 일) · › 오늘 나간 것(읽음·안 읽음·리허설·못 보냄 셈 · 줄마다 「📨 보냄 · 👁️ 읽음 · 🔁 N번」) · 저장줄(sticky): 전체 선택 · 📨 선택한 것 지금 보내기 · ⏰ 예약(오늘 21:00 · 내일 9:00 · 직접) | `app/send/page.js` · `board.js` · `actions.js` · `lib/send-plan.js` · 메뉴 「발송」 | check-fast(3단 · 파도 2) · e2e/screens +1 · e2e/today +8 · 치수·글꼴·대비에 `/send` |
| **받는 쪽** — 🔔 알림 카드(`app/_shell/bell.js` — 「나」·학부모 화면 · 기기마다 길 안내: 아이폰은 홈 화면에 담아야 · `lib/push-plan.js` 순수) · 기기 저장·끄기(`app/push/actions.js`, 제 것만 · revoked_at) · `/api/push/seen`(폰이 받은 때·누른 때 회신 → `mark_notify_seen`, 표-10) · `/api/icon/[size]`(원장님 로고, 없으면 404) · `app/manifest.js`(홈 화면 앱 「클로이영어」) | `app/_shell/bell.js` · `app/push/` · `app/api/push/seen` · `app/api/icon/[size]` · `app/manifest.js` | check-sw(계약 14건 — 이제 초록) · 치수·글꼴·대비(`/me` · `/parent`) |
| **씨앗** — 리허설은 방해금지 없음(`send.quiet_*` 00:00 — 걷기가 몇 시에 돌든 같게) | `scripts/e2e/seed.sql` | e2e |

## 2차-17 「🔁 루틴 11 — 영역마다 한 벌 · 아이마다 고른 것 · 교재는 잇기만」 끝 (2026-09-06 — 목업 11 · 확정-㉒·㊷·53·54·57 · 답 ㉔ 「b」)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0119** — 통과 기준 칸(`student_routine.criterion`, 확정-54) · 예습은 **새 칸 없이** 0065 의 자리 「예습(next)」 그대로(확정-57 · 답 ㉔ b · 원칙-1 — 실데이터 「교재예습」 2줄이 거기 있다) · 내린 줄은 `routine_areas` 도 안 센다(0075 의 경고를 이제 지킴) · 학원 사람의 루틴·교재 잇기 쓰기 권한(규칙은 있는데 권한이 없던 자리) · 루틴 판 한 벌 `routine_board`(항목 · 영역 루틴(내린 것까지) · 영역별 교재 수 · 아이들 · 고른 아이의 줄·교재(안 한 소단원 수 · 앞으로 400일 수업일) · 이을 수 있는 교재 — 아이를 안 고르면 첫 아이) | `supabase/migrations/0119_routine.sql` | check-sql · check-sql-funcs · check-grants · check-tables |
| **판단** — `lib/routine-plan.js`(순수: 영역 일곱 `AREAS` 한 벌 · 살아 있는 줄 · 영역 머리 알약 · 아이의 한 영역 · ▲▼ · 예습 소단원 · 이대로면 · 체크리스트) · `planBook` 에 예습 줄(place next)과 그 소단원 · `lib/routine.js`: 깔기가 내린 줄을 거르고 예습 줄은 **숙제 자리에 다음 소단원**으로 깐다 · 손 아홉(`addItem` 이름이 있으면 되살림 · `editItem` · `setLine` 자리(학원·숙제·둘 다·예습)·필수·내림·갯수·기준 · `moveLine` · `customizeStudent` 베끼기 · `resetStudent` 전부 내림 · `reviveStudentLine` · `setBook` 회차 1~6·기준·단원평가 · `assignBook` 오늘부터) — 지우는 손이 없다(확정-㊷) | `lib/` | check-routine(40건 — +10, 검사-㊿) · check-close(손 아홉은 판이 아니라 루틴 표) · check-fast(4단) |
| **화면 `/settings/routine`** — 머리(영역 N · 교재 예외 0 · + 항목 · 「교재가 늘어도 안 늡니다」) · 영역 일곱 격자(머리: 그림 · N권 · 학원 a·숙제 b·필수 c / 줄: 번호 · 이름·하는 법·체크리스트 · 필수 · 학원/숙제/둘 다/예습 · ▲▼ ✎ 🗑 / 「내린 것 N」 되살리기 / 단어는 「학원이 0줄 — 맞습니다 · 숙제 N줄」) · + 항목 폼(영역 · 이름 · 하는 법 · 체크리스트 · 자리 넷 · 필수) · 「교재마다 짜면 N벌 → 영역마다 N벌」 · 🧑 아이(고르기 `?s=`): 교재 있는 영역마다 「학원 기본 그대로」/「이 아이만 고침」 · 이 아이만 고치기/학원 기본으로 · 줄마다 N개·기준·자리·▲▼·🗑 · 「뺐습니다」 ＋ · 📚 교재는 잇기만: 코드 · 이름 · 영역 · 「{영역} 루틴을 씁니다 / 이 아이만 고친 / **루틴이 없습니다 — 위에서 만드세요**(대시보드 빵꾸와 같은 판단)」 · 기준(대단원·소단원) · 회차 −/+ · **이대로면 YYYY-MM-DD · 남은 소단원 n/N · 수업 N회 · N.N개월** · 단원평가 본다(대단원마다 · 소단원 5·10개마다) · + 교재 잇기 · 발 알약 | `app/settings/routine/page.js` · `board.js` · `actions.js` · 설정에 🔁 카드 | e2e/screens +9 · e2e/today +1 · 치수·글꼴·대비에 `/settings/routine` |

## 3단계-1 「📅 일정 12 · 📡 학사일정 받아오기 12b — 8회 채우기 · 전국과 학교를 가른다」 끝 (2026-09-06 — 목업 12·12b · 확정-㉔·㊲ · 인수인계 s12·s12b)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0120** — 규칙 줄 `schedule.sessions_per_month`(8 — 「8회가 모든 반의 기준」) · `class_extra_days`(반 보강일 = 빠진 날 없는 보강 줄의 날짜 수) · 쓰기 권한(휴강·시험·학교·할 일·도장 — 규칙은 있는데 권한이 없던 표들) · 일정 판 한 벌 `schedule_board`(그 달 앞뒤 7일: 반·회차 · 휴강 · 결석·보강 · 지각 예정 · 시험 · 할 일 · 학교 · 규칙) · 받아오기 판 한 벌 `import_board`(학교·나이스 코드·홈페이지·아이 수 · 지난 한 달부터의 시험 · 전국 낱말 · 마지막 받은 때(세어 나온다) · 열쇠 있나(참·거짓만)) | `supabase/migrations/0120_schedule.sql` | check-sql · check-sql-funcs · check-grants · check-tables |
| **판단** — `lib/schedule-plan.js`(순수: 칸의 일들과 차례 · 반 고르기 · 회차 8회 채우기 · 보강 안 잡힘 · 하루의 줄) · `lib/neis-plan.js`(순수: 옛 앱 lib/neis.js 의 셈을 한 벌로 — 학년도 · 주소 · 답 읽기 · 이름 펴기 · 갈래는 **원장님의 낱말 목록**으로 · 잇기 · 회차 후보 · 출처 열쇠) · `lib/schedule.js`(휴강 넣기·무르기(off) · 할 일 · 시험(손으로) · 영어 시험일 · 시험 물리기 · 반 보강일 잡기·물리기 · 아이 보강 잡기 = 02c 의 `planSave` · 홈페이지 주소 · 전국 낱말 더하기) · `lib/neis.js`(열쇠는 연동 neis · 학교 찾기 · 코드 붙이기 「교육청:학교」 · 다시 받기 = 코드 있는 학교 전부 · 이 학년도 · 출처+열쇠로 upsert · 영어일·손으로 넣은 줄은 안 건드린다) | `lib/` | check-schedule(12건 · 검사-52) · check-neis(17건 · 검사-51) · check-fast(4단 · 3단) |
| **화면 `/schedule`** — ◂ 달 ▸ · 반 고르기 · 「보강 안 잡힘 N」 · 반 회차 카드(N회 · 8회 채움 ✓ / ⚠️ N회 모자람 — 보강 필요 → 📅 보강일 잡기 / 특강은 회차만큼) · 42칸 달력(칸마다 🚫 ✕ 📝 ↻ ⏰ 📋 — **정상 수업은 안 띄운다**) · 고른 날의 줄(결석 아이마다 보강 잡기·안 잡음 · 휴강 무르기 · 할 일 끝냄 · 시험 영어일·물리기 · 반 보강일 물리기) · + 일정(시험) · + 휴강 · + 할 일 · 범례 | `app/schedule/page.js` · `panel.js` · `actions.js` · 메뉴 「일정」 | e2e/today +9 · 치수·글꼴·대비에 `/schedule` |
| **화면 `/schedule/import`** — 「나이스 · M/D 받음」 · 학교 N곳 · 🔄 다시 받기(열쇠 없으면 잠김 + 안내) · 🌏 전국(학교 없음 · 전국 낱말 + 더하기) · 🏫 학교별(기간 · 학년 · 영어일 없으면 그 자리에서 적기 · 출처 꼬리표 나이스/홈페이지/손으로 · 물리기) · 🧩 나이스에 없는 것(학교마다 코드·홈페이지 주소·학교 찾기 · + 손으로 넣기 · 「나이스에 없는 학교 N곳」) · 🛡️ 덮지 않습니다 | `app/schedule/import/page.js` · `board.js` | e2e/today +3 · 치수·글꼴·대비에 `/schedule/import` |

## 3단계-2 「💳 수강료 13 — 넣고 체크만 · 금액을 적으면 단가 줄이 이 달부터」 끝 (2026-09-06 — 목업 13 · 처음-1 · 대전제-5 · 답 ⑮ · 인수인계 s13)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0121** — 쓰기 권한(단가 줄 `fee_rule` · 수납 `payment` — 지우는 권한은 없다, 대전제-6 · 규칙은 0016 에 있는데 권한이 없던 표) · 수강료 판 한 벌 `fee_board(달)`(그 달 재원생과 반 — 회차 = session_count + 반 보강일, 일정 12 와 같은 셈 · 그 달에 걸친 단가 줄 · 그 달 수납 · 학년별 기준(옛 설정 `tuition.byGrade`) · 운영 권한 줄). **청구액은 저장하지 않는다** — 받은 금액·받은 날만(대전제-5) | `supabase/migrations/0121_fee.sql` | check-sql · check-sql-funcs · check-grants · check-tables |
| **판단** — `lib/fee-plan.js`(순수: 단가 줄 학생 › 반 › 학년 기준 · 특강은 단가 × 회차 · 그 달 줄(받은 금액이 있으면 그것) · 상태 셋(받음·안 받음·금액 없음) · 합계 · **금액을 바꾸면 이 달부터 새 단가 줄**(지난달 소급 없음, 처음-1) · 결제선생 엑셀 읽기(열 이름 후보 — 정확히 맞추라고 안 한다) · 엑셀로 내보낼 줄) · `lib/fee.js`(판 읽기 · 저장 = `payment` upsert (학생, 달) + 단가 줄 닫기·고치기·더하기 · 엑셀 올리기 = 이름으로 맞춤, 같은 이름 둘이면 못 맞춘 채 알림) · 달 이름 `monthLabel` 을 `plan-plan` 한 벌로(달력 09b · 일정 12 가 같은 식을 둘 갖고 있었고 13 이 셋째가 될 참 — 원칙-1) | `lib/` | check-fee(19건 · 검사-53) · check-fast(4단 · 상한 4) |
| **화면 `/ops`** — 💳 수강료: ◂ 달 ▸ · 「안 받음 N」 「금액 없음 N」 · 표(학생 · 반 + 특강 N회 + 단가 출처 · 금액 · 받은 날 · 상태) · **바뀐 줄만 저장**(화면이 먼저 세고 저장은 뒤에) · 합계·안 받음은 화면이 센다 · 📄 결제선생 엑셀 올리기 · 엑셀로(`/api/ops/fee?m=`) · 강사·조교는 `ops.fee` 가 안 정함이면 **닫힘**(답 ⑮ 「강사는 수강료 못 보게」 — 기본값은 코드에 없다) · 🗂 상담일지·신규 문의 자리(18 에서) · 메뉴 「운영」 | `app/ops/page.js` · `fee.js` · `actions.js` · `app/api/ops/fee/route.js` · `lib/perm.js`(OPS) · `lib/menu.js` | e2e/today +5(적기 전 금액 없음 · 화면이 먼저 셈 · 저장 → 단가 줄 · 다음 달에 미리 · 엑셀 200/400) · e2e/screens +1(강사 닫힘 · 엑셀 403) · 치수·글꼴·대비에 `/ops` |

## 3단계-3 「🏫 시험 회차 06b — 범위와 시험일 · 교재 멈춤을 회차에 묶는다」 끝 (2026-09-06 — 목업 06b · 확정-⑬·㊺b·㊲ · 9/5 ⑳ · 인수인계 s6b · 영어시험-특징 §5·§6)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0122** — 규칙 줄 다섯(`prep.stop_weeks.high/middle/elem` 6·4·4 · `prep.phase_before_days` 7 · `prep.phase_after_days` 3) · 칸 셋(`students.stop_weeks` 아이 따로 · `student_book.stop_from` 멈춤 시작일 · `exams.hidden` 회차 숨김) · 표 `exam_skip`(아이 × 회차 「안 봄」 — 되돌리면 skipped=false, 지우지 않는다) · 쓰기 권한(`prep_scope` · `students.stop_weeks` 한 칸) · `exam_takers(회차)`(보는 아이 — 학교+학년 · 전국은 고등 · 안 봄 뺌 · 한 곳) · `exam_board(날)` 한 벌(지난 한 달부터의 회차: 보는 아이 · 안 봄 · 범위 · 묶인 교재 수 · 학교 · 재원생 · 교재 · prep.* 규칙) · `routine_board` 에 stop_from·stop_exam_id | `supabase/migrations/0122_exam.sql` | check-sql · check-sql-funcs · check-grants · check-tables · check-schema |
| **판단** — `lib/exam-plan.js`(순수: 보는 아이 `takes` · 그날·끝나는 날 · 몇 주 전부터(아이 따로 › 학교급) · **멈춤 창 = 영어 시험일 − N주 ~ 시험 끝**(영어일 없으면 안 세운다 — 목업 06 ⚠️) · 부모님께 글의 시험전·시험후 · 범위 묶음(교재 × 대단원 · 더함 · 학교가 뺌 · 글) · 알약 · 머리 · 멈춤 글) · `lib/routine-plan.js stopOn` 에 「시작 전이면 진행중」 한 줄(판정은 그 한 곳 그대로) · `lib/exam.js`(판 · 범위 더하기(단원 여럿·글, 뺐던 것은 되살림) · 빼기 = removed_on · 안 봄 · 숨김 · 몇 주 전부터(규칙 줄) · 아이 따로 · **syncStops**: 보는 아이의 살아 있는 정규 교재(내신 교재 빼고)를 창에 book_off — 돌아가는 줄·이 회차에 묶인 줄·기한 지난 줄만, 손으로 멈춘 줄과 「풀기」로 푼 줄은 안 건드린다 · 지금 멈춤(오늘부터 · 푼 줄도) · 풀기 · 앞으로의 회차 전부 다시 맞추기) · 시험을 넣거나 영어일을 적거나 물리거나 받아오면 **한 곳(syncStops)** 이 따라 돈다(`lib/schedule` · `lib/neis`) · 오늘 파도에 「곧 있는 회차」 한 줄(`lib/day.js examsSoon`, 앞뒤 한 달) → 부모님께 글의 **시험전·시험후가 저절로**(2차-7 남긴 것 ✅ · `pickKind` 의 exam) | `lib/` · `app/today/row.js` | check-exam(14건 · 검사-54) · check-routine(stopOn) · check-fast(3단) |
| **화면 `/schedule/exams`** — 머리(시험 회차 N · 영어일 없음 N · 범위 N줄 · 🙈 숨긴 회차 N · 📅 일정 · 📡 받아오기(+ 회차는 거기서 — 시험 넣는 꼴을 셋째로 안 만들었다)) · 학교 회차 카드(🏫 「신정중 · 중2」 · 영어 M.D / 영어일 없음 · N명 · 안 봄 N · 출처 · 🙈 숨김 · 시험 기간 · 영어일 없으면 그 자리에서 넣기 · 범위 칩(on · 더함 · 학교가 뺌) · + 범위(교재 고르기 → 대단원 체크 · 글 한 줄) · ⏸ 교재 멈춤 줄(「M/D부터 멈춤 · M/D에 저절로 풀림 · N주 전부터」 · 멈춘 교재 N · 지금 멈춤 · 풀기) · 안 봄(아이 고르기 · 본다)) · ⏸ 교재 멈춤 — 언제부터(고등·중등·초등 세그먼트 3·4·6·8주 → 규칙 줄 · 아이만 따로) · 🌏 전국(고등 아이 전부 · N명) · 🙈 숨긴 회차(보이기) · 저장줄 알약 | `app/schedule/exams/page.js` · `board.js` · `actions.js` · 12·12b 머리에 「🏫 시험 회차 ↗」 | e2e/today +13 · 치수·글꼴·대비에 `/schedule/exams` |

## 3단계-4 「📈 성적 16 — 아이가 넣고 원장님이 확인 · 등급은 세어 나온다」 끝 (2026-09-06 — 목업 16 · 07·09 📈(9/5 ⑯) · 대전제-5 · 영어시험-특징 §3·§4 · 인수인계 s16)

| 한 것 | 어디 | 확인 |
|---|---|---|
| **표 0123** — 회차의 등급컷 `exams.cuts`(내신에만 · 중학교는 비면 절대평가) · 아이의 공개 기본값 `students.score_show`(14 의 「성적 공개 — 학생 ✓ · 학부모 ✓」, 확인하는 순간 줄의 show_to 가 된다) · 시험지 문항표 `exam_question`(번호마다 영역 — 확정-⑤ 「문항표는 엑셀」, 지금은 글로) · 틀린 번호 바꾸기 한 곳 `set_score_wrong`(지우는 권한은 아무에게도 없어 정의자 함수 — 학원 사람 · 또는 아이가 넣고 확인 전인 제 것) · `set_exam_questions` · `score_board` 한 벌(고른 회차: 컷·문항표 · 보는 아이 · 그 회차 성적(틀린 번호) · 고를 회차) · 아이가 제 「안 봄」 줄을 읽는 정책 | `supabase/migrations/0123_score.sql` | check-sql-funcs · check-grants · check-tables · check-schema |
| **판단** — `lib/score-plan.js`(순수: 등급은 회차의 컷으로 세어 나온다(옛 앱 lib/scores.js 그대로 · 중학교 A~E · 학교가 준 등급이 이긴다) · 다음 등급까지 · 컷 글 · 문항표 글 ↔ 줄 · 틀린 번호 글 · 영역 셈 · 그 회차의 줄 · 알약 · 짧은 이름 · 카드 한 줄 · 넣을 회차 · 엑셀 한 줄) · `lib/score.js`(판 · 등급컷 · 문항표 · 넣기(원장 대신 · 아이 제 것 — 학생 × 회차에 하나) · 확인(굳고 공개 기본값) · 모두 확인 · 공개 바꾸기(확인한 줄만) · 엑셀 올리기(이름으로 · 바로 확인)) · 아이 화면 파도에 성적·넣을 회차 두 줄(`lib/me.js`) · 학부모 파도에 성적 한 줄(`lib/parent.js` — 카드 한 줄은 아이 화면과 같은 한 벌) | `lib/` | check-score(13건 · 검사-55) · check-fast(4단) |
| **화면 `/scores?e=`** — 회차 고르기(낸 N/M · 확인 안 함 N) · 영어 M.D · 확인 안 한 것 N · 안 낸 아이 N · ⬆ 엑셀로 한꺼번에 · 컷 줄(한 번 적으면 등급은 세어 나온다) · 문항표 줄 · 표(학생 · 원점수 · 등급(세어 나옴 · 학교가 준 것이면 「(학교)」) · 틀린 문항 · 낸 때(아이가 넣음) · 공개 · 확인/대신 넣기/고쳐 저장) · ❌ 틀린 문항 판(눌러도 적어도 같은 값 · 영역 셈) · 저장줄(모두 확인 · 안 낸 아이 N — 재촉은 발송 10 에서) · 06b 카드에 「📈 성적」 | `app/scores/page.js` · `board.js` · `actions.js` | e2e/today +11 · 치수·글꼴·대비에 `/scores` |
| ⚠️ **고친 사고** — 손으로 넣은 회차가 **둘째부터 안 들어갔다**(`exams (source, source_key)` 가 nulls not distinct 유일이라 (manual, null) 이 겹침 — 3단계-1 의 구멍, 16 걷기가 지난달 회차를 넣다 잡음). 출처 열쇠를 「manual:갈래:학교:학년:이름:시작날」로 세우고 같은 회차면 「이미 있습니다」 | `lib/exam-plan.js manualKey` · `lib/schedule.js addExam` | check-exam +1 · e2e/today(지난달 회차) |
| **아이 07 · 학부모 09 📈 성적 카드** — 아이: 원장님이 공개한 시험 + 내가 넣은 것(「확인 기다리는 중」) + **넣을 회차**(지난 60일 안에 본 시험 — 원점수·만점·틀린 번호 → 넣기) · 빈 카드는 숨긴다 · 학부모: `parent.reports` 켠 집에 공개한 것만(RLS 가 show_to 를 본다) | `app/me/cards.js`(ScoreCard) · `app/me/actions.js`(submitScore) · `app/parent/page.js` · e2e 씨앗에 parent.reports | e2e/today(아이가 넣고 · 확인되면 꼬리표가 사라진다 · 학부모 카드) |

### 2차에 남긴 것 — 목업 01 과 다른 곳 (다음 세션은 여기서 시작한다)

목업 01 의 카드 여덟 중 **넷**(1 숙제 검사 · 2+3 학습·숙제 · 3b 늦귀가 · ✉️ 글)이 섰다. 없는 것:

1. ~~시험 카드~~ ✅ 2차-2 (표는 `v2.quiz`, `word_test` 는 0038 에서 이미 걷혔다). 남은 것: **내신 범위**(prep_scope — 시험 화면 04 을 지을 때) · **방식 고치기** 모달(quiz_style 아이 줄) · **재시험지(종이)** 는 할 일 05 · 카드 자리 「시작하자마자 / 다 끝내고」(아이마다) · 「전체 개수 없음」을 리포트 10 이 막는 자리는 10 을 지을 때(문은 SQL 에 이미 있다) · 문장 시험 방식(받아쓰기·녹음) 표시.
2. 🃏 **클래스카드 플래너** — 받는 쪽부터(확정-⑩·⑱·55). 확장은 따로.
3. ~~경고·반성문 + 월초 정리 띠~~ ✅ 2차-4. 남은 것: 늦귀가 사유 **꼬리표 줄**(✓ 워크북 미제출 · ✓ 반성문 · ＋ 클카 … — 사유 후보를 칩으로) · 학생 14 경고 타일 · 대시보드 17 「반성문 1명」 · 학부모 09·발송 10 의 「반성문」 문구(사유 한 줄이 원본이라 저절로 실린다) · 학생별 「정리」.
4. ~~루틴 자동 깔기~~ ✅ 2차-1. 남은 것: 줄이기의 **「한 교재 멈춤」** 단추(어느 교재인지 원장님이 고르는 자리가 없다 — 교재 머리의 세그먼트로 대신) · 「그대로 27 · 필수만 12」의 **숫자**(줄이기 전 개수) · 「오늘 좀 많습니다」 말(확정-㊺a, 임계값은 v2.rule) · ✕ 면 「그 단원 다시」가 기본이 되는 것(지금은 회차 세그먼트에서 「다시」를 누른다) · 「매 수업 / 다음 회차」 꼬리표 · ~~예습 줄(확정-57 — 루틴 11 의 「예습」 칸)~~ ✅ 2차-17 · 같은 조절 3번째면 「루틴을 고칠까요」.
5. **모달 하나 남음** — ~~02 조절~~ ✅ 2차-3 · ~~02b 진도 체크~~ ✅ 2차-5(남은 것: 조각이 원본을 다 덮으면 ○(progress_part, 확정-⑳) · 「메모로만 3회 연속이면 앱이 부른다」(대시보드 17) · 아이가 찍는 문·「확인 기다리는 중」·❗ 이의(07 아이 화면) · 학생 줄 머리 진도 점 셋 · 「이번에 낸 것 · 남은 것」 막대) · ~~02c 결석·지각 예정~~ ✅ 2차-6(남은 것: 📝 **「시험 기간 = 저절로 결석 예상」** 범례·표시는 시험 06(`exams`)을 지을 때 · 목업의 「결석 지우기」 단추는 「없음」 세그먼트로 — 물림(cancelled), 지우지 않는다 · 보강 달력의 ◂▸(다른 달은 날짜 칸에 직접 적는다) · 12 원장 달력은 같은 표(`makeup`·`late_plan`·`student_days`)를 보기만 · ~~큐 `attend_plan_notice` 의 **실제 발송 손**은 10~~ ✅ 2차-16 · 「빈 자리 셈」(그 시각에 몇 명인가)은 12 를 지을 때).
6. ~~📝 단원평가 · 🗺 진도·영역별 메모 · 학생 줄의 진도 점 셋 · 3회독째 · 2일째 안 봄 · 학년·학교 이름~~ ✅ 2차-8. 남은 것: 단원평가를 **내는 자리**(학생 루틴의 「단원평가 본다」 → 할 일 05 「📝 단원평가 출제」 — 지금은 줄이 있어야 카드가 선다) · 목업의 「교재 CHAPTER → 단원평가 단원」 화살표(`unit_topic`) · 아이 화면 07 이 영역 메모를 그대로 보이는 것(마감해야) · 「N일째 안 봄」 걷기(씨앗에 이틀 전 숙제가 없어 순수 검사로만).
7. ~~✉️ 글의 갈래 다섯 · 글자 수 · 브리핑 만들기 · 미리보기~~ ✅ 2차-7. 남은 것: **시험전·시험후가 저절로 골라지는 것**(시험 06 의 `exams` 를 지은 뒤 — 지금은 원장님이 누른다) · 브리핑에 **오늘 경고·반성문 처분**을 재료로(warn_states 를 draftComment 파도에) · 아이별 지난 글을 말투 본보기에 더하기 · 마감 뒤 학부모 화면 09 가 `preview()` 한 벌을 그대로 쓰는 것(2차-15 는 글 + 꼬리표만 — 붙는 줄(시험·귀가 예정)은 카드로 따로 선다) · 확정-㉕(발송은 고르고·한 번에·예약)와 검사-①·⑪·⑬ 는 발송 10 과 함께.
8. ~~늦귀가 나머지~~ ✅ 2차-9 (처분 셋은 2차-4 에서 이미). 남은 것: 3b 의 **「남」 줄**(숙제에서 옮겨온 항목 목록 · 항목 더하기 · ⏭ 남은 것 다음 숙제로 넘기기 · 「남아서 하려다 못 한 것」 세기 — `day_item` 에 stay 자리가 없다, 지금은 「남아서」가 사유 한 줄에 「숙제 나머지 — …」로 선다) · 사유 **꼬리표 칩**(3 과 같음) · 「평소 9:40」(반 끝 시각) 알약 · ~~큐 `late_notice` 의 **실제 발송 손**(폰 푸시)은 10 발송에서(확정-㊿)~~ ✅ 2차-16 · ~~아이가 찍는 「집에 가요」는 07~~ ✅ 2차-12 · ~~학부모 09 가 `late_for_family` 를 읽는 것~~ ✅ 2차-15.
9. ~~폰 03 — 하단 저장줄 safe-area 위~~ ✅ 2차-10. 남은 것: 저장줄의 **「📅 다음 시간 ↗」**(같은 화면, 날짜만 넘긴다 — `/today` 에 날짜 인자) · iOS 는 키보드가 열리면 sticky 줄이 키보드 뒤로 간다(닫으면 돌아온다 — **실물 리허설**에서 볼 것) · 폰의 카드 여백 8px(목업 03 은 카드·패널 여백이 더 촘촘하다).
10. 검사 — check-routine2(⑩) · check-day(⑪, 리허설 학생만) · check-progress(⑭) 는 그 판단을 지을 때.
12. ~~🎒 아이 화면 07~~ **07-1** ✅ 2차-12(등원·하원 · 오늘 할 것 · 오늘 낼 숙제 · 시험 · 남아서 · 앞으로 · 내 교재 · 선생님 한 마디) · **③ 달력 09b** ✅ 2차-13 · **① 받을 학습지 · ⑤ 남기실 말** ✅ 2차-14(원장님 「계획대로 해」). **07-2 남은 것**: 🗺 내 교재 로드맵 08(대단원 지도) · 📈 성적 16(공개한 것만) · 💬 남기실 말(`request` — 아이가 원장님께) · 진도 찍기(확정-㊶ — 「확인 기다리는 중」·❗ 이의 · 설정으로 켜고 끔 · 대시보드 띠) · 카드 순서·접기(`screen_pref`) · ~~등원 알림·하원 알림 실제 발송(10)~~ ✅ 2차-16(손 + 🔔 알림 켜기 카드) · 반이 둘인 날 실물 리허설 · 시험 「오늘 건너뜀」 표시.
16. ~~📅 일정 12 · 📡 받아오기 12b~~ ✅ 3단계-1(원장 달력 · 8회 채우기 · 휴강·할 일·시험 · 보강 잡기 · 나이스 받아오기 · 전국 낱말 · 홈페이지 주소). **남은 것**: **다음 달 일정 도장 셋**(`month_confirm` 안내 → 확인 → 확정 · 휴강이 도장을 푼다(`holiday_unstamp`) · 학부모의 「다음 달 1차 확인」 — 표는 있고 화면이 없다) · **예상 수업일정 보내기**(원장님 2026-08-23 「먼저 일정을 보내고 봐라」 — 발송 10 의 갈래 하나로) · 「빈 자리 셈」(확정-㉔ — 그 시각에 몇 명인가) · 학교 홈페이지 **브라우저 확장**(나이스에 없는 학교 — 클카 확장과 같은 방식) · 나이스가 날짜를 바꾸면 「학교 일정이 바뀌었어요」 알림(지금은 나이스 줄이면 덮는다 — 손으로 넣은 줄만 안 건드린다) · 영어 시험일 **전날 등원·안내·마감**이 서는 것(06 시험 회차 · 10 발송) · 시험 기간의 「타과목 시험 결석 예상」 표시(02c 남긴 것) · 시험 회차에 교재 멈춤 묶기(확정-⑬) · 실제 나이스 열쇠로 실물 리허설(㉖ 뒤) · `todo` 는 「한 줄」만(05 할 일 화면이 주인) · 폰 달력(42칸 세로) 실물 리허설.
19. ~~📈 성적 16~~ ✅ 3단계-4(회차 고르기 · 등급컷 · 문항표 · 아이가 넣고 원장님이 확인 · 틀린 문항 판 · 공개 · 엑셀 · 07·09 카드). **남은 것**: **📨 안 낸 아이 재촉**(발송 10 의 갈래 하나 — notify 종류에 없다) · **문항표 엑셀**(확정-⑤ — 지금은 글로 적는다) · 14 학생 화면의 「성적 공개」 설정(`students.score_show` 는 있다 — 화면은 14 를 지을 때) · 모의고사 백분위(`score.percentile` — 칸은 있다, 16 표엔 아직 없음 · 내신과 섞어 평균 내지 않기) · 07 「내 성적 추이」(성적 여러 회차를 줄로) · 성적 미입력 재촉이 「안 봄」을 빼는 것은 `exam_takers` 가 이미 한다(16 의 안 낸 아이 = 보는 아이 − 낸 아이) · 단원평가(unit) 성적은 01 카드가 주인(16 엔 안 섞는다) · 확인 뒤 「고치기」(지금은 확인하면 잠긴다 — 되돌리는 손은 없다) · 학교가 발표한 등급 넣는 칸(엑셀로만) · 폰 표(가로 스크롤) 실물 리허설.
18. ~~🏫 시험 회차 06b~~ ✅ 3단계-3(회차 카드 · 범위 · 안 봄 · 숨김 · 교재 멈춤을 회차에 묶기 · 시험전·시험후). **남은 것**: **범위를 고치면 할 일이 저절로**(목업 06 — `todo`·`material` 은 05 할 일을 지을 때, 지금은 범위만 적힌다) · 🔤 시험 카드의 **내신 범위**(`quiz.source=prep` → `prep_scope` — 시험 04·quiz 에서 열기) · 새로 이은 교재가 멈춤 창 안이면 저절로 묶이는 것(지금은 영어일·규칙·아이 주 수·받아오기·안 봄이 바뀔 때만 맞춘다 — 매일 새벽 정리에서 한 번 더 맞추기) · 학교 홈페이지 **브라우저 확장**(나이스에 없는 학교 「다시 받기」 — 12b 남긴 것과 같다) · 영어 시험일 **전날 등원·안내·마감**(10 발송) · 시험 기간의 **타과목 결석 예상**(02c 남긴 것) · 대시보드 「영어일 없음」이 숨긴 회차를 빼는 것(`dash-plan examLines`) · 「이 학년 안 봄」 한 번에(지금은 아이마다) · 초등 회차(규칙 줄은 있다) · 성적 16 의 미입력 재촉이 `exam_takers` 를 쓰는 것 · 회차 카드의 폰 실물 리허설(두 칸 격자).
17. ~~💳 수강료 13~~ ✅ 3단계-2(그 달 표 · 바뀐 줄만 저장 · 단가 줄 자동 · 결제선생 엑셀 · 엑셀로 · 강사 닫힘). **남은 것**: 결제선생 엑셀 올리기 **e2e**(순수 검사만 있다 — 실물 파일로 실물 리허설) · **학년별 기준을 적는 화면**(지금은 옛 설정 `integration tuition.byGrade` 를 읽기만) · 특강 회차제 단가 줄(`fee_rule.per_session`)과 **반 단가 줄을 만드는 자리**(지금은 표에 직접 — 반 화면 14/15 에서) · 💰 수강료 안내 발송(10 의 갈래 · `{{치환}}` 문은 있다) · 학부모 화면 「수강료」(09 남긴 것) · 지난달 미납 **이월**(지금은 달마다 따로 센다) · 입금 확인 한 번에(도장) · 수납 방법(`payment.method`)을 화면에서 적는 것(엑셀은 읽는다) · 폰 표 실물 리허설(가로 스크롤 `.tblwrap`).
15. ~~🔁 루틴 11~~ ✅ 2차-17(학원 기본 루틴 더하기·고치기·차례·내리기·되살리기 · 자리 넷(예습은 0065 의 next)·필수 · 아이만 고치기·빼기·되살리기·N개·기준 · 교재 잇기·기준·회차·단원평가·이대로면). **남은 것**: 「이 교재만 루틴 다르게」(교재 예외 — 표가 없다, 지금은 0 고정) · 줄 사이 잠금(`gate_prev`, 확정-㉒ — 화면에 아직 없음) · 같은 조절 3번째면 「루틴을 고칠까요」(⑭) · 「단원평가 본다」의 실제 출제(05 할 일) · 항목 자체 내리기(`learn_items.state` — 지금은 줄만 내린다) · 교재 잇기의 시작일 고르기(지금은 오늘) · 잇기 끝내기(to_date — 화면 없음) · 폰(1열 격자) 실물 리허설 · 원장님이 채우신 39줄 실데이터가 이관돼 있는지(㉖ 뒤 실제 DB 에서) · 「이대로면」의 휴강 반영은 `student_days` 가 이미 뺀다(12 일정에서 눈으로).
14. ~~📨 발송 10~~ ✅ 2차-16(나가는 길 한 곳 · 손 다섯 · 고르고·한 번에·예약 · 오늘 나간 것 · 알림 켜기 카드 · sw 주소 둘 · manifest). **남은 것**: **실제 발송 켜기**(㉘ — Vercel `NOTIFY_SINK` off → self(원장님 폰) → live · 열쇠는 0072 가 옮긴 push 줄 · 학부모 기기 0대라 켜도 닿는 집이 없다 — 전환일에 학부모가 🔔 알림 켜기) · 📊 월간 리포트·💰 수강료 안내 예약(13 · `{{치환}}` 문은 있다) · 공지(`notice` 표 · 05 할 일) · 「읽음」 아이콘 투명도(확정-㉖ 정정 — 지금은 👁️ 글자) · 대시보드 📨 카드가 자취(안 읽은 집 N)를 읽는 것 · 학부모 화면 「보낸 것」이 자취도 읽는 것 · 아이 기기(who=all — 시험·지각처럼 아이도 알아야 하는 것) · 「글 보기」(자취엔 본문이 없다 — 판으로 간다) · 방해금지 사람마다(옛 push_prefs — 지금은 학원 규칙 하나) · 옛 앱 기기(public.push_subscriptions → v2.push_sub) 이관 확인 · 실물 리허설(원장님 폰 self).
13. ~~👨‍👩‍👧 학부모 09~~ ✅ 2차-15(늦귀가 안내 · 오늘 · 마감한 수업일지 + 꼬리표 · 다음 숙제 · 다음 시간 시험 · 앞으로 · 선생님 한 마디 · 달력 · 아이 · 보낸 것 · 남기실 말 — 형제 고르기). **남은 것**: 📈 성적 16(공개한 것만) · 📊 월간 리포트(발송 10 의 글을 학부모 화면에도) · 📎 사진·파일(자료함 20) · 형제 **실물 리허설**(씨앗은 아이 하나) · ~~늦귀가·등원·하원 안내의 **폰 푸시**(큐 손은 10)~~ ✅ 2차-16(스위치 켜기는 ㉘) · 「읽음」 표시 — 자취는 있다(2차-16), 학부모 화면 「보낸 것」이 자취를 읽는 것은 다음 · 학부모가 보는 「수업 일정·수강료」(12·15) · 목업 09 의 「지난 글 더 보기」(달력이 대신한다 — 필요하면 목록으로).
11. ~~🏠 대시보드 17~~ ✅ 2차-11. 남은 것: 맨 위 「진도 체크가 N일째 열려 있습니다」 띠(확정-㊶ — 아이 화면 07 의 설정을 지을 때) · 빵꾸 「**커서 잠김**」(3주째 같은 대단원 — 진도 시각을 봐야 한다) · 「안 읽은 집 N」(자취 `notify_log.opened_at` 은 2차-16 에 생겼다 — 대시보드 발송 카드가 읽는 것은 다음) · 「화·목반 7회 — 8회를 못 채웁니다」(달 회수 규칙이 아직 없다 — 12 일정) · 「단어 재시험지 뽑기」(할 일 05) · 「답하기」「보기」 단추(남기실 말·신규 상담 화면 18) · 「끄기/보기」(07) · 카드 순서 사람마다(`v2.screen_pref`).

### (걸음 1 목록 — 1차 때 적은 것, 2차가 이어 쓴다)

목업 01 을 열어 대조하고, 빠진 것이 있으면 이 목록부터 고친다.

### 부품 (목업 01 실측 — 카드 8 · 학생 줄 8 · 단추 107 · 입력칸 23 · 세그먼트 19)
1. **상단 띠** — 월초 정리 띠(8월 경고 정리: 전원 정리하기 / 이번 달은 그냥 두기, ㉗) · 「검사 안 본 것 N」 알약 · 반·시간 알약.
2. **학생 줄 머리** — 이름 · 학년·학교 · 진도 점 셋 · **출결 세그먼트**(왔음·지각·결석·조퇴·온라인, ①) · 「학원 15 · 숙제 12」 · 3회독째 · 경고 3·반성문(㉗) · 상태 말(2일째 안 봄 / 마감됨 / 결석·보강 안 잡힘) · 펴기/닫기.
3. **펼친 카드 여덟** — 🔤 시험(지난 시간 낸 범위 그대로: 단어·문장 줄, 틀린 개수·전체·통과선, 재시험지 만들기 · 오늘은 재시험 건너뜀) · 🃏 클래스카드 플래너(확장이 15분마다 받아 적음 · 목표 미달 넘기기 = 원장님이 누름) · **1 숙제 검사**(항목마다 시작만·절반·거의 다 → 오늘 학습으로·다음 숙제로·남아서, 「3/6 남음」) · **2 오늘 학습 + 3 오늘 숙제**(검사 끝나면 저절로 깔림 · 그대로 27·필수만 12·한 교재 멈춤 · 진행중·숙제멈춤·교재멈춤 · 조절 ↗ · 갯수 세그먼트 1-3 다시·1-4·1-4·1-5 · 미루기 · 항목 더하기 · 남은 것 다음 숙제로) · 📝 단원평가(없으면 안 엶) · **3b 늦귀가**(✕ 둘이면 저절로 뜸 · 처분 셋 다음 시간 숙제·오늘 남아서 쓰기·유예 ㉗ · 예상 귀가 +20/+40/+1시간 · 학부모에게 지금 보내기) · 🗺 진도·영역별 메모(닫힘) · ✉️ **부모님께 나갈 글**(늘 열림 · 보통·숙제안함·시험전·시험후·늦은밤 · 50/100/200/300자 · 브리핑 만들기 · 저장하고 마감 · 임시 저장).
4. **모달 셋** — 02 조절(교재마다 갯수 스테퍼 · 화면엔 문항·쪽 합계 · 도는 차례는 읽기만, 확정-㉓) · 02b 진도 체크(대단원 접기 · 소단원 ○◐· · 이 대단원 건너뛰기 · 저장) · 02c 결석·지각 예정(달력에서 날짜 · 보강은 앱이 제안 안 함 · 학부모 알림, 확정-㉔).
5. **폰(03)** — 같은 화면, 카드는 위아래 · 하단 저장줄은 safe-area 위.

### 판단 (lib/ 한 벌 · 규칙 번호)
출결 쓰기 `attend`(확정-⑨ 방아쇠는 숙제 검사 · 검사-②) · 판 세우기·마감 `close`(day_sheet.closed_at · 검사-⑤ 마감 가리기) · 숙제 검사 → 오늘 학습·숙제 깔기 `routine`+`chunk`(확정-⑨a·㉒·㉓ · 검사-⑩) · 진도 `progress`(확정-⑳·㊳·㊶ · 검사-⑭) · 단어시험 통과 `word`(90%) · 클래스카드 판정 `classcard`(확정-⑩·⑱·55) · 늦귀가 `late`(확정-⑭·㊼·㊿) · **경고·반성문**(확정-㊼ — 표 새로) · 교재 멈춤 `stop`(확정-⑬·㊺b) · 부모님께 글 `notify` 준비(확정-㉕ · 검사-①·⑪·⑬) · 오늘 분량 셈(확정-㊺a 앱이 밀지 않는다 — 말만 한다).

### 표 (v2 에 있음 / 새로)
- **있음**(0002·0006~0011): classes · class_schedule · class_member · students · profiles · day_sheet · day_item · word_test · unit_test · late_stay · books · units · student_book · progress(+part·flag·edit) · learn_items · area_routine · student_routine · exams · stop_rule · job_queue.
- **새로**(마이그레이션 0101~, 멱등): 경고·반성문(확정-㊼: 하루 1회 · 월초 정리 · 처분 셋 · 유예) · 조절 값(갯수·필수만·멈춤 — day_item 에 칸이 있는지 0011 을 먼저 읽는다) · 부모님께 글 임시저장(day_sheet.comment 인지 확인). **grep 먼저**(0-1) — 같은 칸이 있으면 그것을 쓴다.

### 상태 (경계 — 빈 것 · 0개 · 중복 · 같은 아이 여럿 · 폰)
- 오늘 수업이 없는 날(휴강·주말) → 「오늘 수업 없음 · 다음 수업 날」 · 학생 0 · 배정 교재 0(오늘 학습 없음) · 숙제 항목 0(검사 없이 학습으로) · 시험 범위 없음(시험 카드 안 엶).
- 같은 날 반이 둘(원장님 9/3 ⑫ 「출결하면서 바로 연달아 고르게」) · 같은 아이가 두 반 · 형제 두 줄(부모님께 글은 각각).
- 마감된 판은 읽기만 · 마감 뒤 메모(0086) · 결석이면 판이 안 서고 숙제가 안 나간다(02c).
- 폰 390: 세그먼트 줄바꿈 · 카드 위아래 · 넘침·겹침 0(check-sizes).
- 속도: 오늘 **20 조회 · 4단** 상한(속도-상한) · 파도 하나 · 출결·검사 칩은 낙관적(속도-5), 마감·발송은 서버 답을 기다린다.

### 이 화면에서 되살릴 검사
check-attend(검사-②) · check-close(⑤) · check-routine2(⑩) · check-day(⑪, 리허설 학생만) · check-progress(⑭) · check-fast(속도-상한) · e2e 걷기: 출결 → 숙제 검사 → 학습·숙제 깔림 → 늦귀가 → 부모님께 글 → 마감 (원장 · 강사).

## (지난 안내) 1단계 「뼈대」

로그인 s0 · 세션(`lib/session.js` sessionUser — `auth.getUser()` 금지) · 권한(원장·강사·조교·학생·학부모, 기본값은 코드에 안 박고 원장님이 웹에서) · 메뉴(어디서든 나가는 길, 대전제-10) · 배색 고르기(목업 `.skins`) · 큐·자동화 뼈대(뼈대-1~11).
표는 v2 에 이어 짓는다 — 오늘 수업이 쓰는 표(반·수업·소속·판·출결·숙제·학습·늦귀가·단어시험)는 0006~0011 에 이미 있다. 목업 01 이 바꾼 것만 마이그레이션으로 더한다.
그다음 2단계 하루 동선: 오늘 수업 s1 부터. 화면 하나마다 여덟 걸음(프롬프트) — 검사가 안 늘었으면 안 끝난 것.

## ✅ 2026-09-05 밤 — 운영 주소(chloe-english.vercel.app)가 v2 빌드를 보이던 사고 — **원장님이 Vercel 에서 되돌리심 · 해결**

원장님 9/5 밤: 「버셀 고쳤고 https://chloe-english.vercel.app/plan 정상작동해」 — 옛 앱(main)이 운영 주소로 돌아왔다. 아래는 그 경위(다시 밟지 않게 남긴다).


원장님 폰 사진: 주소창 `chloe-english.vercel.app/today` · 메뉴에 🚪 · 「DB 에 못 붙었습니다 — getaddrinfo ENOTFOUND base」. 원장님 확인(9/5 밤): 「https://chloe-english.vercel.app/today 이주소연거야」.
- 그 주소는 **학원이 쓰는 옛 앱(main)의 운영 주소**다(`docs/개발자-인수인계.md` 「브랜치와 배포」). 그런데 「DB 에 못 붙었습니다」 문구는 **main 에 없고 지운 옛 v2 코드(11e8b46 전)에만 있다** → 운영 주소가 v2 빌드를 보이고 있다. ⚠️ 정정(원장님 9/5 「옛앱자체를 학생학부모에게 배포하지않았어」): 옛 앱은 **원장님(·강사)만** 쓴다 — 학생·학부모 폰에는 안 나갔다. 그래서 이 사고의 영향은 원장님 쪽뿐이었다.
- 어느 쪽이든 손은 Vercel 에 있다(이 세션은 Vercel 에 손이 없고, vercel.app 은 여기서 열리지도 않는다 — 망 정책). **원장님이 하실 것 셋**: ① Vercel → 프로젝트 `dashboard-claude` → **Settings → Environments → Production → Branch Tracking** 이 `main` 인지(Settings → Git 에는 없다 — 원장님 9/5 실측) ② Deployments → `main` 의 마지막 배포(8/31 「교재에 붙일 학습 순서표를 종이로 뽑게 함」) → ⋯ → **Promote to Production**(또는 Redeploy) ③ 폰에서 `chloe-english.vercel.app` 을 열어 옛 앱 일정 화면이 정상인지. 옛 앱은 DATABASE_URL 을 안 쓰니 그 오류는 저절로 사라진다. v2 를 보시려면 Deployments 의 **Preview 주소**로(원장님 계정으로 잠겨 있다).
- **되풀이 막이(대전제-13)**: `vercel.json` 에 ignoreCommand — `V2_PRODUCTION_OK=1` 이 없으면 v2 의 운영 빌드를 건너뛴다. 미리보기는 그대로. 전환일에 그 변수를 켠다(인수인계 §10). 「미리보기를 손으로 Promote」는 못 막는다. 지킴 `check-deploy`.

## 돌리는 법

- 검사 전부: `bash scripts/check-all.sh` (검사 + 빌드). 화면 검사만: `node scripts/check-{palette,mockup,sizes,fonts,contrast}.mjs`
- 눌러보기: `bash scripts/e2e/run.sh` → 내리기 `bash scripts/e2e/down.sh`. 앱 화면이 늘면 `run.sh` 의 `CHECK_URLS` 에 주소를 더한다.
- 목업을 고친 날: `node scripts/mockup-css.mjs` → `check-all` → 아티팩트 다시 올리기 (`docs/목업/README.md`)

## 숫자 (2026-09-05 밤 실측)

- 규칙 182 · 검사 5(겉) + 25(세션·권한·큐·출결·마감·속도·배포·루틴·진도·예정·글·머리·늦귀가·대시보드·등원·달력·학습지·학부모·발송·알림 길·일정·나이스·수강료·시험 회차·성적) + DB 11(+시험·경고·SQL 함수 · 시간대는 UTC 로도) + 2 + e2e 걷기 46 + 190건 · 「지킴: —」 95 (규칙.md 에서 센다 · 끝의 정의는 0)
- 목업 27화면 · 토큰 312 · 새 앱 화면 17(로그인 · 비밀번호 · **대시보드** · **오늘 수업**(+모달 02·02b·02c) · **발송** · **일정** · **학사일정 받아오기** · **시험 회차** · **성적** · **운영(수강료)** · **아이 「나」** · **아이 달력** · **학부모** · **학부모 달력** · 설정(+학원 회선) · 누가 무엇을 보나 · **루틴**) + 주소 넷(크론 · 알림 회신 · 아이콘 · 수강료 엑셀) + manifest + 크론 · e2e 마이그레이션 107 중 106
- 오늘 수업 조회 — 다시 열기 14 · 처음 열기 18(아이가 등원을 먼저 찍은 날은 14 — 판이 이미 서 있다) · 4단 · 대시보드 19 · 5단 (e2e/today 실측) · 아이 「나」 16 · 4단(셈: 로그인 1 · 오늘 1 · 학생 줄 1 · 파도 13) · 학부모 14 · 4단(로그인 1 · 오늘 1 · 내 아이들 1 · 파도 10 · 최근 판의 시험 1 — 판이 있을 때만) · 발송 7(백스톱 2 포함 — 화면 5) · 3단(껍질 1 · 로그인 1 · 오늘 1 · 파도 2 — e2e 실측은 렌더 뒤 백스톱 2 를 더해 센다) · 루틴 4(셈: 껍질 1 · 로그인 1 · 오늘 1 · routine_board 1) · 4단 · 일정 4(셈 — 속도-상한 8) · 4단 · 받아오기 4(셈) · 3단 · 운영 4(셈: 껍질 1 · 로그인 1 · 오늘 1 · fee_board 1) · 4단 · 시험 회차 3(셈: 로그인 1 · 오늘 1 · exam_board 1) · 3단 · 오늘 처음 열기는 곧 있는 회차 한 줄이 더해 +1(파도 안) · 성적 4(셈: 로그인 1 · 주소 인자 · 오늘 1 · score_board 1) · 4단 · 아이 「나」 는 성적·넣을 회차 두 줄이 더해 18(파도 안 · 상한 20) · 학부모는 성적 한 줄이 더해 15
