# 핸드오프 — 2026-08-21 (8/19~21 대개편 뒤)

새 세션은 이 파일 → `CLAUDE.md` → `docs/PRINCIPLES.md` → `docs/지금-남은것.md`
순서로 읽는다. 8/16 핸드오프는 그 전 상태다.

## 이번 사흘에 바뀐 큰 것 (SQL 0133~0142)

- **수업 흐름 전면 개편** (오늘 수업 학생 판):
  검사 1차 판단 자동 채움(클카 ccJudge + 학생 제출물 + 「다 했어요 ○로
  채우기」 버튼, 「자동」 태그, 저장해야 확정) → 안 해온 숙제 처분
  (「오늘수업으로」·「숙제 다시」, 항목별 기본 처분 0141 redo_default) →
  등원 학습 **순서 목록**(0140 inclass_sort, ↑↓·✕·「다음수업」carry_next·
  「숙제로」) + **소화량 게이지**(lib/pace — 학생별 8주 타이머 실적,
  기록 없으면 추정 안 함, 넘치면 「숙제로 돌리기」 제안) →
  **다음 수업 미리 계획**(plan_next, nextRoutine peek) → 저장 시 목록
  바뀌면 학생 푸시. 저장 줄은 .savebar 로 화면 아래 고정.
- **루틴 체계 완성**: routine_steps 에 회독 분기(0135 round — n회독부터,
  가장 가까운 정의 우선) · 예습 숙제(0136 home_next — 다음 단원) ·
  항목별 주의사항(0139 item_notes — 이름[대괄호]) · **영역 루틴**(0137
  area — 교재별이 우선). 원본은 `app/api/admin/routine-sim/drafts.json`
  한 벌 — 고치면 `?op=rebuild` (staff GET)가 전체 삭제 후 재주입
  (학생 위치는 routine_step 번호 폴백으로 유지, 없는 항목 자동 생성,
  잘게 쪼갠 옛 항목은 미사용시 삭제/사용시 숨김).
  **커버리지: 재원 전원** (단어책·저절로 기본1/2·입문2 는 의도적 제외).
- **학습항목**: 굵은 판 재편(클카 문장훈련/단어훈련 등 19개), 활동→항목
  연결(0138 textbooks.act_items — 진도판 📝가 그 항목으로 담음),
  redo_default(0141), 숙제체점→숙제채점 개명.
- **교재·단원**: 쌍둥이 단원 722개 청소(dedupe-units), 3300제 옛 이관
  259개 prune(excel-paths.json), 단원 전량 입력 완료 — 단원 구멍 0.
- **시뮬레이터** `/api/admin/routine-sim` (staff GET, 전부 읽기만):
  기본=부하 시뮬, `?op=flow`(사슬+밀림), `?op=month`(**DB 실루틴 한 달 +
  실코드 nextRoutine 맞대조** — 8/21 오류 0 확인), `?op=retest`(오늘 단어
  재시험 명단+시험 방식 — 클카 시험지 인쇄 런북은 클로드 메모리
  retest-print-runbook), `?op=rebuild`(루틴 재주입).
- **기타**: 전달사항 3단계(조정→확인(잠금)→처리 완료 0142 done_at),
  대시보드 등원 밀림 카드+학생 배너, 플래너 오탐 수정(확장 v1.7 —
  클카 달력이 홑따옴표), 시험기간 학교별 정렬·보충설명 제거,
  단어 전체개수 폴백, 단원평가 공통 목록(integrations grammar_units),
  재시험 건너뛰기(skip_kinds retest), BookProgress lazyRefresh(12초)·
  대단원 막대 탭=통째 고르기·워크북 빼기(0133 skip_acts)·
  marked_on(0134 — 리포트 오늘수업 자동 문구).

## 다음 1순위 — 보강 3건 (원장님 2026-08-21)

① 보강 일정 수정 불가 → 수정 가능하게 ② 보강날 결석 시
「다시 보강/보강 없음」 선택 ③ 결석 아닌 추가 보강에 사유 칸.
(`지금-남은것.md` 맨 위에도 있음)

## 운영 중 주의

- 클카 자동판정 오판은 원장님이 그때그때 알려주기로 — lib/classcard
  ccJudge·CC_ITEM_KIND 를 고친다. 문법훈련 매핑은 나중.
- 원장님 활동 시간대엔 배포 몰아서 (배포 교체 순간 폰 앱이 죽는 이슈).
- check-pages.sh 를 파이프에 물리면 종료코드가 삼켜진다 — `> /tmp/x`
  로 받고 tail 로 볼 것 (8/20 실제로 깨진 커밋 푸시된 적 있음).
