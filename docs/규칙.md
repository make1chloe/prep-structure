# 규칙 — 클로이영어 새 앱(v2)

> **이 파일이 규칙의 원본이다.**
> 지금까지 규칙은 **레포 밖 계획 파일과 코드 주석에만** 살아 있었다. 그래서
> grep 이 안 되고, 검사가 못 걸고, 새로 시작하는 사람이 계획 파일을 찾아야 했다.
>
> **규칙마다 번호가 있다.** 어긋난 곳을 말할 때 그 번호로 가리킨다.
> 코드 주석에서도 번호로 부른다 (`대전제-5` · `0-7` · `검사-⑮`).
>
> ⚠️ **새 규칙이 확정되면 여기에 먼저 적는다.** 코드에만 적으면 다시 흩어진다.
>
> **규칙마다 「지킴:」에 그 규칙을 지키는 검사 이름이 적혀 있다.**
> ⚠️ 2026-09-03 실측 — 규칙 **123개 중 지키는 검사가 있는 것은 52개**,
> **71개는 아직 없다(「지킴: —」).** 없는 자리가 다음에 어긋날 자리다.
> `scripts/check-tables.mjs` 가 **여기 적힌 검사 이름이 진짜 있는지** 본다 —
> 검사 파일 이름을 바꾸면 그 자리에서 걸린다.

---

## 대전제 — 이 계획 안에서 움직이지 않는 것

| 번호 | 규칙 |
|---|---|
| **대전제-0** | **안 되는 것과 모르는 것은 정확하게 말한다. 근거 없는 낙관 금지.** 모르면 「모른다」고 적는다 · 숫자를 지어내지 않는다(센 것만, 어디서 셌는지 밝힌다) · 구현 못 하는 것을 그리지 않는다 · 확인 안 한 것을 확인한 것처럼 쓰지 않는다. **모든 것에 앞선다** <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) · `check-doc-numbers`(인수인계 「한 장 요약」의 숫자를 근거 칸의 셈법 그대로 다시 세어 대조 — 문서가 코드보다 늦으면 빨갛다 · DB·걷기 숫자는 건너뜀으로 센다) |
| **대전제-1** | 전환일까지 **옛 앱이 진실**이다. 새 앱 값은 이관으로만. 예외 셋 — `fixture`(안 지움) · `rehearsal`(재적재가 먼저 지움) · `excel`(재적재가 안 지움) <br>**지킴:** — |
| **대전제-2** | **옛 앱은 동결.** `main` 에 안 푸시한다. 치명 버그만 고친다 <br>**지킴:** `check-deploy`(v2 가 운영으로 빌드되지 않는다 — vercel.json ignoreCommand · V2_PRODUCTION_OK 는 전환일에) · main 푸시 금지는 사람 규칙(CLAUDE.md) |
| **대전제-3** | 둘 중에 고를 때는 **원장 일이 적어지는 쪽**. 기능이 많은 쪽도, 유연한 쪽도 아니다 <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **대전제-4** | **판단은 `lib/` 한 곳.** 화면은 받아 그리기만. 같은 판단이 있는지 grep 먼저 <br>**지킴:** `check-sql` · `check-fast`(오늘 화면이 표를 직접 안 읽나) · `check-close`(오늘의 손이 표를 직접 안 만지나) (옛 검사 check-notify·check-session·check-classcard·check-perm 는 0단계에서 지움) |
| **대전제-5** | **세어 나오는 값은 저장하지 않는다** — 회차·청구액·성취도 %·진도율·출석률·경고 횟수·통과 여부. DB 에는 원재료만 <br>**지킴:** `check-fee`(수강료 합계·청구액은 화면이 센다 — 표엔 받은 금액·받은 날만) · `check-score`(등급·영역 셈은 회차의 컷·문항표로 센다 — 성적 줄엔 원점수·틀린 번호만) · 회차·경고 횟수·진도율은 그 화면을 지을 때 검사도 다시(옛 check-derive 는 0단계에서 지움) |
| **대전제-6** | **지우지 않는다.** 상태로 내린다 (끝냄·중단·절판·보관·휴지통·retired) <br>**지킴:** `check-routine`(뺀 줄은 지우지 않고 `day_item.off` 로 내린다) |
| **대전제-7** | **밖으로 나가는 길은 함수 한 곳에서만** 막는다 (`lib/notify.js`) <br>**지킴:** `check-notify`(알림 서버를 부르는 자리는 `lib/push.js` 하나 · 그것을 들여오는 곳은 `lib/notify.js` 하나 · 자취(notify_log)에 쓰는 곳도 하나) |
| **대전제-8** | 화면을 고르는 기준은 셋뿐 — ① 한눈에 보이나 ② 빠른가 ③ 클릭이 쓸데없이 늘지 않나. 가르는 물음은 「몇 번 여나」가 아니라 **「안 열면 일이 되나」** <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **대전제-9** | **`position:fixed` 스크롤 잠금 · `history.pushState` 닫기 · `createPortal` 안 쓴다** <br>**지킴:** — (옛 검사 check-layout·check-screen-* 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **대전제-10** | **닫는 길은 언제나 화면 안에.** 전면 화면에 닫기 단추를 반드시 둔다. **저장 중이라고 닫기를 잠그지 않는다.** `alert`/`confirm` 안 쓴다 <br>**지킴:** — (옛 검사 check-layout·check-screen-* 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **대전제-11** | **옛 앱 화면을 베끼지 않는다.** 메뉴·페이지 나눔·배치는 백지에서. (데이터 구조는 다르다 — 엑셀 DB 가 뼈대다) <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **대전제-12** | **재원생·학부모 진짜 비밀번호는 전환일까지 한 번도 안 건드린다.** 리허설은 `zz_시험_` 계정으로만 <br>**지킴:** `e2e/screens` · `check-student`(비밀번호 0000 초기화는 `profiles.issued_by_app` — 앱이 발급한 계정만 · 0127) |
| **대전제-13** | **전환일 전에 `v2` 는 운영(Production)으로 지어지지 않는다.** `vercel.json` 의 ignoreCommand 가 Vercel 환경변수 `V2_PRODUCTION_OK=1` 없이는 운영 빌드를 건너뛴다(미리보기는 그대로 선다). 전환일에 그 변수를 켠다 — 코드는 안 고친다. 사고 2026-09-05: 운영 주소 `chloe-english.vercel.app` 에 옛 v2 빌드가 떠 「DB 에 못 붙었습니다」가 보였다(원장님 「이 주소 연 거야」) — 학원이 쓰는 옛 앱이 그 주소에서 죽어 있던 것. ⚠️ 이 막이는 「미리보기를 손으로 운영에 올리기(Promote)」는 못 막는다 — 그것은 Vercel 에서 사람만 막는다 <br>**지킴:** `check-deploy` |

## 원칙

| 번호 | 규칙 |
|---|---|
| **원칙-1** | **같은 값을 두 번 입력하거나 두 벌로 출력하지 않는다** <br>**지킴:** — (옛 검사 check-notify·check-session·check-screens·check-classcard·check-childsaid·check-arrival·check-perm 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **원칙-4** | **고치기 전에 전체 정합성을 검토한다.** 예외가 세 번째로 필요해지면 설계를 다시 본다 <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **원칙-5** | **세어 나오는 값은 저장하지 않는다** (대전제-5 와 같다 — 원칙 쪽 번호로도 부른다) <br>**지킴:** `check-rules-db`(세어 나오는 이름(pct·count·total·sum·avg)의 칸은 넣는 값 아홉뿐 — 래칫) · 판마다 「세는 것은 화면이 센다」 주석(대전제-5 와 같다) |

> ⚠️ **원칙 2·3 은 이 계획에 안 적혀 있다.** 옛 앱 문서에 있었을 수 있으나 **확인 안 됨**(대전제-0).

---

## 0단계 — 처음에 못 박지 않으면 끝까지 못 고치는 것

| 번호 | 규칙 |
|---|---|
| **0-1** | 표마다 **「무엇이 한 줄인가」**를 한 문장으로 — 그 문장이 곧 열쇠. 비는 칸이 열쇠에 들어가면 `nulls not distinct` 나 센티넬. 표마다 **주인**(이관/엑셀/앱)도 한 줄로. 새 이름은 **grep 먼저**. 여러 화면이 함께 쓰는 이름에 화면 하나의 규칙을 붙이지 않는다 — **흔한 한 글자 클래스 금지**(`.v`·`.q`·`.open` 으로 세 번 터졌다) <br>**지킴:** `check-schema` (옛 검사 check-layout 는 0단계에서 지움) |
| **0-2** | **시간대는 하나 — `Asia/Seoul`.** DB 함수 한 벌로 <br>**지킴:** `check-tz` |
| **0-3** | 저장은 **「내가 읽은 그 줄이 그대로일 때만」** — 줄마다 고친 시각을 두고 대조, 다르면 덮지 말고 알린다 <br>**지킴:** `check-schema`①(트리거만 있고 updated_at 칸 없음) · `check-rules-db`(updated_at 이 있으면 touch 트리거 — 손이 직접 적는 여덟은 래칫). · **대조(0131 · 9/7)**: 판 셋이 읽은 시각을 싣고(`grid_board.cells_at` 칸마다 · `student_board`·`inquiry_board` 줄의 `updated_at` — `check-rules-db` 가 판 본문에서 본다) 손 셋이 `.eq(updated_at, 읽은 시각)` 으로 고친다(`setCell` · `setStudent` · `setInquiry` — 0줄이면 「다른 사람이 먼저 고쳤습니다 — 새로고침 뒤 다시 적어 주세요」 `STALE`) · `e2e/today`(학생 14 두 창 — 다른 창이 먼저 고치면 옛 화면의 저장은 STALE · 새로고침하면 남의 것이 그대로). 판 마감은 마감 자체가 대조(assertOpen) · 칩·토글(출결·숙제·진도)은 값 하나를 쓰는 손이라 대조 없이 마지막이 이긴다(잃는 글이 없다) |
| **0-4** | **동작마다 트랜잭션 경계**를 정한다 <br>**지킴:** `check-tx`(lib 의 내보낸 손이 표 둘 이상에 insert·update·upsert·delete(보관함·auth 포함)하면 KNOWN 에 **까닭이 적혀 있어야** — 16 손 · 없는 이름은 빨강 · 목록에만 있는 이름도 빨강). 경계 셋: SQL 함수(plpgsql — 한 트랜잭션) · 걸음마다 ✓/✕ 로 보이는 것(등록 전환 일곱) · 뒤 표가 앞 표를 가리켜 반쯤 돼도 다음에 이어지는 것(sync·멱등 upsert) |
| **0-5** | **목록·JSON 으로 다른 표를 가리키지 않는다.** 전부 짝 표 + 외래키 <br>**지킴:** `check-schema` |
| **0-6** | **앞날 상한** — 지나간 것을 세는 모든 자리는 「오늘까지」로 자른다. 셈 함수 한 벌에 상한을 박는다 <br>**지킴:** — (옛 검사 check-session 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **0-7** | **고르는 값은 DB 에도 건다.** `datalist` 는 안 쓴다(아이폰에서 안 보인다) <br>**지킴:** — (옛 검사 check-excel·check-screen-books 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **0-8** | **목업으로 구상을 먼저 확인받는다** <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **0-9** | **「`v2` 밖에 있는 것」 목록**을 문서로 못 박는다 — `auth.users`·Storage·`public` 함수/타입 이름·PostgREST 노출·Realtime·DB 역할. **파기가 손으로 하는 자리**도 같이 <br>**지킴:** `check-v2only` (옛 검사 check-switchday 는 0단계에서 지움) |
| **0-10** | **어느 화면에서든 늘 손에 닿아야 하는 것** — 퀵메모·로그아웃·되돌아가기. 스크롤로 접히는 자리에 두지 않는다 <br>**지킴:** `e2e/screens` (로그아웃이 상단바에 있나를 진짜 브라우저로) |

## 표를 그릴 때 지키는 것 — 옛 앱이 여기서 다쳤다

| 번호 | 규칙 |
|---|---|
| **표-1** | 번호를 가리키는 칸은 **전부 외래키** <br>**지킴:** `check-rules-db`(_id 칸인데 외래키 아닌 것은 까닭 있는 여덟뿐 — 래칫) |
| **표-2** | **순번을 열쇠로 쓰지 않는다** (가운데를 지우면 뒤가 밀려 남의 기록에 붙는다) <br>**지킴:** `check-rules-db`(sort·seq·no 가 든 열쇠·유니크 0) |
| **표-3** | **비는 칸을 열쇠에 넣지 않는다** (`nulls not distinct` 또는 센티넬) <br>**지킴:** `check-schema` |
| **표-4** | 세어 나오는 값은 칸으로 만들지 않는다 (= 대전제-5) <br>**지킴:** `check-rules-db`(원칙-5 와 같은 검사) |
| **표-5** | 같은 사실을 두 단위로 적으면 **칸을 같게** 둔다 <br>**지킴:** `check-rules-db`(v2 에서 **이름이 같은 칸은 형이 같다** — until_at·count_at·warn_report_at 셋만 예외로 적어 둠 · 새로 어긋나면 빨강) |
| **표-6** | 고르는 값은 DB 에도 건다 (= 0-7) <br>**지킴:** `check-rules-db`(고르는 값 칸에 check — `%_choice` 제약 **11 이상**: 0131 이 notify_log.kind·sink · job_queue.kind · scheduled_send.kind · todo.kind · inquiry.way · payment.source · area 넷(books·area_routine·student_routine·day_area_memo)에 **NOT VALID** 로 걸었다(새 줄만 막고 옛 줄은 그대로) · 아직 없는 15 는 UNCHECKED 래칫 목록 — 새 표는 걸어야 한다) |
| **표-7** | 날짜 기본값은 **「학원이 있는 곳의 오늘」 하나** <br>**지킴:** `check-rules-db`(date 칸 기본값은 v2.today() 뿐) |
| **표-8** | 한 동작이 여러 표를 건드리면 **어디까지가 한 덩어리**인지 정한다 <br>**지킴:** `check-tx`(0-4 와 같은 검사 — 두 표 이상 쓰는 손 16 마다 「어디까지가 한 덩어리 · 반쯤 되면 어떻게 잇나」가 KNOWN 에 있다) |
| **표-9** | 아이가 값을 쓰는 자리에는 **DB 쪽 잠금**. 허용 목록이 아니라 **「그 한 칸만 빼고 비교해 다르면 거절」** <br>**지킴:** — (옛 검사 check-childsaid 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **표-10** | 읽음·수령처럼 「했다」를 남기는 자리는 **서버가 시각과 사람을 정한다.** 앱이 보낸 값을 믿지 않는다 <br>**지킴:** `check-sw`(알림 읽음 — 폰은 자취 번호만 회신하고 시각은 `v2.mark_notify_seen` 이 찍는다) · 등원 시각은 문지기 `arrival_stamp`(0078) — `e2e/screens`(두 번은 못 찍는다) · 수령(material_give.got_at)은 0117 문지기 |

## 처음부터 넣는 것 — 뒤에 붙이기 가장 어려운 것

| 번호 | 규칙 |
|---|---|
| **처음-1** | **돈의 이력** — 단가·기준 회차를 「언제부터 얼마」의 줄로. **반의 요일·시각도 같은 꼴로** <br>**지킴:** `check-fee`(금액을 바꾸면 지난 줄을 닫고 이 달부터 새 줄 — 소급 없음 · 지우지 않음) |
| **처음-2** | **누가 언제 무엇을 무엇으로 바꿨나** — 트리거로 (앱 코드에 맡기면 길이 늘 때 빠뜨린다) <br>**지킴:** `check-schema` |
| **처음-3** | **개인정보 파기** — 보관 기한·파기 예정일·파기한 날 + **파기가 도는 자리 표** <br>**지킴:** — (옛 검사 check-purge 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **처음-4** | **알림 동의** — 언제 동의했나 / 언제 껐나 <br>**지킴:** — (옛 검사 check-push 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **처음-5** | **모든 「소속」에 기간** — 반 명단·학년·교재 배정. 열쇠에 시작일 <br>**지킴:** `check-rules-db`(class_member · class_schedule · student_book · fee_rule 에 from_date·to_date) |
| **처음-6** | **자동화의 뼈대** (아래 뼈대-1~11) <br>**지킴:** 뼈대-1~11 각각의 지킴(check-queue · check-rules-db · check-notify · check-send) |
| **처음-7** | **바깥 서비스 계정을 표로** (제공자 + 저쪽 아이디) <br>**지킴:** `check-phone`(코드가 읽는 환경변수는 다섯뿐 — 로그인 열쇠 둘 · 서버 열쇠 · 스위치 · 크론 열쇠 · 나머지 바깥 계정(푸시·AI·나이스·학원)은 v2.integration 표) |
| **처음-8** | **교과서는 학교의 속성** (학교 · 학년 · 교재 · 연도) <br>**지킴:** 표 `v2.school_book`(0131 — (학교·학년·해·교재) 유니크 · 외래키 둘 — `check-rules-db`) · 판단 `schoolBooksOf`(회차의 학교·학년·해로 고른다 — `check-todo`) · 내신 자료 04 머리 「📚 학교 교과서 YYYY: …」 + 더하기 고르개(`e2e/today` — 고르면 그 학교·학년 아이 모두) |
| **처음-9** | **이관 표시와 묶음 번호**를 모든 표에 <br>**지킴:** — (옛 검사 check-import 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |

## 자동화 뼈대

| 번호 | 규칙 |
|---|---|
| **뼈대-1** | 큐에 **상태·시도 횟수·다음 시도·잠금 시각·마지막 오류**. 「보낸 때」 한 칸을 자물쇠로 쓰면 재시도가 원리적으로 불가능 <br>**지킴:** `check-queue` |
| **뼈대-2** | 자동 생성 열쇠를 **칸으로 쪼갠다** — 규칙·학생·교재·단원·회독·몇번째·**기준 날짜**. ⚠️ 기준 날짜가 없으면 되풀이가 **한 번만** 생긴다 <br>**지킴:** `check-queue` (열쇠 칸은 할 일을 지을 때 다시) |
| **뼈대-3** | 자동으로 생긴 줄이 **「왜 생겼는지」를 외래키로** 가리킨다 <br>**지킴:** `check-rules-db`(todo rule_id·why · notify_log job_id·why · auto_key rule_id · scheduled_send sheet_id·late_id·created_by) |
| **뼈대-4** | **자동 판정과 사람의 번복**을 남긴다 <br>**지킴:** `check-rules-db`(자동 판정 칸 곁에 사람 칸 — progress(status → last_by·confirmed) · score(grade → by_who·confirmed) · progress_flag(kind → seen_by·outcome) · warning_action(kind → by_who) — 하나라도 빠지면 빨강 · 새 자동 판정은 여기 짝을 더한다) |
| **뼈대-5** | **규칙의 임계값은 규칙 줄에** (재시험 「세 번」이 코드에 박히면 원장님이 못 바꾼다) <br>**지킴:** `check-queue`(큐 임계값이 v2.rule 에 있나). 조절·긴 줄 임계값(tune.ask_after · chunk.split_from)도 v2.rule — lib/rule.js 는 줄이 없으면 던진다(조용히 기본값으로 안 돈다) |
| **뼈대-6** | **「오늘 이거 이미 돌았나」를 표로** <br>**지킴:** `check-queue` |
| **뼈대-7** | 바깥에서 받아온 것의 **중복 방지** — 출처가 있을 때만 걸리는 부분 유니크 <br>**지킴:** `check-rules-db`(exams (source, source_key) 유니크 · 살아 있는 줄만 거는 부분 유니크 넷 — 늦귀가 예정 · 보강 · 예약 발송 둘) |
| **뼈대-8** | 치환 자리 **설명은 표에, 자동 자리는 코드에** <br>**지킴:** 표 `v2.placeholder`(0131 — 열쇠·설명·보기 13 · `check-rules-db` 가 학생명·학원명… 열쇠를 본다) · 발송 10 「{{ }} 치환 자리」 표(`e2e/today` — 13 · {{학생명}}) · 자동 자리(채우는 손)는 코드(`lib/send-plan.js placeholderRows` — `check-send`) |
| **뼈대-9** | 크론이 매일 훑는 것은 **새 셈을 만들지 않는다** — `lib/` 의 셈을 부르기만 하고, DB 에는 「그날 이미 만들었나」만 <br>**지킴:** `check-queue` |
| **뼈대-10** | 크론은 **「학원의 오늘」을 인자로 받는다** <br>**지킴:** `check-queue`(크론이 v2.today 를 받는다 · day_ran · runDue 만) |
| **뼈대-11** | **안 채운 치환 자리(`{{`)는 못 나가게** 한다 — 보내지 않고 원장님께 되돌린다 <br>**지킴:** `check-notify`(데일리리포트 손이 `unfilled` 을 본다) · `check-send`(치환 자리 셈 · 발송 판에서 holes 는 못 고른다) |

---

## 속도

| 번호 | 규칙 |
|---|---|
| **속도-상한** | 대시보드 **조회 20 · 5단** / 오늘 **20 · 4단** / 발송 **6 · 2단** / 일정 8·2단 / 검사 8·2단. **합격선 0.5초** <br>**지킴:** `check-fast`(오늘·대시보드·아이·달력·학부모·발송·루틴·일정·받아오기 — 층 상한 · 표 직접 읽기 0 · 파도) · `e2e/today`(오늘·대시보드·학부모·발송 — 조회 수를 PostgREST 요청 로그로 센다). 다른 화면은 지을 때 CAPS 에 더한다 · 0.5초는 실제 배포에서 잰다 |
| **속도-1** | **탭을 만들지 않는다** (탭 전환이 전체 재조회). 접기로 줄인다 — **접기는 다시 조회하지 않는다**.<br>⚠️ **예외 (원장님 2026-09-05 「ㅇㅇ좋아」): 보기 전환(⊞표 ↔ ▦보드)은 재조회 0이면 탭이 아니다** — 한 번 받은 줄을 그 자리에서 다르게 그리기만 한다. 금지의 까닭(다시 받아옴)이 없기 때문이다. 보드를 지을 때 **「보기 바꿀 때 서버 조회 0건」을 보는 검사를 같이 세운다** — 그 검사가 빨개지면 누가 재조회를 넣은 것이다 <br>**지킴:** `e2e/today`(05 ⊞표 ↔ ▦보드 — PostgREST 요청 로그로 조회 0건을 센다) · `check-todo`(카드 목록은 한 벌에서 한 번) |
| **속도-2** | **배지를 첫 페인트에서 뺀다.** 동시 호출은 합치되 **실패한 것은 캐시하지 않는다** <br>**지킴:** `check-phone`(껍질 app/layout.js 는 rpc 0 · 표 읽기는 메뉴 권한 하나 · app/_shell 부품은 표를 안 읽는다) |
| **속도-3** | **예약 발송이 렌더를 막지 않는다.** 크론이 백스톱 <br>**지킴:** `check-notify`(발송 화면의 백스톱은 렌더 뒤 `after` — 기다리지 않는다) · 예약은 표(`scheduled_send`)에만 적고 크론·백스톱이 큐로 넘긴다(`lib/send.js promoteScheduled`) |
| **속도-4** | **줄 수 상한을 안 건다.** 학생·날짜로 좁혀 읽는다 (Supabase 는 한 번에 1000줄) <br>**지킴:** — (발송 판은 오늘 날짜·살아 있는 예약으로만 좁혀 읽는다 `lib/send.js sendBoard` — 검사는 그 화면이 길어질 때) |
| **속도-5** | **누른 그 단추만 바뀐다.** ⚠️ **되돌릴 수 없는 낙관 갱신은 하지 않는다** — 발송·마감·파기는 서버 답을 기다린다 <br>**지킴:** `check-phone`(useOptimistic 0 · 낙관이 코드 글자로 남은 자리 0 — 낙관은 출결·○△✕·진도 셋, lib 주석에만) · `e2e/today`(마감·보내기는 새로고침 뒤 상태로 본다 — 「보내고 마감」 → 보냄 + 마감됨) |
| **속도-6** | 당김 새로고침·「새 버전」 띠가 **적던 것을 안 날린다** <br>**지킴:** `check-phone`(브라우저 저장은 배색(chloe-skin)뿐 — 임시저장은 DB 판이라 새로고침·새 버전 띠가 적던 것을 못 날린다) |

## 폰 화면 — 이미 치른 값

| 번호 | 규칙 |
|---|---|
| **폰-1** | 입력 요소 글씨 **16px 이상** — 인라인 `fontSize` 도. ⚠️ **`(pointer:coarse)` 로 가른다**(폭이 아니라 기계로 — 아이패드 768px 이 빠졌었다). ⚠️ **9/5 디자인 지침 8절: 폰에서는 본문 토큰(`--fs5`)째 16으로** — 입력칸만 키우면 본문과 어긋난다(원장님 「크기도 통일」). 옮길 때 `globals.css` §10 을 그렇게 바꾼다 <br>**지킴:** — (옛 검사 check-layout·check-screen-* 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **폰-2** | 열릴 때 **`autoFocus` 안 건다** <br>**지킴:** `check-phone`(autoFocus 0 — 06c 새 메모 칸에서 하나 걷었다 2026-09-06) |
| **폰-3** | sticky 를 넣는 자리는 **조상에 `overflow:hidden` 이 없는지** 먼저 본다. 모서리만 자를 거면 `overflow:clip`(스크롤 상자가 아니다) — 2026-09-05 밤 `.frame{overflow:hidden}` 때문에 줄 끝 저장줄이 안 붙었다 <br>**지킴:** `e2e/today`(폰 390: 저장줄의 rect 아래가 화면 아래와 같나 — 「position 이 sticky」만 보면 헛통과한다, 폰-5) |
| **폰-4** | 당김 새로고침이 **편집 중 표시를 보는가** <br>**지킴:** `check-phone`(body{overscroll-behavior-y:contain} — 목업 규칙에서 갈라낸 것 · 당김 새로고침 자체가 안 일어난다) |
| **폰-5** | **글자로 훑는 검사는 헛짚고 헛통과한다** — ① 주석을 먼저 지운다(줄 끝 `//` 포함) ② 앞뒤를 정확히 문다 ③ 같은 조건 블록이 여럿이면 합집합. **일부러 어기는 본보기를 두고 검사가 잡는지 확인한다** <br>**지킴:** `check-phone`(lib·app 글자를 훑는 검사 아홉이 전부 주석을 먼저 지운다(strip) — check-notify·check-queue·check-sw 에 2026-09-06 붙였다) |
| **폰-6** | `visualViewport` 로 저장줄 높이 · `env(safe-area-inset-bottom)` + `viewportFit:"cover"`. ⚠️ 새 앱(2차-10)은 저장줄을 **흐름 안 sticky**(줄의 직접 자식 `.rowbar`)로 붙여 높이를 잴 일이 없다 — `visualViewport` 는 고정(fixed) 줄에만 필요했다 <br>**지킴:** `e2e/today`(폰 390: 줄 끝 저장줄이 sticky 로 화면 아래 · padding-bottom 에 env(safe-area-inset-bottom) · 폰에서만 「닫기」) · `check-sw`(viewportFit cover) · `check-sizes`(붙는 줄은 겹침에서 뺀다) |
| **폰-7** | 임시저장 열쇠에 **반까지** 넣는다 + 읽는 쪽에 **옛 열쇠 폴백** <br>**지킴:** `check-phone`(임시저장 열쇠 = 판(day_sheet: 학생 × 반 × 날) — 브라우저 열쇠가 없다) |
| **폰-8** | 줄을 열고 닫을 때 **목록 자리를 지킨다** (`scroll-margin-top`) <br>**지킴:** `check-phone`(.row{scroll-margin-top} — 목업 규칙에서) |
| **폰-9** | **클래스 이름 겹침 금지** · flex/grid 자식에 **`min-width:0`** · 몸통에 **`word-break:keep-all`** · grid 는 **`minmax(0,1fr)`** · **글씨를 `opacity` 로 흐리게 금지**(색으로 말한다).<br>⚠️ **아이콘은 예외** — 투명도가 곧 뜻이다(확정-㉖ 정정, 2026-09-03) <br>**지킴:** — (옛 검사 check-layout 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |

---

## 자동 검사 — 계획이 만들라고 한 것

| 번호 | 무엇을 보나 |
|---|---|
| **검사-①** | 밖으로 나가는 길이 `notify` 한 곳을 지나나 <br>**지킴:** `check-notify` |
| **검사-②** | 출결 쓰는 길이 `attendanceWrite` 를 부르나 · 값은 `v2.day_sheet.attend` CHECK(0101)와 같다 — 세그먼트의 다섯 + 앱이 채우는 `makeup`(보강으로 온 날)·`off`(휴강). ⚠️ 0101 이 처음엔 `makeup` 을 빠뜨려 옛 앱에서 옮긴 보강 출결 368줄이 있는 실제 DB 에선 멈출 것이었다 — 실제 DB 에 돌기 전이라 제자리에서 고쳤다(9/5 밤) <br>**지킴:** `check-attend` (등원 찍기는 아이 화면을 지을 때 다시) |
| **검사-③** | 조회에 1000줄 페이징이 있나 <br>**지킴:** `check-queue` |
| **검사-④** | `alert`/`confirm` 이 없나 <br>**지킴:** — (옛 검사 check-screen-* 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑤** | 마감 가리기가 빠뜨린 칸이 없나 — 판에 쓰는 손 전부가 assertOpen 을 지나나 <br>**지킴:** `check-close` |
| **검사-⑥** | 마이그레이션이 `auth.`·`storage.`·`public.` 을 건드리나 (전환일 파일만 예외). <br>**지킴:** `check-v2only` |
| **검사-⑦** | 발송이 `NOTIFY_SINK` 를 안 보고 쏘는 자리가 없나 <br>**지킴:** `check-notify`(스위치를 읽는 곳은 `lib/notify-plan.js sinkOf` 하나 — 쏘는 길이 하나라 안 보고 쏠 자리가 없다) · `check-send`(기본 off · 이상한 값은 던진다 · off 는 아무 기기에도 안 보냄) |
| **검사-⑧** | 접근 규칙 흉내내기 (`SET ROLE` + jwt claims) <br>**지킴:** `check-v2-rls` (옛 검사 check-v2-all 는 0단계에서 지움) |
| **검사-⑨** | 파기 목록에 없는 새 표를 만들면 깨지나 <br>**지킴:** — (옛 검사 check-purge 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑩** | 뺄 항목을 얹은 뒤에도 세 묶음이 안 비나 · 분모에서 뺄 것이 빠지나 <br>**지킴:** `check-routine`(필수만으로 줄여 묶음이 통째로 비면 줄이지 않는다) · `e2e/today`(뺀 줄은 분량에서 빠지고 되살아난다) |
| **검사-⑪** | 저장·삭제가 **「몇 줄 바뀌었나」**를 보고 **0줄이면 실패**로 되돌리나 <br>**지킴:** — (옛 검사 check-day·check-routine-edit·check-areamemo 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑫** | 자동 생성 열쇠가 계기의 칸 조합과 맞나 <br>**지킴:** `check-rules-db`(auto_key 유니크 = 규칙·학생·교재·단원·회독·몇번째·기준 날짜) · `check-queue` |
| **검사-⑬** | 치환 자리 이름이 `lib/` 상수와 겹치면 실패 <br>**지킴:** — (옛 검사 check-notify 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑭** | 예습 또는 「소단원을 다 안 덮은 배정」에 ○ 을 줘도 단원이 완료로 안 올라가나 · 메모 자동완료가 **그 교재에만** 걸리나 <br>**지킴:** `check-progress`(예습 next · 조각 range_note 에 ○ 을 줘도 완료 아님 · 메모 자동 ○ 은 그 교재의 학습 줄만) · `e2e/today` |
| **검사-⑮** | `classRoster` 밖에서 반 명단을 **직접 조회**하는 자리가 없나 <br>**지킴:** — (옛 검사 check-session 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑯** | `excel`·`fixture` 줄이 재적재가 지우는 줄을 가리키면 실패 <br>**지킴:** — |
| **검사-⑰** | 한 교재에 `excel` 단원과 이관 단원이 함께 있으면 실패 <br>**지킴:** `check-book`(올릴 때 묶음은 그 교재의 기존 묶음을 따른다 — 이관 교재에 올리면 이관으로) · 실제 DB 전수는 그 화면을 다시 지을 때 |
| **검사-⑱** | 옛↔새 매핑표에 짝 없는 이관 줄이 있으면 실패 <br>**지킴:** — (옛 검사 check-import 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑲** | 고르는 값이 **화면에 두 벌**로 적혀 있지 않나 <br>**지킴:** — (옛 검사 check-excel·check-screen-books·check-perm 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **검사-⑳** | 마이그레이션이 만드는 표가 `docs/표-유도.md` 에 없으면 실패 <br>**지킴:** `check-tables` |
| **검사-㉑** | 이관 뒤 옛 앱에 짝 없는 새 교재가 생기면 실패 <br>**지킴:** — |
| **검사-㉒** | **잠금화면에 내용을 안 띄우나** (원장님 2026-08-07) <br>**지킴:** `check-sw` (옛 검사 check-notify 는 0단계에서 지움) |
| **검사-㉓** | 레이아웃 여섯 — 삐져나감·겹침·세로 글자·잘림·화면 밖·늘어난 단추 (**320·390·768·1400px**) <br>**지킴:** `check-sizes`(1280·390 — 넘침 · 형제 겹침 · 글씨>상자 · 잘림(세그먼트·단추) · **세로 글자**(자식 없는 글자 상자의 띄어쓰기 없는 2~8자 낱말이 두 줄로 꺾임 — 2026-09-05 폰 캡처의 「상/황」이 잡음 · 표 칸과 세운 글은 뺀다 · 목업에도 돌린다)) · 320·768·1400 은 아직 |
| **검사-㉔** | 앱 CSS 로 목업 27화면을 그리면 원본과 **화소가 같은가** · 토큰이 같은가 (PC 1280 · 폰 390) <br>**지킴:** `check-mockup` |
| **검사-㉕** | 부품 높이가 종류마다 하나인가 · 틀 밖 넘침 0 · 글씨>상자 0 · 형제 겹침 0 · **잘림 0**(세그먼트가 칸보다 넓다 · 단추 글씨가 단추보다 크다 — 회차 세그먼트가 잘렸는데 초록이던 2026-09-05) · **눌린 입력칸 0**(글자를 받는 입력칸이 60px — 점수칸은 44px — 보다 좁으면 값이 안 보인다 — 13 수강료 금액 칸이 폰에서 23px · 16 성적 점수칸이 35px 이던 2026-09-06 · 스테퍼 안은 일부러 좁아 뺀다) <br>**지킴:** `check-sizes` |
| **검사-㉖** | 글꼴이 하나인가 · 글씨 크기가 열 단계 안인가 · 입력칸 = 본문인가 <br>**지킴:** `check-fonts` |
| **검사-㉗** | 배색 다섯 벌 × 밝음·어두움 열 자리에서 글씨 대비 3.5 아래 0 · 갈색 0 (토큰은 `check-palette`, 그려진 글씨는 `check-contrast`) <br>**지킴:** `check-contrast · check-palette` |
| **검사-㉘** | 로그인 확인이 `lib/session.js` 한 벌인가 — `auth.getUser()` 0 · 모든 화면이 guard/whoami 로 시작 · 스키마는 db() 한 벌 <br>**지킴:** `check-session` |
| **검사-㉙** | 권한 열쇠가 `lib/perm.js` 에만 있나 · 칸 32 · `v2.can` 이 fail closed · principal 이 role_access 에 못 들어가나 <br>**지킴:** `check-perm` |
| **검사-㉚** | 큐에 다섯 칸 · 자물쇠 locked_at · 임계값은 `v2.rule` · 크론은 오늘을 받고 runDue 만 부르나 <br>**지킴:** `check-queue` |
| **검사-㉛** | 진짜 브라우저로 걷기 — 역할마다 로그인 · 틀린 비밀번호 · 비밀번호 바꾸기 문 · 켠 만큼만 메뉴 · 권한 켜고 끄기 · 로그아웃 (`bash scripts/e2e/run.sh`) <br>**지킴:** `e2e/screens` |
| **검사-㉜** | 진짜 브라우저로 **오늘 수업(01) 걷기** — 판이 서고 지난 숙제가 검사 줄로 오나 · 출결이 남나 · △ → 어디까지 → 나머지가 다음 숙제 줄로 · 항목 더하기·미루기(지우지 않는다) · 루틴 깔림·회차·조절 02·줄이기·교재 상태·메모 · 진도 체크 02b · 시험·다음 시간 시험 · 경고·반성문·기준 스테퍼 · 결석·지각 예정 02c · 늦귀가 +20분·적어 두기·보내기 · 글·마감 · 마감 뒤 읽기만·자동 ○ · 월초 띠 · 처음 열기·다시 열기 조회 수 ≤ 20 <br>**지킴:** `e2e/today` |
| **검사-㉝** | 오늘 화면(page.js)이 표를 직접 안 읽나(lib 만 부른다) · 화면 머리의 await 수(= 층)가 상한 안인가 · `lib/day` 의 판 여는 길에 파도(Promise.all)가 남아 있나 <br>**지킴:** `check-fast` |
| **검사-㉞** | 루틴 깔기 판단(`lib/routine-plan.js`, 순수)이 본보기대로 도나 — 덩어리는 안 한 소단원 차례에서 같은 대단원 안(확정-④) · 자리마다 줄(학원 class+both · 숙제 home+both) · 필수만은 필수 줄만(묶음이 비면 줄이지 않는다, 검사-⑩) · 교재 상태 셋(확정-⑬) · 회차 고르기 셋씩 · 뺀 줄 off(대전제-6) <br>**지킴:** `check-routine` · **4단계-1**(9/7): `trimCounts`(줄이기 「그대로 N · 필수만 M」 — 판 안의 `day_item.required` 로 · 교재에 필수가 없으면 다 필수) · `heavyBand`(📣 「오늘 좀 많습니다 — 합쳐 N쪽」 — 문턱 규칙 `day.heavy_pages` · 같은 소단원은 한 번 · 뺀 줄은 안 셈 · 0 이면 없음) |
| **검사-㉟** | 시험 판정·리포트 문이 SQL 한 곳에서 맞게 도나(진짜 DB, 되돌린다) — lib·화면에 「>= 통과선」이 없다 · style_for 가 아이 → 교재 → 학원 기본 차례 · 경계값(90%)은 통과 · 틀린 개수 없으면 판정 없음 · 값 없으면 리포트에 안 나감 · 교재멈춤이면 못 냄, 직접 범위는 상관없음 <br>**지킴:** `check-quiz` |
| **검사-㊱** | 경고를 사실에서 세는 SQL 한 곳(`v2.warn_days` · `warn_states` · `warn_band`)이 맞게 도나(진짜 DB, 되돌린다) — 지각+미제출 같은 날 = 1 · 미흡은 `warn.weak_from` 건부터 · 단어 미통과만(문장·재시험은 아님) · `warn.report_at` 째와 배수째에 묻고 유예는 다음 경고에 다시 · 「정리」는 그 달 1일부터 세고 기록은 남는다 · 「그냥 두기」는 띠만 · 달의 1일이 아니면 DB 가 막는다 <br>**지킴:** `check-warn`(하루 1회 · 미흡 규칙 건수 · 단어만 · 마지막 반성문 뒤 N회째(아이 기준 → 학원 기본) · 유예 · 정리·띠 · 달의 1일) |
| **검사-㊲** | 진도 판단(`lib/progress-plan.js`, 순수)이 본보기대로 도나 — ○ done · △ doing · ✕ 그대로 · 단원 없는 줄은 손 안 댐 · 예습·조각은 완료 아님 · done·skip 은 검사로 안 내려감 · 마감 메모 자동 ○ 은 학습 줄만(조각 ◐) · 대단원 요약(끝냄·건너뜀·지금) <br>**지킴:** `check-progress` |
| **검사-㊳** | 결석·지각 예정 판단(`lib/plan-plan.js`, 순수)이 본보기대로 도나 — 달력은 월요일부터 42칸 · 날마다 표시의 우선(휴강 > 결석 > 지각 > 보강 > 수업) · 물린 것(cancelled)은 안 뜬다 · 수업일·결석·지각 날만 고를 수 있다(보강 날·휴강·수업 없는 날은 못 고른다) · 보강 글(잡음 「보강 10/18 14:00」 · 안 잡음 · 안 잡힘). 수업일 자체는 SQL 한 곳 `v2.student_days` <br>**지킴:** `check-plan` |
| **검사-㊴** | 날짜 글자(`2026-09-06`)의 **요일·며칠 뒤 셈이 프로세스 시간대에 안 기대나** — `lib/day-plan.js`(순수, UTC 메서드) 한 벌만 쓴다. Vercel 은 UTC 로 돌아 `new Date(d+"T00:00:00+09:00").getDay()` 는 거기서 **하루 전 요일**이 된다(오늘 수업이 전날 요일의 반을 부른다). 2026-09-05 02c 캡처 「9월 6일 **토**」가 잡았다 <br>**지킴:** `check-tz`(UTC 로 돌려 2026-09-06 = 일 · 30일 전 · 달 넘김 · lib·app 에 흩어진 getDay·setDate·+09:00 셈 0) · `e2e/today`(고른 날 머리의 요일) |
| **검사-㊵** | 부모님께 글 판단(`lib/comment-plan.js`, 순수)이 본보기대로 도나 — 갈래 다섯·길이 넷 · **상황이 길이를 먼저 고른다**(값은 `v2.rule comment.cap.*`) · 그날 상태에서 갈래가 저절로(늦은밤 › 시험후 › 시험전 › 숙제안함 › 보통) · 글자 세기(공백 포함) · 넘으면 **문장 끝에서** 자른다 · 글 밑에 붙는 줄(개수 있는 시험만 · 늦귀가 · 반성문) = 미리보기 = 09·10 · AI 초안 그대로인가 · 지시문에 길이 상한·사실만 <br>**지킴:** `check-comment` |
| **검사-㊶** | 학생 줄 머리 판단(`lib/roster-plan.js`, 순수) — 「중2 · 신정중」(급+학년 · 학교 이름 줄임) · 진도 점(오늘 검사 줄 하나에 점 하나, ○△✕·) · 「N회독째」(2부터) · 「N일째 안 봄」(안 본 줄 중 **가장 오래된 숙제가 며칠 전** 것인가 — 낸 다음 날은 아직 아니다) · 🗺 오늘 한 단원(검사 결과대로 ○◐✕ · 오늘 학습 ◐) · 영역 네 칸 · 📝 단원평가 결과(통과선 규칙) <br>**지킴:** `check-roster` |
| **검사-㊷** | 늦귀가 판단(`lib/late-plan.js` · `lib/day-plan.js` 의 서울 시각, 순수) — 예상(약속)과 실제 하원의 **차이는 세어 나온다**(「실제 하원 22:05 · 예상보다 25분 늦게」) · 되풀이 띠(「3주 안 3번째 남습니다 — 숙제량을 볼까요?」 — 셈은 SQL `v2.late_states`, 기준은 `v2.rule late.repeat_count·late.repeat_days`) · 안 보낸 채인가 · 마감 전에 묻는 것은 **한 상자**(AI 초안 그대로 + 안 보낸 늦귀가) · 서울 시각 읽기·찍기가 시간대와 무관(UTC 로도 돈다) <br>**지킴:** `check-late` · **4단계-1**(9/7): `reasonChips`(3b 사유 칩 — 검사 ✕ 항목 「… 미제출」 · 처분이 남아서면 「반성문 — 오늘 남아서」 는 고정 · 글에 든 조각은 on) · `toggleReason`(「 · 」 로 넣고 뺀다 · 손으로 쓴 글은 그대로) · `e2e/today`(✕ → 칩 → 누르면 사유에 · 다시 누르면 뺌) |
| **검사-㊸** | **SQL 함수가 없어진 칸·표를 읽고 있나** — v2 의 읽기만 하는(stable·immutable) `language sql` 함수를 전부 null 인자로 한 번씩 불러 본다(한 트랜잭션, 끝에 되돌림). 0083 이 `late_stay.left_at` 을 걷어냈는데 0071 의 `late_for_family()` 가 그 칸을 그대로 읽고 있었다(부르면 「column l.left_at does not exist」) — 함수는 만들 때만 보고, check-sql(SETUP_ALL 3번)도 0071 이 0083 보다 먼저 돌아 못 잡는다 <br>**지킴:** `check-sql-funcs`(DB) |
| **검사-㊹** | 대시보드 판단(`lib/dash-plan.js`, 순수) — 빵꾸 막이 세 까닭(루틴 없음 › 회차 없음 › 안 한 소단원 없음 — 깔기 `planBook` 과 같은 판단 · 학생 루틴이 영역 루틴을 이긴다 · 멈춘 교재는 뺀다 · 아이 수로 센다) · 날짜 글 「9월 6일 일」 · D-day · 시험 줄(가까운 차례 · 영어일 없는 학교) · 클래스카드 수신(2일째부터 ✕) · 하루 정리(새벽 4시 전은 아직) · 「어제 21:40」 <br>**지킴:** `check-dash` · 교재 줄(book_id)이 있으면 그 교재는 빵꾸가 아니다(4단계-5) |
| **검사-㊺** | 등원·하원 판단(`lib/arrival-plan.js`, 순수) — 걸음 셋(핸드폰·출석·숙제) + 집에 가요(4) · 도착은 **가장 이른 등원 걸음**(저장 안 함) · 지각 분은 반 시작과 견줘 세어 나온다(유예 분은 `integration arrival.graceMin`) · 학원 회선(IPv4 그대로 · IPv6 앞 4덩어리 · 빈 목록은 아무도 못 찍음) · 요청 주소 읽기 · 반 고르기(하나·둘(답 ⑫)·보강·없음) · 「앞으로」 줄 · 학원 줄의 차례(0084 ⑱) <br>**지킴:** `check-arrival` · `e2e/screens`(아침: 등원 전에도 「오늘 낼 숙제」 · 출석 → 판이 선다 · 두 번 못 찍음) · `e2e/today`(저녁: 남아서 · 집에 가요 → 원장 화면 「실제 하원」 같은 줄 · 마감 뒤: 다 했어요 차례·무르기 · 선생님 한 마디) · 숙제 줄 잠금 homeSteps(🔒 줄만 바로 앞 숙제 줄을 끝내야 — 4단계-5) |
| **검사-㊻** | 달력 판단(`lib/cal-plan.js`, 순수 — 목업 09b, 학생·학부모가 같은 한 벌) — 달 범위(월요일부터 42칸) · 날마다 표시의 우선(휴강 – › 결석 ✕(보강 ↻) › 지각·늦귀가 ⏰ › 정시 ✓ › 보강 ↻ › 앞날 수업 예정 · + 숙제 📘 · 시험 📝) · 날 하나의 줄(등원→하원 · 결석·보강·사유·「미리 알려 주셨습니다」 · 수업일지(마감한 글) · 숙제 N개 · 시험 결과 · 휴강 사유(답 ①)) <br>**지킴:** `check-cal` · `e2e/today`(마감 뒤 아이 달력: 오늘 ⏰📘📝 · 어제 ✓📘 · 내일 ✕ · 글피 ⏰ · 오늘의 줄 · 어제·내일 칸) |
| **검사-㊼** | 받을 교재·학습지 판단(`lib/material-plan.js`, 순수 — 목업 07 📚) — 단계 넷(아직·받음·하는 중·완료) · 갈래(자료 종류)별 묶음 · 끝낸 것은 따로 · 스스로 정한 마감 글(까지·오늘까지·지났어요) · 달력 🚩(끝낸 것은 안 뜬다). DB 문지기 `0117`(아이는 stage·due_on 만 · got_at 은 서버가 · 학부모는 못 고침) <br>**지킴:** `check-material` · `e2e/today`(받음 → 저장 · 마감 날짜 → 「M/D까지」 · 달력 🚩) |
| **검사-㊽** | 학부모 화면 판단(`lib/parent-plan.js`, 순수 — 목업 09) — 숙제 % · 수업일지 꼬리표(단어 N/M · 숙제 N% · 늦귀가) · 오늘 늦귀가(보낸 것만 · 실제 하원이 찍히면 「HH:MM 에 갔습니다」) · 보낸 것 줄 · 오늘 등원(아이가 찍은 걸음 → 「HH:MM 도착 · 하원 아직」) · 다음 시간 시험 줄. **학부모는 마감한 판만**(RLS `sheet_visible_to` — 사고 #7 · 0084) · 빈 카드는 숨긴다(확정-⑮) <br>**지킴:** `check-parent` · `e2e/screens`(아침: 등원만 보이고 오늘 판은 안 보인다 · 어제 판 · 빈 카드 숨김) · `e2e/today`(마감 뒤: 늦귀가 안내 · 오늘 수업 + 꼬리표 · 남기실 말 · 달력) |
| **검사-㊾** | 발송·알림 판단(`lib/send-plan.js` · `lib/notify-plan.js`, 순수 — 목업 10) — 묶음(🕘 지금: 늦귀가는 보냄 표시만 · 📨 마감하면: open·ready·holes·scheduled·queued·sent · 🔔 저절로: 안 끝난 일만 · 📢 예약: 표 + 방해금지로 미뤄진 일) · 오늘 나간 것의 상태(보냄·읽음·N번 열어봄 · ⚠️ 못 보냄 · 🧪 리허설은 그렇게 말한다, 대전제-0) · 읽음 셈 · 예약 때(확정-㉕) · 치환 자리(뼈대-11) · 제목 「[클로이영어] 수업 안내」 · 잠금화면 문구 「앱에서 확인해주세요.」(원장님 2026-08-07) · 스위치 off·self·live · 받는 기기(학부모 기기 · 끈 기기 빠짐 · 같은 endpoint 한 번) · 방해금지(규칙 `send.quiet_from`·`send.quiet_to`, 원장님 2026-08-07 「오후 11시부터 오전 9시까지 기본설정」 — 가족 알림은 끝나는 때로 미룬다) <br>**지킴:** `check-send`(21건 — + 뼈대-8 치환 표 줄) · `check-notify`(길 하나 · 스위치 하나 · 손 다섯 · 크론이 손을 들여온다) · `e2e/screens`(아침: 묶음 넷 · 마감 0/N · 리허설 알약) · `e2e/today`(마감 뒤: 조회 ≤ 6+2 · 보냄 표시만 · 보낼 것 2/2 · 백스톱 · 지금 보내기 · 예약·취소 · 크론) · **4단계-2a**(9/7): 갈래 열둘(`score` 성적 입력 안내 · `fee` 수강료 안내 — 0133 이 notify_log·scheduled_send 제약을 같은 열로 다시 건다) · `check-parent`(💰 `feeLine` — 수납 줄이 있을 때만 · 📨 `noticeLines` — 실제로 나간 자취만 · 읽음/안 읽음) · `e2e/today`(16 「📨 안 낸 아이 재촉」 1명 · 13 「💰 안 받은 집에 안내」 1집 · 학부모 💰 카드 「9월 수강료 300,000원 · 받음」 · 대시보드 「안 읽은 집 0」) |
| **검사-㊿** | 루틴 화면 판단(`lib/routine-plan.js`, 순수 — 목업 11) — 살아 있는 줄(내린 줄·내린 항목 빠짐) · 영역 머리 알약(학원 a · 숙제 b · 필수 c) · 아이의 한 영역(학원 기본 그대로 / 이 아이만 고침 / 뺐습니다) · ▲▼(이웃과 sort 맞바꿈) · 예습 소단원(place next) · 「이대로면」(남은 소단원 ÷ 회차 = 수업 N회 → 앞으로의 수업일 목록에서 그날 · N.N개월 · 수업일이 모자라면 없음) · 체크리스트 글 가르기 · 영역 일곱 · 교재 예외(교재 줄 › 아이 영역 줄 › 학원 영역 줄 · 11 교재 칸의 「이 교재만 고침」+뺀 것 — 4단계-5) <br>**지킴:** `check-routine`(+12건) · `e2e/screens`(루틴 걷기 14 — 「이 교재만 다르게」 → 「영역 루틴으로」 · 영역 줄 🔒) · `e2e/today`(교재 잇기 「이 날부터」 → 「독해 루틴이 없습니다」 → 끝내기 「오늘까지」) |
| **검사-51** | 나이스 받아오기 판단(`lib/neis-plan.js`, 순수 — 목업 12b · 확정-㊲) — 학년도(3/1~2월 말) · 주소·답 읽기(INFO-200 은 비었을 뿐) · 이름 펴기(1회고사 → 1학기 중간고사 · 학평=모평=모의고사) · 갈래(쉬는 날 먼저 › 전국은 원장님의 낱말 목록으로만 › 평가(수행·성취도)는 회차 아님 › 내신 지필 › 행사) · 같은 날 학년별 줄 합치기 · 주말만 건너뛰어 기간 잇기 · 학교 회차 후보(학년 하나면 학년 적음) · 전국 회차(모의고사는 학년마다 하루 · 수능은 영어일 = 그날) · 출처 열쇠(학교코드:이름:시작날 · common:날:이름:학년) <br>**지킴:** `check-neis`(17건) |
| **검사-52** | 일정 판단(`lib/schedule-plan.js`, 순수 — 목업 12) — 칸의 일들과 차례(휴강 › 결석 › 영어 시험일 › 시험 기간 › 보강 › 지각 › 할 일) · 물린 것은 안 뜬다 · 반을 고르면 그 반 아이들·그 반(또는 전체) 휴강만 · 회차(요일×달−휴강+반 보강일 — 규칙 `schedule.sessions_per_month` 8 과 견줌 · 특강은 회차만큼) · 「보강 안 잡힘 N」 · 42칸(그 달 밖은 빈 칸) · 하루의 줄(결석은 아이마다 사유 → 보강 잡힘/안 잡힘) <br>**지킴:** `check-schedule`(12건) · `e2e/today`(이 달 · 오늘 결석 예정 · 02c 에서 적은 내일·모레·글피 · 다음 달에 휴강·할 일·시험 넣고 무르기) |
| **검사-53** | 수강료 판단(`lib/fee-plan.js`, 순수 — 목업 13 · 처음-1 · 대전제-5) — 단가 줄(학생 › 반 › 학년 기준 · 그 달에 걸친 것만) · 특강은 단가 × 회차(일정 12 와 같은 셈) · 그 달 줄(받은 금액이 있으면 그것 · 상태 셋 받음·안 받음·금액 없음) · 합계는 화면이 센다 · 금액을 바꾸면 **이 달부터** 새 단가 줄(같은 달 줄이면 고침 · 반·학년 기준과 같으면 안 만듦 · 회차제 줄은 안 건드림) · 결제선생 엑셀(열 이름 후보 · 미납 · 「10월」은 그 해) · 엑셀로 내보낼 줄 · 학년별 기준 양식(초1~고3 · 빈 칸은 안 건드림 · 0·글자는 막음) · 수납 방법 넷 · 지난달 안 받음 알약 글(옮겨 적지 않는다) <br>**지킴:** `check-fee`(23건) · `e2e/today`(적기 전 금액 없음 · 화면이 먼저 셈 · 저장 → 단가 줄 · 수납 방법 · 학년별 기준 저장·유지 · 다음 달에 미리 + 지난달 안 받음 알약 · 엑셀 200/400) · `e2e/screens`(강사는 닫힘 · 엑셀 403) |
| **검사-54** | 시험 회차 판단(`lib/exam-plan.js`, 순수 — 목업 06b · 확정-⑬·㊺b · 9/5 ⑳) — 보는 아이(학교 회차는 같은 학교 + 학년 · 전국은 고등 전부 · 「안 봄」 뺌 · 숨긴·물린 회차 ✕ — SQL `exam_takers` 와 같은 판단) · 그날·끝나는 날 · 몇 주 전부터(아이 따로 › 학교급 규칙 · 없으면 null) · 멈춤 창(영어일 − N주 ~ 시험 끝 · 영어일 없으면 안 세운다) · `stopOn` 의 「시작 전이면 진행중」 · 부모님께 글의 시험전·시험후(날 수는 규칙) · 범위 묶음(교재 × 대단원 · 더함 · 학교가 뺌 · 글) · 알약(숨긴·지난 것은 안 센다) · 머리 · 멈춤 글 · 손으로 넣은 회차의 출처 열쇠(학교·학년·이름·시작날 — (manual, null) 이 nulls not distinct 유일에 걸려 둘째 회차가 안 들어가던 사고) <br>**지킴:** `check-exam`(15건) · `e2e/today`(회차 카드 · + 범위 · 학교가 뺌 · 안 봄/본다 · 지금 멈춤 → 오늘 수업의 「교재 멈춤」 · 풀기 · 규칙 4주→6주 · 숨김/보이기) |
| **검사-55** | 성적 판단(`lib/score-plan.js`, 순수 — 목업 16 · 07·09 📈 · 9/5 ⑯) — 등급은 **회차의 등급컷으로 세어 나온다**(1등급컷부터 · 중학교는 비면 절대평가 90·80·70·60 A~E · 학교가 준 등급이 이긴다 · 다음 등급까지 몇 점) · 컷 글(빈 칸을 먼저 버린다) · 문항표 글 ↔ 줄(「1-5 듣기, 6-20 독해」) · 틀린 번호 글(눌러도 적어도 같은 값) · 영역 셈(문항표 밖은 「영역 모름」 · 많은 것부터) · 그 회차의 줄(없음·기다림·확인됨 · 공개 글) · 알약(확인 안 한 것 · 안 낸 아이) · 짧은 이름 「26-2 중간」 · 카드 한 줄 · 아이가 넣을 회차(그날 지남 · 60일 안 · 아직 없음) · 엑셀 한 줄 · 모의고사 백분위(0~100 정수 · 전국 회차에만) <br>**지킴:** `check-score`(14건) · `e2e/today`(지난달 회차 → 컷·문항표 → 아이가 넣고(07) → 16 에서 줄·틀린 문항 판·확인 → 풀기 → 고쳐 저장 → 다시 확인 → 아이·학부모 카드) |
| **검사-56** | 교재 · 단원 판단(`lib/book-plan.js`, 순수 — 목업 15 · 15b · 확정-⑤) — 같은 교재 열쇠(판·연도·기호만 없앤다, 옛 앱 lib/bookName 그대로) · 활동 차례(줄 순서에서 저절로) · 목록 줄·알약(단원 없음 · 영역 없음) · 엑셀 읽기(열 이름 후보 · 완전히 같은 것 먼저 · 교재명·대단원 이어받기 · 빈 줄 버림 · 워크북 · 문항범위 → 개수 · 엑셀이 날짜로 바꾼 것 짚기) · 교재 맞추기(교재ID › 이름 › 다른 이름 › 비슷한 이름 · 후보 둘이면 보류) · 올리면 이렇게 됩니다(새로·바뀜·같음·파일에 없는 기존 줄 · **덮어쓰기는 기존 차례를 지키고 끼운다** · 지우고 새로 · 건너뛰기 · 묶음은 교재를 따른다 — 검사-⑰) · 엑셀로 · 단원 한 줄 손질(쪽 「10-12」 · 문항 · 막는 것) · 단원 상태 둘(쓰는 중 · 숨김 — 지우지 않는다) <br>**지킴:** `check-book`(15건) · `e2e/today`(목록 · 고른 교재 · 배정 겹 · 다른 이름 · 문법 분류 잇기 · 단원 쪽·문항 고쳐 저장 · 숨김/되살리기 · 엑셀 미리보기(덮어쓰기/② 위험/보류) · 저장 → 9단원 · + 교재 · ⬇ 엑셀) |
| **검사-57** | 할 일 · 내신 자료 판단(`lib/todo-plan.js`, 순수 — 목업 04 · 05 · 확정-㉛·㉟·㊱·㊵ · 9/5 ⑨⑩㉖) — 카드 목록은 한 벌(todo_board)에서 **한 번** 세고 ⊞표·▦보드가 같은 목록을 그린다(재조회 0) · 종류가 바깥 축(칸 여덟: 자료 만들기 · 인쇄(장수) · 배부 · 단원평가 출제 · 재시험지 · 성적 받기(회차에서 세어 나온다 — 낸 아이 < 보는 아이) · 되풀이 · 메모) · 마감 줄(D-N 은 영어일까지 · 지남은 마감 기준 · 다음 수업 · 오늘 남아서) · 학교 거르개(내신 아닌 것 = 회차 없는 것) · 차례 · 숨긴 그룹(끝냄 · ♻️ 이미 있는 것 · 뺀 것) · 🔥 못 따라갑니다(남은 자료 > 남은 날 × 규칙) · 한 번에 뽑기(장 = 항목 × 아이) · 자료 단계 체크·흐름(자료 하나 안에서만 순서 · ♻️ 는 만들기가 끝난 채로) · 되풀이 글·읽기(31일은 짧은 달 말일) · 04 나무(자료(출처) · 갈래 · 항목 — 뺀 것은 안 센다) · 학생별(진도를 알아야 냅니다 · 오늘 낼 것 = 만들거나 인쇄한 것 중 안 준 것) · ♻️ 줄 · 새 자료 읽기 <br>**지킴:** `check-todo`(20건 — + 처음-8 학교 교과서) · `e2e/today`(04: + 자료 → 할 일 셋 저절로 · ♻️ 가져오기 → 만들기 끝냄 · 학교 진도 · ⬇ 엑셀 / 05: 칸 여덟 · **표 ↔ 보드 조회 0건** · 메모·되풀이·단원평가 출제 · 한 번에 뽑기 · 끝냄 → 숨긴 그룹 · 📦 흐름) · 재시험지 만들었음(paper_at → doing) · 되풀이 사건 갈래(신규 학생 days · 교재 끝나감 left — 4단계-5) |
| **검사-58** | 로드맵 판단(`lib/road-plan.js`, 순수 — 목업 08 · 확정-④·⑬·㊶·51) — 세 칸(끝냄 = 다 끝냄·건너뜀 · 하는 중 = 진도가 조금이라도 있는 대단원 · 없으면 커서 · 아직) · 소단원 줄(쌤 · 내가 · 검사 · 확인 기다리는 중 · **찍을 수 있나 = RLS 0052 ② 와 같은 판단**: 열려 있고 원장·검사가 찍은 줄이 아닐 때) · 머리 꼬리표(회독 · 소단원씩/대단원씩 · 끝낸 대단원 n/N · 이대로면 M월 초/중순/말 — 앞으로의 수업일에서) · 내 교재 상태 글(진행중 · 숙제멈춤(수업만) · 교재멈춤 — M/D에 풀림 · 기한 지나면 진행중 = stopOn 한 곳) · ❗ 줄 · 열림 띠 · 원장 쪽(N일째 · 아이별 셈 · ❗ 처분 후보 — 진도는 원장 손으로만) <br>**지킴:** `check-road`(11건) · `e2e/today`(원장 켬 → 대시보드 띠 「1일째」 → 아이가 ◐ → 「내가 · 확인 기다리는 중」 · 원장 줄엔 ○◐· 없음 · ❗ 달기 → 원장 확인·처분 → 끔) |
| **검사-59** | 학교별 표 판단(`lib/grid-plan.js`, 순수 — 목업 06c · 확정-56 · 원장님 9/3 「같은 줄」 · 9/5 ㉒) — 본 여섯(시험범위 · 진도체크 · 학사일정 · 교재(보드 축 「진행 상황」) · 특이사항 · 빈 표) · 칸 종류 여섯(글 · 날짜 · 선택 · 예/아니오 · 체크 목록 · **앱에서 고르기** 교재·회차·단원) · 칸 읽기(선택지 · 무엇을 고르나) · 값 읽기·글(종류마다 · 비면 없음 · 목록에 없으면 「목록에 없음」) · 체크 목록 손 · 줄 이름 · 셈(내린 것 뺌) · 칸으로 이동 · 차례 옮기기(10씩) · **보드 = 선택 칸 하나로 묶기**(값 없음은 숨긴 그룹 · ◀ ▶ = 그 칸 값) · 카드 제목 · 따로 챙길 아이들 · 내 표/전체 표 <br>**지킴:** `check-grid`(13건) · `e2e/today`(+ 새 표 → 칸 5 · 학교 줄 · 셀 종류마다 손 떼면 저장 · + 칸 · **표 ↔ 보드 조회 0건** · 카드 ▶ → 표의 셀이 「인쇄」 · 칸 내리기 · 따로 챙길 아이 · 표 삭제 = 내림 → 되살리기) |
| **검사-60** | 학생 판단(`lib/student-plan.js`, 순수 — 목업 14 · 답 ⑩ 하원 날짜별 · 원장님 9/3 「퇴원해도 줄은 남는다」) — 재원 기간 글 · 학년 글 · 반 글 · KPI 여섯(이 달 숙제 % · 단어 통과율 · 단원평가 n/m · 출석 n/m · N일 안 늦귀가 · 이달 경고 — 재료가 0이면 —) · 교재 막대(stopOn 한 곳) · 성적 줄(등급은 학교 것 › 컷) · 약한 영역(세 번 연속 1등) · 이 달 출결(등원·하원 시각) · 단원평가 알약(통과선) · 지나온 것(새것부터) · 목록 줄(재원·퇴원 · 찾기) · 형제 글 · 고치기 양식 · 아이디 제안·읽기 · **초기화는 앱이 발급한 계정만**(대전제-12) <br>**지킴:** `check-student`(13건) · `e2e/today`(새 아이 머리·KPI·교재·지나온 것 · 성적 공개 · 상담 적기 · 퇴원 → 줄이 남는다 → 복귀 · zz_시험_학생 늦귀가 3회 · 하원 시각) |
| **검사-61** | 신규 상담 판단(`lib/inquiry-plan.js`, 순수 — 목업 18 「전화 끊고 바로」 · 등록 전환 일곱) — 칸 다섯(답 안 한 것은 hot) · 「N시간째 답 안 함」 · 「어제 21:14」 · 방문 「M/D 요일 HH:MM」 · 레벨 같은 날 · 연락 가림 · 왜 글(단계마다) · 문의 양식 · 전환 양식 · 학교 맞추기 · 일곱 <br>**지킴:** `check-inquiry`(8건) · `e2e/today`(+ 전화 문의 → 🔥 · 📨 안내 보냄 · 방문 잡기 → 상담 잡힘 · 점수 → 레벨 봄 · 등록 전환 → 일곱 걸음(학생 · 계정 둘 · 반 · 교재 · 루틴 · 결제선생 할 일 · 첫 등원 안내) · 새 계정은 첫 로그인에 비밀번호 바꾸기) |
| **검사-62** | 자료함 판단(`lib/files-plan.js`, 순수 — 목업 20 「주고받기」 · 확정-㊸ · 원장님 9/2 「1달」 · 9/3 「그냥 둬. 지우지 마」 · 9/5 ㉒) — 갈래 여섯 · 받는 종류(9000 버킷과 같은 벌) · 크기 글 · 확장자·경로(사람 이름 없음) · 사진 줄이기 계획(긴 변 `file.photo_px`) · 한 번에 `file.batch_max`(넘으면 나눠 올리라고 — 조용히 안 자른다) · 파일 하나 검사(종류 · `file.max_mb`) · 누가 보냈나(이/가) · 저절로 꼬리표(학교·학년·학기 — SQL `term_of` 가 붙인다) · 갈래별 칸(학교·학년마다 · 🧑‍🎓 아이별 · 빈 묶음 없음) · 가장 또렷(가장 큰 사진 — 지우지 않는다) · 보낸 것 줄(붙인 자리 · 💾 저장/✓ 안 보기/아직 · N/M) · 아이 쪽 1달(`file.child_days` — 지나면 안 보일 뿐) · 숫자 셋 · 보내기 양식(아이 → 마지막 판의 숙제 줄). **받은 것은 원장님만**(RLS own_file · file_board 는 학원 사람만) · 보관함은 서버 자신만 닿고 열기는 `/api/files/id` 한 길(읽어도 되나는 v2.file 의 규칙) <br>**지킴:** `check-files`(19건 — + 답 한 줄 자동 글·고쳐 쓴 글 · 내가 보낸 것 줄 · ⑦ 고른 줄·빼기) · `e2e/today`(방금 온 것 → 갈래 → 칸 · 열기 200 · 갈래 옮기기 → 아이별 · 📤 보내기(폰이 줄여) → 보낸 것 「아직」 · 아이 📎 → 💾 저장(attachment 200) · ✓ 안 보기 → 지난 것 · 아이·학부모 📷 보내기 → 원장 방금 온 것 2 · 학부모는 형제 하나라 안 묻는다 · **9b**: 사진 미리보기 → 라이트박스(Esc) · 갈래를 고르면 답이 저절로 · 옮기면 따라 바뀜 · 고쳐 쓰면 그대로 · 아이·학부모 「내가 보낸 것」에 답) · `check-schema`(file 은 파기 목록에 없다 — 예외로 안다) · `.tmp/rls0129`(답은 원장님 것 — 아이·학부모는 RLS 로 0줄) |
| **검사-63** | 영상 판단(`lib/video-plan.js`, 순수 — 목업 19 「앱 안에서 봅니다」 · 계획 「영상」 절 · 0076) — 분:초 · 유튜브 아이디(앱 안에서 트는 것은 유튜브뿐) · 길이 읽기 · + 영상·배정 양식 · 상태(다 봄은 규칙 `video.done_pct` · N% 봄 · 길이 몰라 %가 없으면 초 · 아직) · 셈(다 봄·보다 맒·안 봄 — 내린 배정 뺌) · 마감 글 · 아이 화면 줄(안 본 것 먼저 · 지났어요 · 이어 볼 자리) · **재생기 구간 셈**(`stepSpan` — 이어지면 잇고 · 뛰면 새 구간 · 20초마다 · 멈추면 닫기). 겹침 합치기와 「다 봄」 도장은 SQL 한 곳(`video_mark` · `video_progress` — range_agg, 0128) — 화면·lib 이 다시 합치지 않는다 · 한 번에 2분 넘는 구간은 끌어다 놓은 것이라 SQL 이 던진다 · 대략치 — 아이를 판단할 숫자가 아니다(대전제-0) <br>**지킴:** `check-video`(16건 — + 본 구간 막대 조각·머리 · ⑥ 다음 영상 nextOf · ④ 「N번 열어봄」) · `e2e/today`(🎬 카드 「1개 남음」 · 나 › 영상 20% 봄 · 이어 볼 자리 · **막대 조각 둘 0%·10% · 20%·10% · 머리 30%** · ▶ 보기 → 유튜브에 못 닿으면 「유튜브에서 보기」 · 재생기에도 막대 · 원장 배정 → 마감 미루기 → 📨 재촉 「🧪 리허설(off)」 · + 영상 주소 검사 · ✕ 내리기 · 내린 영상 · **④** 재생기를 열면 「1번 열어봄」(아이 줄 · 원장 줄 · 학부모 줄) · **⑤** 학부모 🎬 카드 · **⑥** 「다음 영상 ▶」 → 안 본 다음 것 · 내린 영상은 아이 목록에서 빠짐) · `.tmp/ap0128`·`ap0129`·`ap0131`(세 번 돌려도 같다 · [0,30)+[20,60) → [0,60) · 2분 넘는 구간은 던진다 · 구간을 돌려준다 · 열 때마다 +1) |
| **검사-64** | 트랜잭션 경계 글자 검사(`scripts/check-tx.mjs` — 0-4·표-8) — lib 의 내보낸 손 하나가 표 둘 이상에 쓰면(insert·update·upsert·delete · 보관함 · auth.admin) KNOWN 에 까닭이 있어야 한다(16 손) · 목록에만 있고 없는 손도 빨강(이름이 바뀌면 목록도 바꾼다) · 주석은 먼저 지운다(폰-5) <br>**지킴:** `check-tx` |
| **검사-65** | 월간 리포트 판단(`lib/report-plan.js`, 순수 — 목업 09·10 「월간 리포트」 · 원장 확정 2026-08-28 「마감된 것만 공개」 · 0031 `monthly_report.frozen`) — 달 글 · 줄은 있는 것만(0 은 안 쓴다) · 요약 · 학부모 카드는 보낸 것만 · 보낼 줄 = 안 보냈고 마감한 날이 있는 아이. 숫자는 SQL `month_report` 가 **마감한 판만** 세어(원장·학부모가 같은 숫자) 보낼 때 굳힌다 <br>**지킴:** `check-report`(7건) · `e2e/screens`(아침 — 마감한 날 없음 · 잠김) · `e2e/today`(한마디 → 보내기 → 보냄·잠김 → 학부모 📊 카드) |
| **검사-68** | 문서 숫자(대전제-0 — 2026-09-07 개발자 지적: 인수인계의 lib 73 · 목업 「화면 25개」가 커밋을 안 따라왔다) — `docs/개발자-인수인계.md` 한 장 요약의 숫자(마이그레이션 파일 · 0100~ 개수 · 판 이름 수 · lib 판단/손 · 화면·주소 · 검사 종 · 규칙 「지킴:」·「지킴: —」 · 목업 화면 수 · 4절 머리)를 **근거 칸의 셈법 그대로** 다시 세어 대조한다 · 목업 화면 수는 넷(목업 h2 · README · check-mockup 주석 · 인수인계)이 같아야 하고 목업 뒷장 이름은 「만들 수 있나 점검」(현황은 인수인계 §5 한 곳만 말한다 — 원칙-1) · DB·걷기 숫자는 **건너뜀**(초록 아님) · 본문의 「0100~NNNN」 범위는 어디에 적혔든 마지막 번호(3절 붙여넣기 · 7절 답 · 9절 체크리스트 — 요약만 고치고 본문 다섯 곳을 안 고친 채 나간 일, 같은 날) <br>**지킴:** `check-doc-numbers`(14건 + 건너뜀 3 — 일부러 lib 73 으로 적은 사본이면 빨갛다) |
| **검사-67** | 공지 · 카드 순서 · 영상 폴더 판단(`lib/notice-plan.js` · `lib/pref-plan.js` · `lib/video-plan.js`, 순수 — 남긴 것 14·24·11 · 확정-⑮ · 목업 20 「공지에 붙이기」) — 공지 양식(제목 · 받는 쪽 셋 · 반/학교 · 막는 것) · 받는 쪽 글 · 상태 글(안 보냄·📎 / 보냄 M/D · 읽음 a/b) · 보낼 수 있나(안 보낸 것만 · 📎 도 보내기 전만) · 07·09 카드 줄·안 읽은 것 · 카드 순서(저장한 차례가 앞 · 모르는 카드는 기본 차례 · ▲▼ · 양식) · 영상 폴더 묶기(이름 차례 · 폴더 없음 맨 뒤) <br>**지킴:** `check-notice`(9건) · `e2e/today`(만들기 → 📎 → 보내기 → 07·09 📢 🆕 → 읽음 → ⇅ 카드 순서 → 14 📎·🎬 → 19 📁) |
| **검사-66** | 반 판단(`lib/class-plan.js`, 순수 — 남긴 것 23·17 · 확정-㉔) — 양식 읽기(이름 · 갈래 · 요일 0~6 · 시각 · 이 날부터 — 막는 것 여섯) · 단가 읽기 · 요일·시각 글 · 반 한 줄 · 단가 글 · 넣을 아이 후보 · 그 시각 아이 수 글(막지 않는다). 시간표는 **이 날부터** 바꾼다 — 옛 줄은 닫고(지우지 않고) 옛 회차는 옛 시간표로 <br>**지킴:** `check-class`(7건) · `check-tx`(addClass · closeClass) · `e2e/today`(만들기 → 넣기 → 단가 → 다음 달부터 요일 하나 더 → 빼기 → 닫기 · 02c 「그 시각 N명」) |

---

## 확정 — 원장님이 정하신 것 (뒤집으려면 원장님께 먼저)

| 번호 | 확정 |
|---|---|
| **확정-④** | 루틴 한 바퀴 = **대단원**. 커서는 **(회독, 대단원, 갈래)**이고 **아무 데도 저장하지 않는다** — 진도에서 세어 나온다 <br>**지킴:** `check-routine`(커서는 `v2.todo_units` 가 진도에서 세어 낸다 · 덩어리는 대단원을 안 넘는다) |
| **확정-⑤** | **표마다 주인** — 교재·단원·학습 항목은 전환 뒤 **앱이 주인**. 루틴·자료 종류·시험지 문항표는 엑셀. 나머지는 이관 <br>**지킴:** — |
| **확정-⑨** | 하루는 셋 — **① 숙제 검사가 방아쇠**, ②오늘 학습·③오늘 숙제는 **저절로 깔린다** <br>**지킴:** `e2e/today`(검사 줄이 다 검사되면 학습·숙제가 깔린다 · 검사 줄이 없으면 판이 설 때) · `check-routine` |
| **확정-⑨a** | 화면은 둘 — **②③ 은 한 카드에 좌우**(폰은 위아래). 회차 세그먼트 각각 · **교재마다 메모 둘**(학습·숙제) · 갯수·범위·뺄 항목만 조절 모달 <br>**지킴:** `e2e/today`(좌우 · 회차 각각 · 교재마다 메모 둘 · 조절 모달 02) |
| **확정-⑩** | 클래스카드를 **가른다**(문장훈련·단어훈련). **3초훈련은 판정하지 않는다** — 앱이 못 보는 것은 검증하는 척하지 않는다 <br>**지킴:** — |
| **확정-⑬** | 교재에 상태 셋 — **돌아감 · 숙제멈춤 · 교재멈춤**. 시험 회차에 묶으면 **저절로 풀린다**. 멈춤 기간은 「N주째 같은 대단원」 재촉에서 뺀다 <br>**지킴:** `check-exam`(회차에 묶은 멈춤은 영어일 − N주 부터 · 시험 끝나는 날 저절로 · 영어일이 없으면 안 세운다) · `check-routine`(셋 · 기한 지나면 진행중) · `e2e/today`(숙제멈춤 → 숙제 없음 · 진행중 → 되살아남). 시험 회차에 묶기는 교재 화면을 지을 때 |
| **확정-⑭** | 늦귀가 — 사유 · **예상 귀가 시각** · **실제 하원**(차이가 남아야 한다) · **되풀이면 앱이 먼저 부른다** · 안 보낸 채로 마감하려 하면 한 번 묻는다 <br>**지킴:** `check-late`(차이는 세어 나온다 · 되풀이 띠 · 안 보낸 채 묻기는 한 상자) · `0113 v2.late_states`(오늘을 넣어 `late.repeat_days` 안에 예상 귀가를 적은 날이 `late.repeat_count` 째면 묻는다 — 앱이 센다) · **실제 하원은 등원 걸음 4 한 줄**(0083 · `v2.left_at` — 01·09 가 같은 함수) · `e2e/today`(찍으면 「예상보다 25분 늦게」 · 3번째면 띠 + 「조절 ↗」 · 안 보낸 채 마감 → 한 번 묻고 「📨 보내고 마감」) |
| **확정-⑮** | **카드 순서를 사람마다** 저장 (끌기 + ▲▼) · 끝낸 것은 접고 개수만 · **빈 카드는 아이·학부모 화면에서만** 숨긴다(원장 화면은 그대로) <br>**지킴:** `e2e/screens`(학부모 아침 — 늦귀가·앞으로·보낸 것 없으면 카드가 없다) · 카드 순서는 ⇅ ▲▼(`check-notice` orderCards · `e2e/today` 07 — 4단계-6 · `screen_pref` 사람마다) · 끌기·접기는 아직 — |
| **확정-⑯** | 학생·학부모 **달력** — 마감 안 한 날은 「수업함·정리 중」 · 앞날은 **다음 달까지** · 지난 것은 **재원 기간만** <br>**지킴:** `check-cal`(canPrev·canNext) · `e2e/today`(학부모 달력 — 다음 달에서 ▸ 가 막힌다) |
| **확정-⑱** | 클래스카드 확장이 **목표·실제 점수를 이미 보낸다**(켠 모드만). **미달이어도 앱이 안 넘긴다** — 원장님이 누른다 <br>**지킴:** — (옛 검사 check-classcard 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **확정-⑳** | 조각이 **원본을 가리켜**, 다 덮이면 **저절로 ○** 가 된다 <br>**지킴:** `check-progress`(조각에 ○ 을 줘도 doing 까지 — 다 덮이면 ○ 는 progress_part 를 지을 때) · `e2e/today`(이번에 1-20번 조각은 마감 뒤 ◐) |
| **확정-㉒** | 루틴 = **기본루틴(전부) + 학생루틴(고르고 차례를 짠 것)**. 잠금은 **줄 사이에만**, 모든 줄이 아니다 <br>**지킴:** `check-routine`(학생루틴이 있으면 그것 전부, 없으면 영역루틴 전부 · 화면의 「학원 기본 그대로 / 이 아이만 고침 / 뺐습니다」) · `e2e/screens`(이 아이만 고치기 → 빼기 → 되살리기 → 학원 기본으로 → 이 교재만 다르게 → 🔒) · 잠금은 11 의 🔒(영역 줄·아이 줄·교재 줄 · 둘째 줄부터) → 깔 때 `day_item.gate_prev` → 07 **숙제 줄**만 「앞엣것부터」 — 앞 항목 전부를 끝내야(`check-arrival` homeSteps · `e2e/today`) · 학원 줄은 ⑰ 대로 늘 차례대로 |
| **확정-㉓** | 조절은 **교재마다 · 갯수로**. 화면에 뜨는 것은 개수가 아니라 **문항·쪽의 합계** <br>**지킴:** `check-routine`(갯수는 안 한 차례에서 · 뺀 칩 · 대단원 안 · 합계는 문항·쪽) · `e2e/today`(조절 02: 3개면 90문항·11쪽 · 칩 빼기 · 이번에 1-20번) |
| **확정-㉔** | 결석·지각 예정은 **달력에서 날짜를 고른다**. ⚠️ **보강 시각을 앱이 제안하지 않는다** — 달력을 열어 직접 고른다. 빈 자리 셈은 보여만 주고 **막지 않는다**. 결석 예정 = 보강 줄 하나(`v2.makeup`, of_date 빠지는 날 · 물리면 cancelled) · 지각 예정 = `v2.late_plan` · 그날은 판이 안 서고 숙제가 안 나간다 <br>**지킴:** `e2e/today`(일정 12: 결석 줄의 보강 잡기는 날짜·시각을 직접 적는다 — 제안 없음) · `check-plan`(검사-㊳) · `e2e/today`(02c — 매일 반이라 내일 칸이 수업일 · 결석 저장 → ✕ 와 보강 날 ↻ · 지각 20분 → ⏰ · 없음 → 물림 · 📨 알림 보냄 시각 · 오늘 결석 예정인 아이는 판 없이 「결석 예정 · 보강 안 잡힘」). 「빈 자리 셈」은 12 달력을 지을 때 |
| **확정-㉕** | 발송은 **고르고 · 한 번에 · 예약**(오늘 21:00 · 내일 9:00 · 직접) <br>**지킴:** `check-send`(예약 때 셋 — 시각은 규칙 줄 `send.evening`·`send.morning`) · `e2e/today`(고르고 · 지금 보내기 · 예약 내일 09:00 · 취소) |
| **확정-㉖** | 배색 5종을 고르게 한다 · **글씨를 `opacity` 로 흐리게 금지** — 「좋은 상태」는 **초록으로** 말한다.<br>⚠️ **2026-09-03 원장님 정정: 아이콘은 예외다.** 「그건 **글씨** 얘기고 아이콘은 괜찮아. **낮은 투명도 자체가 정보**인 거야」 — 아이콘의 투명도는 **뜻을 담는 수단**으로 쓴다(읽음 정도·지난 정도). 글씨·본문은 그대로 금지 <br>**지킴:** — (옛 검사 check-layout 는 0단계에서 지움 · 그 화면을 다시 지을 때 검사도 다시) |
| **확정-㉛** | 원장 할일에도 루틴 — 자료 종류 루틴 + **시험 루틴(D-N)**. 시험이 밀리면 날짜도 밀린다 <br>**지킴:** `check-todo`(마감 줄 · 되풀이 읽기) · `e2e/today`(04 + 자료 → 만들기·인쇄·배부 셋이 저절로 · 인쇄 없는 종류는 둘 · 되풀이 규칙 → 오늘 걸리는 것) — 세우는 곳은 DB 한 곳 sync_material_todos·sync_exam_todos(0125 · 마감 = 영어일 − 규칙 N일 · 영어일을 고치면 setEnglishOn 이 다시 맞춘다) · 되풀이는 run_repeats(열쇠 = 규칙 + 마감 날, 뼈대-2) |
| **확정-㉟** | 내신은 **파이프라인이 아니다** — 「남은 것」 목록. **마감만 있고 순서는 없다.** 자료 하나 안에서만 순서가 있다 <br>**지킴:** `check-todo`(칸은 종류마다 · 흐름 만들기→인쇄→배부→풀이→채점은 자료 하나 안에서만 · 인쇄했으면 만든 것) · `e2e/today`(05 📦 카드를 누르면 그 자료의 흐름만) |
| **확정-㊱** | **한 번 한 것은 누적된다** — 자료·성적·반 이동·회독·교재 갈아탐·수업 기록 <br>**지킴:** `check-grants`(authenticated 의 delete 0) · `check-schema`③(기록은 CASCADE 로 안 딸려 간다) · 대전제-6 |
| **확정-㊲** | 시험은 두 갈래 — **전국**(수능·학평·모의, 학교를 **안 붙인다**) vs **학교**(중간·기말). 받아올 때 **이름으로 판정**하고 그 **낱말 목록을 원장님이 고칠 수 있게** 한다. 받아온 것을 **덮지 않는다**(출처 + 출처 열쇠) <br>**지킴:** `check-neis`(전국은 낱말 목록으로만 · 전국 줄에 학교 없음 · 열쇠가 같으면 한 줄 · 학교 기간 한 줄) · `lib/neis.js`(출처+열쇠로 upsert · 영어 시험일·손으로 넣은 줄은 안 건드린다) · `e2e/today`(낱말 더하기 · 손으로 넣은 시험은 「손으로」 꼬리표) |
| **확정-㊳** | 메모로 대신한 날도 **마감이 방아쇠**가 되어 그 교재 그 회차가 저절로 ○. **그 교재만.** 되돌릴 수 있다. **3회 연속이면 앱이 부른다** <br>**지킴:** `check-progress`(학습 줄의 소단원만 · 조각은 ◐) · `e2e/today`(메모 있는 교재 — 마감 뒤 1-4 ○). 「3회 연속이면 앱이 부른다」는 대시보드 17 을 지을 때 |
| **확정-㊵** | 재활용은 고르는 것이 아니라 **「체크된 채로 서는 것」**. 개정판이면 체크하지 않는다 <br>**지킴:** `check-todo`(♻️ 는 state todo 여도 만들기 ✓ · 04 ♻️ 줄 「체크된 채로」/「이미 가져왔습니다」) · `e2e/today`(04 가져오기 → 만들기 할 일이 끝난 채로 · 개정판 체크박스면 체크 없이) |
| **확정-㊶** | **아이가 자기 교재 진도를 찍는다** — 설정으로 켜고 끈다(날짜 자동 만료 안 씀 · 대시보드에 「N일째 열려 있습니다」) · 아이가 찍은 줄은 **「확인 기다리는 중」** · **원장이 찍은 줄은 아이가 못 덮는다** · 되돌리기가 한 자리에 · **❗ 이의는 진도를 안 바꾼다** <br>**지킴:** `check-progress`(done·skip 은 검사로 안 내려간다) · `e2e/today`(02b 에서 ○ → · 되돌리기). 아이가 찍는 문(세 겹 RLS 0052)은 07 의 진도 찍기(07-2)를 지을 때 검사 — 07-1(2차-12)은 등원·오늘·다 했어요까지 · **3단계-6**: `check-road`(찍을 수 있나 = 0052 ②) · `e2e/today`(원장 켬 → 「N일째」 띠 → 아이가 ◐ → 확인 기다리는 중 → 원장 확인·되돌리기·❗ 처분(진도는 원장 손으로만) → 끔) |
| **확정-㊷** | 루틴은 **더하고 · 고치고 · 차례를 바꾸고 · 내린다**. 🗑 는 **지우는 것이 아니라 `retired`** — 되살릴 수 있다 <br>**지킴:** `check-routine`(내린 줄은 셈에서 빠진다 · ▲▼ 는 내린 줄을 건너뛴다) · `check-close`(루틴 손 아홉엔 지우는 손이 없다 — 화이트리스트) · `e2e/screens`(+ 항목 · ✎ · ▲ · 🗑 → 「내린 것 1」 → 되살리기) · 0119(`routine_areas` 도 내린 줄을 안 센다) |
| **확정-㊸** | 자료 주고받기 — **30장** · 사진은 **긴 변 1600px** · 분류는 **올린 아이에게서 저절로** · 형제가 있으면 **누구 것인지 먼저 묻는다** · 받은 것은 원장님만 <br>**지킴:** 규칙 줄 `file.batch_max`·`file.photo_px`(0128 — 코드에 숫자 없음) · `check-files`(30장 · 줄이기 계획 · 누가 보냈나) · `e2e/today`(폰이 줄여 보낸다 · 학부모 형제 하나면 안 묻는다 · `/api/files` 는 학부모의 아이만 · 원장 자료함에만 뜬다) · `0128 file_sort`(학교·학년·학기는 아이에게서) |
| **확정-㊺a** | **앱이 교재를 밀지 않는다.** 「오늘 좀 많습니다」라고 **말만** 한다 — 줄일지는 원장님이 <br>**지킴:** `e2e/today`(줄이기 그대로·필수만은 원장님이 누른다 — 앱은 개수만 말한다) |
| **확정-㊺b** | 교재 멈춤 — **고등 6주 · 중등 4주** 기본. 학생별로도, 손으로도 <br>**지킴:** `check-exam`(아이 따로 › 학교급 규칙 줄 `prep.stop_weeks.*`) · `e2e/today`(중학교 4주 → 6주 · 지금 멈춤 · 풀기) |
| **확정-㊻** | 배정 덩어리 열쇠에 **갈래(본책/워크북)** 축이 있다. **대단원 기준** = `(대,소,갈래)` — 본책 전부 → 워크북 전부. **소단원 기준** = `(대,소)` <br>**지킴:** `check-rules-db`(`v2.todo_units` 본문에 `ob = 'chapter' and is_workbook then 1 else 0` 과 `order by ch_sort` — 대단원 기준이면 본책 전부 → 워크북 전부 차례) |
| **확정-㊼** | **경고 · 반성문** (원장님 2026-09-05 「반성문 만들어」). 경고는 **하루 1회** — 지각 · 숙제 미제출 1건 · 미흡 2건부터 · 단어 미통과 — 저장하지 않고 리포트에서 센다(대전제-5). **3회면 반성문** — 생길 때 원장님이 정한다: **다음 시간 숙제** / **당일 수업 후 쓰고 가기**(늦귀가 사유) / **유예**(미룸 — 다음 경고에 다시 묻는다). 달이 바뀌면 원장님이 「정리」 — 횟수만 0, 기록은 남는다. 「세 번」은 규칙 줄에(뼈대-5). 면제는 없다 <br>**지킴:** `check-warn`(하루 1회 · 미흡은 규칙 건수부터 · 단어만 · 3회째와 배수째 · 유예는 다음 경고에 · 정리하면 그 달 1일부터 · 월초 띠) · `e2e/today`(알약 「경고 3 · 반성문」 · 처분 셋 · 사유·숙제 줄 · 전원 정리) |
| **디자인-1** | **바탕은 중립, 색은 뜻이 있을 때만 — 색 여덟(브랜드 파랑 · 성공 초록 · 주의 노랑 · 경고 주황 · 위험 빨강 · 시험 보라 · 학부모 분홍 · 출결 청록), 한 화면에 6 이하.** 색 하나 = 뜻 하나, 색만으로 뜻을 전하지 않는다. 원본 `docs/디자인-기본.md`(디자인 지침 — 목업·새 앱·배색 다섯 벌·밝은/어두운 모두) 0절 금지 8 · 1~4 <br>**지킴:** `check-palette` 다섯 벌 |
| **디자인-2** | **글씨 4.5:1 · 부품 테두리 3:1 · 회색 글씨로 위계 만들지 않음 · 아이콘엔 글.** 어두운 쪽은 순검정 금지, 채움은 200~300 단계. `docs/디자인-기본.md` 5·6·9·12 <br>**지킴:** `check-palette · check-contrast` |
| **디자인-3** | **앱의 겉은 목업과 같은 자다.** `app/globals.css` 는 손으로 고치지 않고 목업 `<style>` 에서 갈라낸다(`node scripts/mockup-css.mjs`). 치수 한 벌(칩 22 · 작은 28 · 보통 36) · 글꼴 하나 · 입력칸 = 본문 · 폰 넘침·겹침 0 — `docs/디자인-기본.md` 8절 <br>**지킴:** `check-mockup · check-sizes · check-fonts` |
| **확정-㊽** | **갈색 금지** (원장님 2026-09-05 「이 색 금지야 · 왜 싫어하는지 생각해서 다시」). 노랑·주황은 **어둡게 만들지 않는다** — 글씨 대비 때문에 누르면 겨자·카키·똥색이 되고, 탁한 색은 「밝고 명랑」의 반대다. 노랑·주황은 **채움·테두리·연한 바탕**으로만, 그 위 글씨는 남색(`--warn-fg`), 강조 글씨는 파랑(`--accent`). 따뜻한 색 토큰(색상 20°~75°)은 밝기 50% 아래 금지 <br>**지킴:** `check-palette` |
| **확정-㊾** | **전환일 알림 확인** — 옛 앱 알림 열쇠가 새 앱과 같은지 **전환일에 원장님 폰으로 시험 알림 한 번**으로 확인한다. 끊기면 그 자리에서 알림 켠 기기 3대를 다시 켠다 (원장님 9/5 「1. a」) <br>**지킴:** — (전환일 체크리스트) |
| **확정-㊿** | **늦귀가 알림 보내기는 오늘 카드(01) 한 곳**. 발송(10)에는 「01에서 보냄」 표시만 — 사유가 두 벌이 되지 않게 (원장님 9/5 「3. a」) <br>**지킴:** `check-late`(「안 보낸 채」 = sent_at 없음 — 보내는 손은 `lib/late.js sendLate` 하나: 큐 `late_notice` + sent_at 은 누른 때) · 학부모(09)는 `v2.late_for_family`(sent_at 있는 것만) · (10 발송을 지을 때 「01에서 보냄」 검사) |
| **확정-51** | **진도 나무는 표 하나 · 보기 넷** — 진도 체크(02b)·로드맵(08)·학생(14)·오늘(01)은 같은 값을 그리는 보기다. 한 곳에서 찍으면 넷에 같이 뜬다 (원장님 9/5 「4. a」) <br>**지킴:** `e2e/today`(02b 가 그리는 값 = 01 의 검사·마감이 올린 값). 로드맵 08 ✅ 3단계-6(`road_board` 가 같은 v2.progress 를 읽고 `check-road` 가 chapterSummary 한 벌 위에서 센다) · 학생 14 를 지을 때 같은 표를 그리는지 검사 |
| **확정-52** | **아이 실물 리허설 3명은 27화면이 다 선 뒤, 전환 직전에 Vercel 배포 보호를 잠깐 끄고** 한다 (원장님 9/5 「5. a」) <br>**지킴:** 사람이 한다 — 전환 체크리스트(docs/개발자-인수인계 §10) · 검사 없음 |
| **확정-53** | **오답노트 = 루틴 ②(오답 고치기)에서 △★ 표시한 것을 노트에 옮겨 쓰는 것 — 걸음 둘**(표시 → 옮겨 쓰기). 루틴 줄과 검사 자리가 둘이다 (원장님 9/5 「6. △★ 표시한 것을 노트에 옮겨 쓰는 것 맞음」) <br>**지킴:** — (걸음 둘은 원장님이 채우신 루틴 줄 둘(오답 고치기 · 노트)로 선다 — 데이터라 검사가 없다 · 아이 줄의 「N개」(count_n)가 노트 갯수, 루틴 11) |
| **확정-54** | **오답 고치기의 통과 기준은 그때그때 다르다** — 고정 %를 코드에 두지 않는다. 배정할 때 원장님이 적는 칸 하나(비면 「검사받으면 끝」) (원장님 9/5 「7. 그때그때 다름」) <br>**지킴:** 0119(`student_routine.criterion` — 아이 줄마다 「기준」 칸, 비면 「검사받으면 끝」) · `check-notify`·`check-send` 처럼 글자 검사는 없다(코드에 %가 없는 것은 grep 으로 — `lib/routine*.js` 에 숫자 % 없음) |
| **확정-55** | **클래스카드 스크램블·드릴 순서배열 결과도 받는다** — 확장이 읽는 다섯에 한 줄 더하고 시험한다. 확정-⑩(문장·단어 훈련 판정)에 이 둘이 더해진다 (원장님 9/5 「8. B」) <br>**지킴:** — (클래스카드 받기를 지을 때 검사) |
| **확정-56** | **06c 학교별 표의 칸 종류는 여섯** — 글 · 날짜 · 선택 · 예/아니오 · 체크 목록 + **「앱에서 고르기」**(교재 · 시험 회차 · 단원). 앱에 이미 있는 것은 글로 치지 않고 목록에서 고른다 — 오타가 새 항목이 되지 않고, 고른 교재의 진도·회차가 표에서 바로 보인다 (원장님 9/5 「A」) <br>**지킴:** `check-grid`(종류 여섯 · 고르기 값은 {of, id} · 글은 목록에서 이름을 찾는다) · `e2e/today`(06c 교재 칸을 목록에서 고른다 — 글로 안 친다) |
| **확정-57** | **「예습」 줄은 루틴 항목마다 「예습」 표시 칸을 두고 원장님이 켠다** — 이름에 「예습」이 들었는지로 판단하지 않는다(글자가 바뀌면 조용히 틀린다). 켜진 줄은 오늘 소단원이 아니라 **다음 소단원**을 가리킨다. 칸이 생기기 전까지 숙제 줄은 전부 오늘 학습 소단원(복습) (원장님 9/5 「b」) <br>**지킴:** 자리(place) 「예습(next)」 한 벌 — 0065 가 이미 만든 칸, 새 칸을 만들지 않았다(원칙-1 · 실데이터 「교재예습」 2줄이 거기 있다) · `check-routine`(예습 줄은 따로 서고 소단원은 오늘 덩어리 다음 것 · 숙제 자리로 깔린다 · 숙제멈춤이면 같이 빠진다) · `e2e/screens`(자리를 「예습」으로 → 알약 「예습 1」) |
| **확정-58** | **시험은 낸 날과 본 날이 다르다** — 숙제 낼 때 「다음 시간 시험」 범위를 정해 숙제와 같이 내보내고, 다음 수업에 본다(0038 · 원장님 9/2 「숙제 낼 때 다음 시간 테스트 범위를 주고, 그다음에 리포트에 출력」). 범위는 셋 중 하나 — 교재 · 내신 범위 · 직접. 원장님은 **틀린 개수만** 적고 맞은 개수·%·통과는 세어 나온다(0039 · 9/5 ③ 개수 직접 입력). **값이 없으면 리포트에 안 나간다** — 전체 개수 없는 「다음 시간」, 틀린 개수 없는 「오늘 본 것」은 줄이 안 선다 <br>**지킴:** `check-quiz` · `e2e/today` |
| **확정-59** | **통과선·방식은 학생 × 교재 × 회독마다 한 곳**(`v2.style_for`: 아이 것 → 교재 것 → 학원 기본값 90%). 오늘 고르지 않는다. 회독이 오르면 어려워지고(1회독 객뜻 50·주뜻 50 → 2회독 주관식 100) **재시험도 더 어렵게**(0040). 판정은 SQL `v2.quiz_passed` 한 곳 — 화면·lib 이 다시 세지 않는다 <br>**지킴:** `check-quiz` |
| **확정-60** | **미통과면 둘이 저절로** — ① 재시험 줄(retry_of)이 오늘 낸 것으로 선다(그날 남아서 본다 — 9/5 ⑪, 미루는 일이 아니다) ② 늦귀가 사유에 「단어 재시험이 남음 — 범위 %」가 붙는다. **「오늘은 재시험 건너뜀」은 재시험 줄만 오늘 빠진다** — 점수는 그대로, 통과선을 낮추는 것이 아니다(9/5 ②). 통과하면 둘 다 걷는다 <br>**지킴:** `e2e/today`(못 넘음 → 재시험 줄·사유 · 건너뜀 → 사유에서 빠짐·점수 그대로) |
| **확정-61** | **교재멈춤이면 그 교재 시험도 못 낸다**(0037 · 원장님 9/2 「단어 교재 멈춤 누르면 시험도 멈춘다」) — DB `quiz_guard` 가 막는다. 숙제멈춤은 시험을 안 막는다. 내신 범위·직접 범위 시험은 멈춤과 상관없다 — 시험철에도 본다 <br>**지킴:** `check-quiz` |
| **확정-62** | **조절은 교재마다 하나(02)** — 갯수·범위(긴 줄의 「이번에 낼 번호」)·뺄 칩·메모 둘을 한 자리에서. **같은 아이 같은 교재에 같은 조절이 N번째면 「루틴을 고칠까요」를 묻는다** — N 은 규칙 줄 `tune.ask_after`(9/5 ④·⑦), 답은 학생 루틴 11 에 적히고 01·02 는 보여만 준다. 긴 줄 눈금은 `chunk.split_from` 문항부터. 도는 차례(대단원·소단원 기준)는 여기서 읽기만 — 고치는 자리는 11 <br>**지킴:** `e2e/today` · `check-routine`(splitPresets) |
| **확정-63** | **반성문 기준 횟수는 학원 기본 3회(규칙 `warn.report_at`), 아이마다 따로 둘 수 있다**(`students.warn_report_at`, 비면 기본). **한 번 쓰면 그 뒤로 다시 N회를 센 뒤 또 한 번** — 3의 배수째가 아니라 「마지막으로 쓴 반성문 뒤 N회째」. 유예는 쓴 것이 아니라 다음 경고에 다시 묻는다. 달 정리(횟수 0)는 그대로 (원장님 9/5 「그 횟수를 기본3회로 하고 학생마다 다르게해야겠어. 그리고 1번쓰면 다시 n번센뒤1번」) <br>**지킴:** `check-warn`(쓴 뒤 1·2회째는 안 묻고 3회째에 · 아이 기준 2 면 2회째에 · 비우면 기본) · `e2e/today`(3b 스테퍼) |
| **확정-64** | **AI 초안을 안 고치고 마감하면 「그대로 보낼까요?」를 한 번 묻는다 — 막지는 않는다**(목업 01 ✉️ 카드의 9/5 ⑥ 표시). 초안은 `day_sheet.comment_ai` 에 남고, 원장님이 적은 글은 초안이 **덮지 않는다**(비어 있거나 지난 초안 그대로일 때만 바꾼다) <br>**지킴:** `check-comment`(sameAsDraft — 공백 차이는 같은 것) · `e2e/today`(열쇠 없을 때 글이 그대로) |

---

## 원장님 말 듣는 법 — 되짚기 (2026-09-05 · 클로드 전체 규칙)

> 원장님: 「이제부터 내가 하는 모든 말은, 효과적이고 빠르게 의도한 결과값을 낼 수 있는
> 프롬프트로 바꾸어서 알아들어. **의도가 뭔지 모르면 물어보고** 바꿔서 이해해. 클로드 전체규칙이야.」
> 앱 고치기만이 아니라 **질문·검토·문서·목업 전부**에 적용한다. **원장님이 외우실 양식은 없다** —
> 말씀은 하시던 대로, 바꿔 적는 것은 이쪽 일이다. (옛 앱 `docs/프롬프트-되짚기.md` 에 보기·말 옮김표가 있다)

| 번호 | 규칙 |
|---|---|
| **듣기-1** | **말씀을 그대로 시작하지 않는다.** 앱에 맞는 명령으로 바꿔 적고, 바꾼 것을 **짧게 되짚어 보여드린 뒤** 시작한다. 틀렸으면 원장님은 **그 줄만** 고치신다 <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **듣기-2** | **의도를 모르면 묻고, 세부가 비면 정해서 내민다.** 「무엇을 원하시나」가 안 잡히면 **한 줄로 되묻는다** — 짐작으로 시작하지 않는다(대전제-0). 「어떻게」만 비면(누구 화면 · 언제 기준 · 없을 때 · 종이) 내가 정해 「경계」에 적고, **진짜 여쭙는 것은 되돌리기 비싼 하나뿐**. 여쭐 것이 둘 이상이면 아직 안 알아본 것이다 <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **듣기-3** | 작은 것(문구·색·단추 하나)은 **한 줄로 되짚고 바로**. 판단(`lib/`) · 같은 값 두 곳 · 속도 상한 · 실제 세계 규칙(시험·학교·수강료) · 새 화면/표 · 예외 하나 더 — 하나라도 걸리면 **여섯 줄**(무엇·어디 / 지금→나중 / 판단 / 소비처 / 경계 / 확인 / 여쭐 것)로 되짚고 확인받는다. 세 번째 예외면 멈춘다(원칙-4) <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **듣기-4** | **검토·질문도 같은 꼴이다.** 「~있어?」 「~검토 못 했어」는 **끝(내놓을 것)이 목록**인 명령으로 바꾼다 — 범위 한 화면 · 갈래(규칙 위반 / 동선 막힘 / 클릭 줄일 것) · 근거(규칙 번호·동선 걸음) · 경계 · 이미 물어둔 것은 되풀이 안 함. 「효과적」의 잣대는 내가 정하지 않는다 — **대전제-3·8**이다 <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |
| **듣기-5** | **답을 받으면 그날 문서에 옮긴다**(`docs/원장님-정하실-것.md`). 세션은 기억을 못 하고 **문서만 기억한다** — 안 옮겨서 답하신 것을 또 여쭌 사고가 있었다(2026-09-03) <br>**지킴:** 사람 규칙 — 검사 없음(CLAUDE.md · 스킬 원장님-말-듣기 · 프롬프트 여덟 단계가 지킨다 · 어긋나면 docs/원장님-정하실-것 에 그날 적는다) |

## 이 문서를 쓰는 법

- **어긋난 곳을 말할 때 번호로 가리킨다** — 「`대전제-5` 위반: `v2.xxx.yyy`」
- **코드 주석에도 번호를 쓴다** — 「⚠️ `0-7` — 고르는 값은 DB 에도 건다」
- **새 규칙이 확정되면 여기 먼저 적고** 그다음 코드로 옮긴다
- **규칙을 지키는 검사가 생기면** 그 검사 이름을 규칙 옆에 적는다
