# 클로이영어 새 앱(v2) — 개발자 인수인계 (2026-09-07 실측)

> **무엇을 어디에 지었고, 검사가 무엇을 지키는지**를 적는다(프롬프트 「끝의 정의」). 규칙은 `docs/규칙.md`, 진행과 남긴 것은 `docs/지금까지.md`, 원장님 답은 `docs/원장님-정하실-것.md`, 표는 `docs/표-유도.md` — **여기서 두 벌로 적지 않는다**(원칙-1). 이 문서는 그 넷을 잇는 지도다.
> **모든 숫자는 이날 레포와 눌러보기 DB(`scripts/e2e/`)에서 센 것**이고, 안 센 것·확인 못 한 것은 「확인 안 됨」이라고 적었다(대전제-0). 실제 DB 에 **0142 까지가 9/7 밤에 들어갔다**(조각 다섯 붙여넣기 · 다섯 다 Success · 0140·0141 은 붙여넣으시니 문지기가 「이미 다 들어가 있습니다」 · 0142 붙여넣음 → 원장님 화면 실측 **43 · 0142_excel_undo.sql** · **0143((가)-①)은 붙여넣기 글을 드렸고 확인 전** — 그 전 이야기: ㉖ 답은 왔다 — 9/7 「해」 · 그런데 클라우드 세션이 5432 에 못 붙는다, 3절) — 실 DB 숫자는 여기 없다.

---

## 0. 한 장 요약

**무엇** — 인천 송도 1인 영어학원 「클로이영어」(학생 23명)의 학원관리 웹앱. 옛 앱(`main` · `chloe-english.vercel.app`)은 **동결**, 새 앱은 같은 레포의 **`v2` 브랜치** · 같은 Supabase 프로젝트의 **`v2` 스키마**. **목업이 화면 명세**(`docs/목업/클로이영어-화면-목업.html`)이고 27화면이 **전부 섰다**(2026-09-05 밤 0단계 → 2026-09-06 3단계-9). 학생·학부모에게는 **첫 배포**다(옛 앱은 원장님·강사만 썼다).

| | 수 | 어디서 셌나 |
|---|---|---|
| 마이그레이션 파일 | **133** = 0001~0143 **131**(0085 는 번호를 건너뛴 것 · 0081 은 실제 데이터 단언) + 전환일 파일 2(9000·9001) | `ls supabase/migrations` |
| 새 앱이 더한 것 | **0100~0143 · 44개**(표는 v2 그대로 쓰고 칸·함수·규칙·권한을 더했다) | 아래 3절 |
| `v2` 표 | **101** — 한 줄이 무엇인가는 전부 주석에 | `docs/표-유도.md`(`node scripts/build-doc.mjs`) |
| `v2` 함수 · 정책 | **131 · 176** (눌러보기 DB 에 돌아간 마이그레이션 122 — 0081 건너뜀) | 눌러보기 DB `pg_proc` · `pg_policies` · `v2.migration` |
| 판 한 벌(`*_board` rpc) | **21**(이름으로 · 다시 낸 것은 하나로) — 화면 하나가 조회 하나로 읽는 jsonb · 아이·학부모 쪽은 `mine_board`(📎·🎬) | `grep -oh "function v2\.[a-z_]*_board" supabase/migrations/01* \| sort -u` |
| `lib/` 모듈 | **81** — 판단(`*-plan.js` 순수 셈) 34 + 손·조회 47 | `ls lib/*.js` |
| 화면 | **31**(`page.js` — + `/send/monthly` 📊 월간 리포트 · `/schedule/classes` 🏫 반 · `/send/notice` 📢 공지) + 주소 **10**(`route.js`)(+ `/api/scores/xlsx` ⬇ 성적 양식·문항표) + manifest · 서비스워커 | `find app -name page.js` |
| 검사 | **59종** + 빌드 — 겉 5 · 판단(순수) 29(+ `check-report` · `check-class` · `check-notice`) · 구조(글자) 11(+ `check-tx` 트랜잭션 경계 · `check-doc-numbers` 문서 숫자) · DB 12(+ `check-undo` 묶음 되돌리기) · 서비스워커·시간대 2 | `bash scripts/check-all.sh`(`scripts/check-*.mjs` 를 훑는다 — 새로 만들면 저절로 든다) |
| 검사 결과 | **통과 58 · 실패 1**(`check-migrations` — 눌러보기 DB 가 0081 을 건너뛰어 그 하나를 센다 · 실 DB 는 ㉖ 뒤에 잰다) · 빌드 통과 | `DATABASE_URL=… SKIP_BUILD=1 bash scripts/check-all.sh` |
| 눌러보기(e2e) | 화면 걷기 **54** · 오늘 수업 걷기 **410** · 치수·글꼴·대비 원장·아이·학부모 셋 다 ✓ | `bash scripts/e2e/run.sh` |
| 규칙 | **197** — 「지킴:」에 검사 이름이 없는 것 **29**(`check-tables` 가 센다 — 사람 규칙·눌러보기만 있는 것 포함) · 「지킴: —」 **29**(실 DB·전환에서만 잴 수 있는 것) | `docs/규칙.md`(「지킴:」 줄) · `check-tables` |
| 목업 | 27화면 · 토큰 312 · 앱 CSS 는 목업에서 **갈라낸다**(손으로 안 고친다) | `check-mockup` |

**절대 하면 안 되는 것 여섯**

1. **`main` 에 푸시하지 마라.** `main` = 학원이 쓰는 옛 앱, 푸시가 곧 배포(대전제-2). 작업은 `v2` 와 `claude/chloe-prompt-optimization-v2` **둘 다**에 같은 커밋을 푸시한다.
2. **마이그레이션에서 `auth.` · `storage.` · `public.` 을 건드리지 마라** — 옛 앱이 평일 저녁에 반쯤 죽는다. `check-v2only` 가 잡는다. 예외는 `9000`·`9001`(전환일에 손으로).
3. **권한 기본값을 코드에 박지 마라.** 줄이 없으면 「안 정함 = 막힘」(fail closed). `check-perm` · 32칸은 원장님이 정하신 그대로.
4. **검사를 통과시키려고 앱 코드를 고치지 마라.** 테스트 전용 뒷문 금지 — 눌러보기의 흉내(인증·보관함)는 `scripts/e2e/auth.mjs` 쪽에만 있다.
5. **재원생·학부모의 진짜 비밀번호를 건드리지 마라**(대전제-12). 리허설은 `zz_시험_` 계정 다섯 — 원장·강사·조교·학생·학부모. 앱이 발급한 계정만(`profiles.issued_by_app`) 0000 초기화.
6. **`app/globals.css` 를 손으로 고치지 마라.** 목업의 `<style>` 을 고치고 `node scripts/mockup-css.mjs` 로 다시 갈라낸다(디자인-3) — `check-mockup` 이 화소를 대조한다.

**다음 할 일** — ㉖ 실 DB: **들어갔다(9/7 밤 — 「최종」 조각 다섯 붙여넣기, 다섯 다 Success · 확인은 미리보기의 새앱_들어간것 40)** → 5단계(㉜ — 정하실 것 ㉜ 의 ①부터) → 아이 실물 리허설 3명(확정-52) → 전환 체크리스트(9절 · ㉘ 은 live 로 답이 왔다) → 5단계(㉜ 「DB 완성부터」 — 후보는 `docs/원장님-정하실-것.md` ㉜). 코드 쪽 남긴 것은 `docs/지금까지.md` 「2차에 남긴 것」 1~25.

---

## 1. 읽는 순서

| | 문서 | 무엇 |
|---|---|---|
| 1 | `docs/지금까지.md` | 어디까지 · 단계마다 「한 것 · 어디 · 확인」 표 · **남긴 것 25** · 숫자 · 돌리는 법 |
| 2 | `docs/규칙.md` | 규칙 190 — 대전제 · 원칙 · 0단계 · 표 · 처음 · 뼈대 · 속도 · 폰 · 검사 · 확정(원장님이 정하신 것) · 듣기. 줄마다 **「지킴:」 = 어느 검사가 지키나** |
| 3 | `docs/원장님-정하실-것.md` | 원장님 말씀 원문과 답 · 답이 필요한 것 셋 · 「제가 정한 것」 |
| 4 | `docs/목업-구현-프롬프트.md` | 짓는 순서와 **여덟 단계**(목록 → 마이그레이션 → lib → 화면 → 검사 → 게이트 → 커밋 · 문서 → 보고) · 끝의 정의 |
| 5 | `docs/목업/클로이영어-화면-목업.html` | **화면 명세** 27장 + 「원장님께」 페이지(설명·물음·안 되는 것) — `docs/목업/README.md` 가 다루는 법 |
| 6 | `docs/표-유도.md` | 표 99 — 한 줄이 무엇인가 · 규칙 수. 만든 것이라 손으로 안 고친다 |
| 7 | `docs/디자인-기본.md` · `docs/목업보다-나아진-것.md` | 겉의 규칙(배색 다섯 벌 · 치수 · 글꼴 · 금지 9) · 목업에서 달라진 자리와 까닭 |
| 8 | `docs/서비스워커-계약서.md` · `docs/미리보기-켜기.md` · `docs/접근규칙-검사.md` | 옛 폰의 서비스워커와 맞추는 것 · 버셀에서 여는 법 · RLS 검사 |
| 9 | `CLAUDE.md` · `.claude/skills/원장님-말-듣기` | 일하는 법 — 되짚고 시작 · 여쭙는 것은 하나 · 소비처 목록이 곧 계획 |

---

## 2. 구조

### 브랜치 · 배포

| | |
|---|---|
| 레포 | GitHub `make1chloe/prep-structure` |
| `main` | **옛 앱 · 동결.** Vercel Production → `chloe-english.vercel.app` |
| `v2` | **새 앱.** 푸시하면 Vercel **Preview** 가 선다. 운영(Production)으로는 **안 지어진다** — `vercel.json` 의 `ignoreCommand` 가 `V2_PRODUCTION_OK=1` 이 없으면 건너뛴다(`check-deploy`) |
| `claude/chloe-prompt-optimization-v2` | 작업 브랜치 — `v2` 와 같은 커밋(둘 다 푸시) |
| 크론 | `vercel.json` `crons` — 매일 20:00 UTC(서울 05:00) `/api/cron`. 운영 빌드에서만 돈다 |
| 전환하는 날 | Vercel **Settings → Environments → Production → Branch Tracking** 을 `main` ▸ `v2`(9/5 실측 — Settings → Git 에는 없다) + `V2_PRODUCTION_OK=1`. 주소가 그대로라 재설치가 없다 |

### 환경변수 — 앱이 읽는 것은 **다섯뿐**(`check-phone` 처음-7)

| 이름 | 무엇 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 로그인·표(supabase-js, `lib/supabase.js` 한 곳) |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 자신 — 크론 · 알림 · 계정 발급 · 보관함(`serviceClient()`) |
| `NOTIFY_SINK` | 발송 스위치 `off`(기본 — 자취만) · `self`(학원 사람 기기만) · `live`. 없으면 off |
| `CRON_SECRET` | `/api/cron` 열쇠 |

`DATABASE_URL` 은 **앱이 안 쓴다** — DB 검사(`scripts/check-*`)만 `.env.local` 에서 읽는다. 바깥 서비스 계정(학원 이름 · 등원 회선 · 푸시 VAPID · AI · 파기 스위치 …)은 **`v2.integration` 표**(0072 가 옛 앱 설정에서 옮겼다).

### Supabase

옛 앱과 **같은 프로젝트** · 스키마만 `v2`. `Project Settings → API → Exposed schemas` 에 `v2` 를 더했다(9/3). `auth.users` · Storage · `public` 은 **전환일 파일 9000 에서만** 만진다 — 버킷 `files`(자료함)와 그 정책, `auth.users` → `v2.profiles` 트리거가 거기 있다.

### 로컬에서 여는 법 둘

1. **눌러보기 자리**(실제 Supabase 없이) — `bash scripts/e2e/fetch.sh`(PostgREST · Playwright 한 번) → `bash scripts/e2e/run.sh`. Postgres :55440 · PostgREST :55441 · 인증·보관함 흉내 :55442 · 앱 :3300. 새 DB 를 만들어 마이그레이션을 **차례대로**(0081 만 건너뜀 — 실제 데이터 단언) 돌리고 리허설 계정을 심고(`seed.sql`) 앱을 지어 띄우고 화면 걷기 → 오늘 수업 걷기 → 치수·글꼴·대비 검사 셋(원장·아이·학부모). 끝나면 앱과 DB 는 떠 있다(`bash scripts/e2e/down.sh` 로 내린다). Playwright 는 `/opt/pw-browsers/chromium-1194`.
2. **진짜 Supabase 에** — `.env.local` 에 위 다섯 + `DATABASE_URL`, `npm install`, `npm run dev`. (9/7 밤부터 실 DB 에 0142 까지 있다 — 원장님 실측 43 · 0142 · 0143 은 붙여넣기 글을 드림(7절) — 그 전엔 없어 화면이 「표·함수가 아직 없는 DB 면 0128 까지의 마이그레이션을 먼저 돌립니다」라고 말한다(㉖).

### 로그인

| 누구 | 아이디 | 비고 |
|---|---|---|
| 원장 · 강사 · 조교 | 이메일 | |
| 아이 | `chloe0000` 꼴(`chloe` + 네 자리 — DB 제약 `profiles_login_id_shape`) | 첫 비밀번호 0000 → 첫 로그인에 바꿔야 넘어간다(`must_change_pw`) |
| 학부모 | 전화번호 | 같은 번호면 형제를 잇는다 |

계정은 `auth.users`(옛 앱과 같은 곳). 새 계정은 서버 자신이 `auth.admin` 으로 만든다(`lib/student.js` — 등록 전환 · 14 계정 발급).

---

## 3. 데이터 — 새 앱이 더한 마이그레이션 0100~0143

표는 옛 v2 것(0001~0088)을 그대로 쓴다(원장님 9/5 「데이터는 버리는 거 아니야」). 아래는 **화면을 지을 때마다 하나씩** 더한 것 — **파일 하나하나는 멱등**이다(제 자리에서 다시 돌려도 같다). ⚠️ 다만 **줄 전체를 처음부터 다시 도는 것은 멱등이 아니다** — 아래 3절 (가) 3번(2026-09-07 실측). 「한 줄이 무엇인가」는 `docs/표-유도.md`.

| 번호 | 무엇 |
|---|---|
| 0100 | 뼈대 — `profiles.must_change_pw` · **`v2.rule`(임계값 — 코드에 숫자를 안 박는다, 뼈대-5)** · `v2.enqueue` · **아이디 꼴 검사를 실제에 맞춘다**(형제 -2·-3 — 0034 는 chloe+숫자 넷만 받아 실 DB 의 chloe8729-2 에서 0100 이 되돌아갔다 · 2026-09-07) |
| 0101~0113 | 오늘 수업 01 — 출결 · 판 항목 열쇠 · 학습·숙제 저절로 깔기 · 시험 카드 · 조절 · 경고·반성문 · 진도 체크 · 결석·지각 예정 · 부모님께 글 · 줄 머리·영역 메모·단원평가 · 늦귀가 |
| 0114 · 0115 · 0116 · 0117 | 대시보드 17 · 아이 07 · 서버 자신 권한 · 받을 학습지의 아이 문 |
| 0118 | 발송 10 — 자취에 「왜」 · 공개키 문 · `send_board` |
| 0119 | 루틴 11 — 통과 기준 · 예습 자리 · 내린 줄 · `routine_board` |
| 0120 | 일정 12 · 받아오기 12b — `schedule_board` · `import_board` |
| 0121 · 0122 · 0123 · 0124 | 수강료 13 `fee_board` · 시험 회차 06b `exam_board` · 성적 16 `score_board` · 교재 15 `book_board` |
| 0125 | 내신 자료 04 · 내 할 일 05 · 로드맵 08 · 진도 체크 열기 — `prep_board` · `todo_board` · `road_board` · `progress_board` · 할 일 동기화 · 되풀이 |
| 0126 | 학교별 표 06c — `grid` · `grid_col` · `grid_row` · `grid_cell` · `student_watch` · `grid_board` |
| 0127 | 학생 14 · 신규 상담 18 — `issued_by_app` · `profiles_guard` · `student_board` · `inquiry_board` |
| 0128 | 자료함 20 · 영상 19 — 규칙 다섯 · `file_sort` · `file_board` · `video_board` · `video_mark`(구간 — 겹침은 Postgres) · **접근 규칙 맞물림 풀기**(0016 의 own_file ↔ own_link) |
| 0129 | 9b(㉙ ①②③) — `file.reply`(원장님 답 한 줄 · 갈래를 고르면 저절로) · `video_progress`·`video_mark` 가 구간을 돌려준다(막대) |
| 0130 | 0004 의 fixture 반 둘을 닫는다(시간표 · 반) — 월·수/화·목에 리허설 학생이 원장님 오늘 화면에 서던 것(9/7 월요일 실측) |
| 0131 | 「다 지금해」(9/7) — 영상 연 횟수(`video_view.opens` · `video_open`) · 치환 낱말 표 `placeholder`(뼈대-8) · 학교 교과서 표 `school_book`(처음-8) · 고르는 값 NOT VALID check 11(표-6) · 판 셋이 읽은 시각을 싣는다(0-3 — `grid_board.cells_at` · `student_board`·`inquiry_board` updated_at) · `video_board`·`video_progress` 에 opens · 아이·학부모 📎·🎬 판 한 벌 `mine_board` |
| 0132 | 4단계-1(9/7 「database-url 제외한 나머지 이어서해」) — `day_item.required`(깔 때 루틴 줄의 필수 표시를 옮겨 적는다 — 줄이기 「그대로 N · 필수만 M」 을 판 안에서 센다) · 규칙 `day.heavy_pages`(📣 「오늘 좀 많습니다」 문턱 · 기본 30 · 0 이면 없음) |
| 0133 | 4단계-2a(9/7) — 알림 갈래 둘 더(`score` 성적 입력 안내 · `fee` 수강료 안내 — notify_log·scheduled_send 의 kind 제약을 열둘로 다시, NOT VALID) · 학부모가 제 아이 수납 줄을 읽는 정책 `own_payment` · `mine_board` 에 `fee`(이 달 수납 줄)·`notices`(내게 온 자취) |
| 0134 | 4단계-2b(9/7) — 📊 월간 리포트: `month_report(아이, 달)`(마감한 판만 센다 — invoker) · `monthly_board(달)`(원장 판) · `mine_board` 에 `report`(마지막으로 보낸 리포트) — 표는 0031 의 `monthly_report`(보낼 때 `frozen` 에 숫자를 굳힌다) |
| 0135 | 4단계-3a(9/7) — 🏫 반 화면: `class_board(날)`(반마다 시간표·다음 시간표·명단·반 단가·이 달 회차 · 재원생 목록) · `slot_count(날, 시각)`(그 시각 아이 수 — 확정-㉔ 보여만 준다). 표는 새로 없다 |
| 0136 | 4단계-4(9/7) — `fee_board(달)` 를 다시 내어 `prev_unpaid`(지난달에 금액을 적고 안 받은 재원생 — 수·합계·이름)를 얹는다. 이월 줄을 만들지 않는다(지난달 줄을 그대로 센다). 표·칸은 새로 없다(`units.state` · `payment.method` · `score.percentile` 은 이미 있던 칸 — 손과 화면만 더했다) |
| 0137 | 4단계-5(9/7) — `student_routine.book_id`(「이 교재만 다르게」 — 열쇠를 영역 줄·교재 줄 둘로) · `day_item.gate_prev`(🔒 줄 사이 잠금이 판까지) · `quiz.paper_at`(재시험지) · `routine_board`·`todo_board`·`routine_areas` 다시 · `run_repeats` 에 사건 갈래 둘(신규 학생 · 교재 끝나감). 표는 새로 없다 |
| 0138 | 4단계-6(9/7) — 📢 공지: 쓰기 권한·정책 · `read_notice` 를 역할·반·학교로 · `mark_notice_read`(읽음은 서버가) · `notice_board` · `mine_board` 에 `board`(내게 온 공지)·`prefs`(카드 순서) · `student_board` 에 상담 📎·`files`·`videos` · `dash_ops` 에 `pref`. 표는 새로 없다(notice·notice_read 0012 · file_link 0015 · screen_pref 0031) |
| 0139 | 4단계-3b(9/7 ㉚ 「예상수업일정보내기 기능 필요없음 무조건확정후 알림」) — 규칙 `schedule.confirm_from_day`(20) · 알림 갈래 `schedule` 「수업 일정 안내」(kind 제약 열셋 · NOT VALID) · `confirm_month(달)`(정의자 — 살아 있는 반마다 `month_confirm` step 3 한 줄 · 다시 확정하면 되살린다) · `schedule_board` 에 `confirm`(도장 · 푼 때 · 누가)·`confirm_sent`(알림 받은 아이 수) · `dash_ops` 에 `confirm_next`(다음 달 반 수·찍힌 수·풀린 수·규칙 날) — 표는 0031·0075 그대로(휴강 방아쇠가 푼다) |
| 0140 | 5단계-①(9/7 밤) — 규칙 `dash.cursor_stuck_days`(21)·`dash.memo_only_streak`(3) · `progress.last_by` 에 memo(마감 때 자동 ○) · `memo_streak(아이, 교재)` · `todo_counts` 에 `last_mark_on`·`since`·`memo_streak`(되돌림 타입 바뀜) · `dash_ops` 에 `rules`(dash.*) — 대시보드 「커서 잠김」 빵꾸 · 「메모로만 N회 연속」 부르기 · 02b ⚠️ |
| 0141 | 5단계-②(9/7 밤) — `day_item.slot` 에 stay(남아서 — 3b 「남」 줄: 검사 나머지 조각 · 더하기 · 다 함 · ⏭ 넘기기 missing) |
| 0142 | 5단계-④(9/7 밤) — 15b 묶음 되돌리기: `excel_row.op` 에 delete(② 지우고 새로만 · before = 줄 전체) · `replace_book_units(p_run)`(지운 줄·새 줄을 적는다 · 옛 세 인자 꼴은 걷음) · **`undo_excel_run(묶음)` 한 곳**(거꾸로 밟아 update → before · insert → 지움/걸리면 숨김·안 씀 · delete → 같은 id 로 되살림 · 뒤 묶음·이미 되돌림·before 비워짐이면 막힘) · `book_board.runs`(최근 8 · can_undo 는 여기서) |
| 0143 | (가)-①(9/7 밤 — 원장님 「그다음 2번해」 · 5단계 뒤 남긴 것 1) — `students.quiz_pos`(🔤 시험 카드 자리 — start 시작하자마자(기본 · 옛 줄도) · end 다 끝내고 → 01 에서 카드가 학습·숙제 아래 · check `students_quiz_pos_choice`) · `routine_board.students` 에 quiz_pos 한 칸(11 아이 머리 세그가 읽는다). 표는 새로 없다 |
| 9000 · 9001 | 전환일 — PostgREST 노출 · `auth.users` 트리거 · 버킷 `files`·정책 / D+30 옛 `public` 사람 정보 비우기(⚠️ 원장님 9/3 「지우지 마」 — 여쭙고) |

**돌리는 법** — 번호 차례대로. 눌러보기는 `scripts/_ap.mjs` 가 돌리고 `v2.migration` 에 sha 를 적는다(`check-migrations` 가 파일과 대조). 실 DB 는 ㉖ 답 뒤 같은 길로.

**실 DB 에 돌리는 법(㉖)** — 원장님 9/7 「㉖ 실 DB 마이그레이션 해」. **열쇠(`DATABASE_URL`)는 이제 있다**(Claude Code 환경변수). 그런데 **이 자리에서는 못 붙는다** — 2026-09-07 실측: 클라우드 세션의 나가는 길은 **전부 HTTP/HTTPS 프록시**라 5432·6543 이 시간초과로 끊긴다(대조군으로 아무 호스트 아무 포트도 같다 · `api.supabase.com` 은 401 로 닿는다). 망 정책은 **도메인** 목록일 뿐이라(Trusted·Full·Custom) **무엇으로 바꿔도 날 TCP 는 안 열린다** — 고칠 수 있는 자리가 아니다. 그래서 길이 둘이다.

**(가) 터미널이 DB 에 닿는 자리에서** — 개발자 PC 의 `.env.local`, 또는 Supabase 에 붙는 아무 셸. 여섯 줄:
1. **먼저 v2 를 통째로 떠 둔다** — `pg_dump "$DATABASE_URL" --schema=v2 -Fc -f v2-before-0138.dump`(되돌릴 길 — `public` 은 안 건드린다).
2. `node scripts/check-migrations.mjs` — 「안 돌린 것」 목록이 0100~0143 인지 본다(9000·9001 은 **전환일 파일 — 지금 돌리면 안 된다**).
3. `node scripts/_ap.mjs $(ls supabase/migrations | grep -E '^01[0-9][0-9]_' | tr '\n' ' ')` — 0100~0143 을 번호 차례로. ⚠️ **끊겼다고 처음부터 다시 돌리면 안 된다** — 파일 하나하나는 멱등이지만 **줄 전체를 처음부터 다시 도는 것은 멱등이 아니다**(2026-09-07 실측: `0106`·`0114`·`0128` 이 `cannot change return type of existing function` 으로 걸린다 — 뒤 파일 `0108`·`0119`·`0129`·`0131` 이 `warn_states`·`routine_areas`·`file_board`·`file_sort`·`video_board`·`video_mark` 를 다른 반환형으로 다시 냈다). 끊기면 **2번을 다시 돌려 「안 돌린 것」만** 넘긴다.
4. `DATABASE_URL=… SKIP_BUILD=1 bash scripts/check-all.sh` — DB 검사만(읽기) · `check-migrations` 가 초록인지. 실 DB 는 0081 도 이미 돌아가 있어 **하나도 안 남아야 맞다**(눌러보기 DB 에서만 0081 하나가 빨갛다 — 확인 안 됨: 실 DB 에서 아직 안 쟀다).
5. 미리보기 Vercel(`…-projects.vercel.app`)에서 원장님 계정으로 오늘 수업을 **연다** — 여는 순간 그날 판이 실제로 선다(옛 앱 `public` 은 그대로).
6. 화면이 「표·함수가 아직 없는 DB 면 …」 을 더는 말하지 않으면 끝. 이상하면 1 의 dump 로 v2 만 되돌린다(`pg_restore --schema=v2 --clean`).

**(나) 터미널 없이 — Supabase SQL Editor 에 한 벌 붙여넣기**(원장님이 혼자 하실 수 있는 길): **먼저 `scripts/real-db-preflight.sql`(읽기만)을 붙여넣어 지뢰 수를 본다** — 리허설 DB 엔 없고 실 DB 에만 있는 것(형제 아이디 `chloe8729-2` 가 0034 의 꼴 검사에 걸려 0100 이 통째로 되돌아간 일 · 2026-09-07). 0 이 아닌 칸이 있으면 그 자리를 먼저 손본다(「보강_같은결석에둘」 은 0109 가 스스로 정리한다 — 9/7 실측 15). 실 DB 에서 센 것(9/7): 아이디 꼴 밖 0·0 · 보강 상태 밖 0 · 같은 결석 보강 둘 15 · 계정 44.
`node scripts/build-real-db-sql.mjs` 가 0100~0143 를 **번호 차례로 이어 붙인 한 파일**을 `.tmp/실DB-마이그레이션-0100~0143.sql`(377KB)로 만든다(**일부만 돌린 DB** 면 그 뒤 파일만 이름으로 준다 — `node scripts/build-real-db-sql.mjs 0143_quiz_pos.sql` → `0143~0143.sql` · 골라 만든 파일도 문지기가 **앞 01xx 전부**가 들어갔는지 먼저 본다(9/7 원장님 실측: 0139 만 붙여넣었더니 `v2.rule` 이 없다고 터졌다 — 0100~ 이 없는 DB 였다 · 그 뒤 고침) · 전체를 또 돌리면 0106 에서 멈춘다 · 편집기가 큰 것을 못 삼키면 `--조각 8` 로 다섯 조각) — `v2.migration` 기록까지 같이 넣어 `check-migrations` 가 초록으로 남는다. Supabase → SQL Editor → New query → 통째로 붙여넣고 Run. **통째로 한 트랜잭션**이라 하나라도 틀리면 아무것도 안 들어가고, 그래서 **실패했으면 그대로 다시** 붙여넣으면 된다. 다 들어간 뒤에 또 돌리면 맨 앞 **문지기**가 사람 말로 멈춘다(위의 「처음부터 다시」 함정을 대신 막는다). ⚠️ 이 길은 1번(pg_dump)을 못 한다 — 되돌릴 자리는 Supabase 대시보드의 **Database → Backups**. **확인**: 돌린 뒤 SQL Editor 에 `select count(*) as 들어간것, max(file) as 마지막 from v2.migration where file like '01%';` → **44 · `0143_quiz_pos.sql`** 이면 다 들어간 것(9/7 밤 원장님 실측은 **43 · 0142** — 0143 앞까지)(0 이면 통째로 되돌아간 것).

**⚠️ 9/7 첫 시도는 통째로 되돌아갔다 — 그리고 그것이 맞게 돈 것이다.** 원장님이 붙여넣어 돌리셨는데 `새앱_들어간것 0` · `v2 표 86 · 함수 65 · 규칙 148` 이 나왔다 — 이 셋은 **돌리기 전 상태 그대로**다(아래 실측의 「준비」 값과 같다). 중간에 실패했고 한 트랜잭션이라 아무것도 안 들어간 것이다(`전환일_돌아간것 0` — 옛 앱도 그대로). **왜 실패했는지는 확인 안 됨** — 편집기의 빨간 글을 못 봤다. 그 사이 가장 잘 걸리는 셋을 묶음에 박아 두었다: ① 트랜잭션 안에서만 `set local statement_timeout = 0`(`lock_timeout`·`idle_in_transaction_session_timeout` 도) — **편집기 기본 시간제한에 걸리면 통째로 되돌아간다** ② 달러인용 표를 한글에서 **ASCII `$guard$`** 로 ③ 크기가 문제일 때를 위한 **조각 나누기** — `node scripts/build-real-db-sql.mjs --조각 8` 이면 다섯 조각(34·42·59·139·72KB)이 나온다. 조각마다 **제 트랜잭션**이고, 맨 앞 문지기가 **① 이 조각이 이미 들어갔나 ② 앞 조각이 다 들어갔나**를 본다 — 차례를 건너뛰거나 두 번 돌리는 사고를 둘 다 막는다(실측).

**2026-09-07 실측(눌러보기 DB)** — 실 DB 를 흉내 내어 **0001~0088 만 돌린 DB**(준비 상태 `v2` 표 **86** · 함수 **65** · 규칙 **148** — 원장님이 보내 주신 실 DB 숫자와 **똑같다**)를 세우고 (나)를 그대로 돌렸다: **한 파일 ✅ 39개** · **조각 1→5 ✅**(누적 8·16·24·32·39) · **차례 건너뛰기 막힘 ✅** · **두 번 돌리기 막힘 ✅**. 끝난 뒤 `v2` 표 **101** · 함수 **133** · 규칙 **178** · `public` 표 89→89 · 규칙 170→170(**한 줄도 안 건드렸다**) · `v2.migration` 에 0100~ **39줄**. 그 뒤 `check-migrations` 는 0081 하나만 빨갛다(흉내 DB 라 그렇다 — 실 DB 는 0081 도 이미 돌아가 있어 하나도 안 남아야 맞다 · 확인 안 됨). **실 DB 에는 9/7 밤 조각 다섯으로 들어갔다**(0100 형제 아이디 · 0109 보강 겹침을 고친 뒤).

**판 한 벌(17)** — 화면 하나 = rpc 하나(`security definer` · `where v2.is_staff()` 로 학원 사람만, 아이·학부모 판은 부르는 사람 자격 RLS). 세는 것은 화면이 센다(대전제-5) — 판은 재료만 준다.

---

## 4. 판단은 `lib/` 한 곳 — 모듈 81

짝으로 읽는다: **`*-plan.js` 는 순수 셈**(표를 안 읽는다 — 검사가 그대로 부른다) · **`*.js` 는 손**(판 읽기 · 쓰기 — RLS 가 문이다). 화면(`app/**/page.js`)은 표를 직접 안 읽는다(`check-fast`).

| 자리 | 판단 · 손 | 검사 |
|---|---|---|
| 세션 · 권한 · 메뉴 | `session`(guard · sessionUser — `auth.getUser()` 0) · `perm`(열쇠 22 · 32칸 · fail closed) · `roles` · `menu` · `rule`(v2.rule 읽기 — 없으면 던진다) · `supabase`(스키마 v2 한 곳) | check-session · check-perm |
| 오늘 수업 01 | `day`(판 세우기·마감 assertOpen) · `day-plan` · `homework` · `routine`·`routine-plan`(저절로 깔기) · `quiz`·`quiz-plan` · `plan`·`plan-plan`(조절) · `warn`·`warn-plan` · `progress`·`progress-plan` · `attend` · `late`·`late-plan` · `comment`·`comment-plan` · `area-memo` · `unit-test` · `roster-plan` · `status` | check-close · check-attend · check-routine · check-quiz · check-plan · check-warn · check-progress · check-late · check-comment · check-material |
| 등원·하원 · 아이 · 학부모 | `arrival`·`arrival-plan`(학원 회선 · 걸음 넷) · `me` · `parent`·`parent-plan` · `cal`·`cal-plan` · `material`·`material-plan` · `request` · `road`·`road-plan`(08) · `score`·`score-plan`(16) | check-arrival · check-parent · check-cal · check-road · check-score |
| 대시보드 · 발송 · 알림 | `dash`·`dash-plan` · `send`·`send-plan`(손 다섯 · 큐 갈래) · `notify`·`notify-plan`(**밖으로 나가는 길 한 곳** · 스위치) · `push`·`push-plan` · `queue`(크론) | check-dash · check-send · check-notify · check-queue · check-sw |
| 달·학기 | `schedule`·`schedule-plan`(12) · `neis`·`neis-plan`(12b) · `fee`·`fee-plan`(13) · `exam`·`exam-plan`(06b) · `book`·`book-plan`(15·15b) · `todo`·`todo-plan`(04·05) · `grid`·`grid-plan`(06c) · `student`·`student-plan`(14) · `inquiry`·`inquiry-plan`(18) · `files`·`files-plan`(20) · `video`·`video-plan`(19) | check-schedule · check-neis · check-fee · check-exam · check-book · check-todo · check-grid · check-student · check-inquiry · check-files · check-video |

---

## 5. 화면 = 목업 — 28

| 목업 | 주소 | 파일 | 판 | 지킴 |
|---|---|---|---|---|
| 00 로그인 · 처음 비밀번호 | `/login` · `/password` | `app/login` · `app/password` | — | e2e/screens |
| 17 대시보드 | `/` | `app/page.js` | 0114 셋 | check-dash · e2e |
| 01 오늘 수업(+02 조절 · 02b 진도 체크 · 02c 예정 · 03 폰) | `/today` | `app/today/{page,board,row}.js` + 모달 | roster · day | e2e/today(283건의 절반) · check-close |
| 10 발송 | `/send` | `app/send` | `send_board` | check-send · e2e |
| 12 일정 · 12b 받아오기 · 06b 시험 회차 · 04 내신 자료 · 05 내 할 일 · 06c 학교별 표 | `/schedule` · `/schedule/import` · `/schedule/exams` · `/schedule/exams/prep` · `/schedule/todo` · `/schedule/grid` | `app/schedule/**` | `schedule_board` · `import_board` · `exam_board` · `prep_board` · `todo_board` · `grid_board` | check-schedule · check-neis · check-exam · check-todo · check-grid · e2e |
| 15 교재 · 15b 엑셀 · 19 영상 배정 | `/books` · `/books/videos` | `app/books/**` | `book_board` · `video_board` | check-book · check-video · e2e |
| 13 수강료 · 14 학생 · 18 신규 상담 · 20 자료함 | `/ops` · `/ops/students` · `/ops/inquiry` · `/ops/files` | `app/ops/**` | `fee_board` · `student_board` · `inquiry_board` · `file_board` | check-fee · check-student · check-inquiry · check-files · e2e |
| 16 성적 | `/scores` | `app/scores` | `score_board` | check-score · e2e |
| 설정 · 누가 무엇을 보나 · 11 루틴 · 02b 진도 체크 열기 | `/settings` · `/settings/access` · `/settings/routine` · `/settings/progress` | `app/settings/**` | `routine_board` · `progress_board` | check-perm · check-routine · e2e |
| 07 아이 「나」(+📎 · 🎬 카드) · 09b 달력 · 08 로드맵 · 19 영상(앱 안 재생기) | `/me` · `/me/cal` · `/me/book` · `/me/videos` | `app/me/**` | 0115 · `road_board` · `video_progress` | check-arrival · check-road · check-video · e2e |
| 09 학부모(+📎 카드) · 09b 달력 | `/parent` · `/parent/cal` | `app/parent/**` | RLS(마감한 판만 — 0084) | check-parent · e2e |

**주소 8** — `/api/cron`(크론) · `/api/push/seen`(옛 서비스워커 회신) · `/api/icon/[size]` · `/api/ops/fee` · `/api/books/xlsx` · `/api/prep/xlsx`(엑셀 셋) · `/api/files`(올리기 — 파일 하나씩) · `/api/files/[id]`(열기 — 읽어도 되나는 `v2.file` 규칙).

**아이·학부모 화면은 목업보다 고도화해서 짓는다**(원장님 9/5) — 07 은 답 ㉗ 대로, 19·20 아이 쪽은 ㉙ 로 목록을 여쭈었다. 전·후는 `docs/목업보다-나아진-것.md`.

---

## 6. 검사 — 무엇을 지키나

**돌리는 법** — `SKIP_BUILD=1 bash scripts/check-all.sh`(DB 열쇠가 없으면 DB 검사를 **건너뜀으로 센다**, 초록이 아니다) · 빌드는 `npx next build --webpack` · 눌러보기는 `bash scripts/e2e/run.sh`(약 12분). **게이트** = check-all + 빌드 + e2e 전부 초록이어야 커밋한다(프롬프트 여덟 단계 6).

| 갈래 | 검사 | 지키는 것 |
|---|---|---|
| 겉 5(팔레트는 글자, 넷은 브라우저) | palette · mockup · sizes · fonts · contrast | 토큰 312 · 27화면 화소 대조 · 높이 한 벌 · 넘침·겹침·세로 글자 0 · 글꼴 1 · 입력칸 = 본문 · 대비 3.5 · 배색 5 × 밝음·어두움 |
| DB 11 | grants · migrations · schema · sql · sql-funcs · tables · residue · v2-rls · rules-db · quiz · warn | 권한 3상태 · 파일↔DB 대조 · 파기 목록·CASCADE·RLS·force · lib 의 SQL 을 진짜 스키마에 PREPARE(죽은 칸) · 함수가 없어진 칸을 안 읽나 · 표 주석 · 옛 앱 잔재 · 아이가 남의 것 못 보나 · 표·뼈대 규칙(래칫) |
| 판단 순수 26 | arrival attend book cal comment dash exam fee files grid inquiry late material neis parent plan progress road roster routine schedule score send student todo video | `*-plan.js` 를 씨앗으로 부른다 — 경계값(빈 것 · 0개 · 중복 · 같은 아이 여럿) |
| 구조 글자 9 | close · fast · notify · perm · phone · queue · session · deploy · v2only | 판에 쓰는 손이 마감을 본다 · 화면은 표를 안 읽고 층 상한 안 · 나가는 길 한 곳 · 열쇠 32칸 · 폰·속도 규칙 · 큐 뼈대 · 세션 한 벌 · 운영 빌드 막기 · v2 밖 금지 |
| 그 밖 | sw · tz | 옛 서비스워커 계약 14건 · 시간대 서울 한 벌 |
| e2e | `scripts/e2e/screens.mjs`(46) · `today.mjs`(283) | 원장 하루 동선(판 → 검사 → 깔기 → 시험 → 늦귀가 → 글 → 마감 → 아이 → 학부모 → 발송 → 달·학기 화면 전부) · 조회 수 상한(오늘 ≤ 20 · 표 ↔ 보드 0) |

**빨간 것 하나** — `check-migrations`: 눌러보기 DB 는 0081(실제 데이터 단언)을 건너뛰어 「안 돌린 것 1」을 센다. 정직하게 빨갛다. 실 DB 에서는 ㉖ 뒤에 0100~0143 를 돌리면 초록.

**검사 문화** — 「이거 왜 이래?」 하나를 고치면 그것을 다시 잡을 검사를 하나 더한다(CLAUDE.md). 글자로 훑는 검사는 주석을 먼저 지운다(폰-5 · `check-phone` 이 검사들을 검사한다). 래칫 검사(`check-rules-db`)는 지금 어긋난 것을 이름으로 적어 두고 **새로 생기는 것만** 빨갛다.

---

## 7. 원장님이 정한 것 · 답이 필요한 것

`docs/원장님-정하실-것.md` 가 원문이다(그날 옮긴다 — 듣기-5). 규칙으로 굳은 것은 `docs/규칙.md` 「확정」 46줄(번호는 ①~59 — 합친 번호가 있다).

**답은 다 왔다(9/7)** — ㉖ ① 굳힌 묶음을 붙여넣음 → **0142 까지 확인**(9/7 밤 원장님 실측 43 · 0142 · 0143 은 붙여넣기 글) · ㉘ live(첫 배포 날 `NOTIFY_SINK=live`) · ㉚ 안내·확인 없이 **확정 후 알림**(3b 지음 · 0139) · ㉛ 발행본은 버린다(레포 `docs/` 만) · ㉜ 5단계 전에 DB 완성부터.

---

## 8. 안 되는 것 · 남긴 것 · 사고

**남긴 것 25** — `docs/지금까지.md` 「2차에 남긴 것」(화면마다 남은 것 · 규칙 중 지킴 없는 12). 큰 것만: 실제 발송 · 공지 화면(공지에 붙이기) · 반 화면 · 첫 등원 안내 **문자** · 클래스카드 받기 · 카드 순서·접기(`screen_pref`) · 표-6 의 26칸 check(0129) · 0-3 「읽은 줄 대조」.

**확인 안 된 것** — VAPID 열쇠가 옛 앱과 같은가 · Vercel Preview 에서 환경변수 · 크론 요금제 한도 · 학교 홈페이지 확장(나이스에 없는 학교) · 재생기 실물(유튜브 IFrame · iOS) · HEIC 사진.

**이 프로젝트가 겪은 사고 — 다시 밟지 말 것**

| 무엇 | 어떻게 |
|---|---|
| 운영 주소가 v2 를 보였다(9/5) | 옛 v2 빌드가 Production 에 떠 학원 앱이 죽었다 → `vercel.json` ignoreCommand(대전제-13 · `check-deploy`) |
| 스키마를 하나 더(v3) 세웠다가 걷었다(9/5) | v2 에 같은 표가 있어 두 벌(원칙-1) |
| 접근 규칙이 서로를 불렀다(9/6) | 0016 `own_file` ↔ `own_link` — 아이가 붙임을 읽는 순간 「infinite recursion」. 정의자 함수로 풀었다(0128) — 옛 앱은 이 길을 연 적이 없어 몰랐다 |
| 부품을 부품 안에 정의했다(9/6) | 06c 의 셀이 글자마다 다시 마운트돼 초점을 잃었다 → 밖으로 올렸다 |
| 글자 검사가 헛통과했다 | `\b` 가 한글 앞에서 안 맞고 주석의 낱말을 읽었다 → strip 먼저(폰-5) |
| 검사가 요일을 탔다(9/7 월) | 0004 fixture 반이 월·수/화·목에 만나 걷기가 엉뚱한 줄을 눌렀다(첫 열기 23 · check-warn 빨강). 주말에만 돌던 검사가 못 봤다 → 0130 · 줄은 id 로. **게이트는 요일을 바꿔 가며도 돌려 본다** |
| 시간대가 아침에 어긋났다(9/7) | timestamptz 를 `String(ts).slice(0,10)` 으로 잘라 UTC 날짜가 됐다 — KST 00~09시에 하루 전. `seoulDate` 한 벌로(check-tz 가 글자로 못 잡는 꼴 — 게이트를 아침에도 돌려 본다) |
| 그려진 적 없는 가지가 터졌다(9/7) | 회차가 모자란 반 카드가 처음 그려지며 `/schedule` 500 — `Panel.ClassMakeup`(클라이언트 부품의 정적 속성)은 서버 부품에서 undefined. 씨앗에 그 상태(회차 모자란 반)를 두어 늘 걷는다. **화면의 가지마다 씨앗이 그 상태를 하나는 가진다** |
| 눌러보기 스크립트가 제 자신을 죽였다(9/6) | `pkill -f "next-server"` 가 그 문자열이 든 제 껍질을 잡았다 → 파일 안의 패턴만 · 껍질에서는 `[r]` 꼴 |
| 옛 앱 사고 여덟(0047 · 0060 · 0069 · 0072 · 0077 · 0084 · 아이가 도착 시각을 정할 수 있었던 것 · 파기 백업) | 옛 인수인계(git 역사 8eee97a 이전)와 `docs/규칙.md` 검사 줄에 남아 있다 |

---

## 9. 첫 배포(전환) 체크리스트

⚠️ **「전환」이 아니라 「첫 배포」다** — 학부모는 한 명도 옛 앱을 연 적이 없다(9/2 실측 · 학부모 20 · 로그인 0). 진짜 일은 학부모 20명이 앱을 깔고 알림을 켜게 하는 **운영**이다(아이 편에 안내지 · 수업 온 아이 폰에서 그 자리에서).

**차례 — 수업 시간 밖에, 되돌리는 줄과 짝으로(`9000_switch_day.sql`)**

| 켜는 것 | 되돌리는 것 |
|---|---|
| ✅ 9/7 밤 — ㉖ 답 → 실 DB 에 **0142 까지**(3절 (나) 붙여넣기 다섯 조각 + 0142 · 원장님 실측 43 · 0142) · 0143 은 붙여넣기 글을 드림 — 붙여넣으시면 44 · 0143 | 새 표·함수는 옛 앱이 안 읽는다 — 그대로 둬도 된다 |
| PostgREST 노출 스키마에 `v2`(이미 9/3) | `v2` 빼기 — **`public` 은 빼지 마라** |
| `auth.users` → `v2.profiles` 트리거(9000 ②) | `drop trigger v2_on_auth_user on auth.users` |
| 버킷 `files` · 정책 셋(9000 ③) — 자료함이 여기부터 돈다 | 정책 drop · 버킷은 파일이 있으면 안 지운다 |
| `NOTIFY_SINK` — `live`(㉘ 답 9/7 「2」 — self 시험 없이 바로) | `off` |
| Vercel `V2_PRODUCTION_OK=1` · Production Branch `main` ▸ `v2` | 변수 지우기 · 브랜치 되돌리기 + 재배포 |
| 옛 앱에 역할 잠금(원장님만) | 잠금 풀기 |

**켠 직후 확인** — ① 원장님 폰의 설치된 아이콘으로 새 화면이 뜨나 · `zz_시험_` 에 알림을 쏴 눌렀을 때 맞는 화면이 열리나(옛 서비스워커가 아직 지배하면 여기서 드러난다 · `check-sw` · `docs/서비스워커-계약서.md`) ② 학생 한 명을 18 에서 실제로 등록 전환 → 계정 발급 → 첫 로그인 → 자기 화면(아이 실물 리허설 3명, 확정-52 — 배포 보호를 잠깐 끈다) ③ 진도 체크 열기를 첫 주에(확정-㊶) ④ 첫 주 매일 밤 옛 앱과 대조.

**D+30** — `9001_purge_public.sql` 은 되돌릴 수 없다. 원장님 9/3 「그냥 둬. 지우지 마」 — **밟기 전에 다시 여쭙는다.**

---

## 10. 일하는 법(짧게)

원장님 말씀은 **되짚고 시작**(한 줄 · 큰 것은 여섯 줄) · 여쭙는 것은 **되돌리기 비싼 하나** · 답은 **그날** 문서에. 고치기 전에 **소비처 목록**(grep)이 곧 계획. 판단은 `lib/` 한 곳, 화면은 가져다 그린다. 「꼼꼼히」는 파수꾼(검사)을 먼저 돌리고 **실패한 것만** 보고. 세션은 잊고 문서만 기억한다 — 끝날 때 `docs/지금까지.md` 를 갱신한다.
