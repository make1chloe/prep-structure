# 실 DB 에 붙여넣을 SQL — 원장님용

Supabase → **SQL Editor** → **New query** → 파일을 통째로 붙여넣고 **Run**.

> **원장님께 드릴 때는 늘 바로 보이는 링크로**(원장님 2026-09-15 「sql은 항상 바로보이는 링크로줘」).
> 꼴은 하나: `https://raw.githubusercontent.com/make1chloe/prep-structure/v2/docs/sql-paste/NNNN.sql`
> (v2 가지에 올라간 파일 그대로 · 열면 글만 보인다 · 전체 선택해서 복사). 파일 이름만 적고 끝내지 않는다.

## 지금 하실 것

**`0164.sql` 다시 하나** (2026-09-15 · 고쳐서 다시 냄) · 링크 https://raw.githubusercontent.com/make1chloe/prep-structure/v2/docs/sql-paste/0164.sql
처음 것은 실 DB 에서 되돌아갔습니다(`todo_kind_choice` 23514): 옛 앱에서 받아온 할 일(「광복절 대체공휴일 · 정상 수업」 같은 것 · 갈래 schedule)이
0131 의 갈래 제약에 안 맞아 한 줄도 못 고치는 상태였습니다. 고친 0164 는 그런 옛 갈래를 📋 메모로 옮기고 제약을 검증한 뒤 글을 고칩니다. 통째로 되돌아갔으니 그대로 다시 붙여넣으면 됩니다.

그 다음 **`0-안-맞는-옛-줄.sql`** (읽기만) · 링크 https://raw.githubusercontent.com/make1chloe/prep-structure/v2/docs/sql-paste/0-안-맞는-옛-줄.sql
→ 나온 표(12줄)를 그대로 주세요. 할 일 말고 다른 표에도 그런 옛 줄이 있는지 셉니다(0 이 아니면 그 표도 같은 길로 고칩니다). · 화면 글에서 「—」를 걷은 것(대전제-18)의 SQL 쪽: 할 일 05 의 까닭 글 · 자료함 답 한 줄 · 오류 글을 짓는 함수 여덟을 같은 말로 다시 내고, 이미 적힌 줄도 고칩니다.
안 돌려도 앱은 돌아갑니다(SQL 이 짓는 글에만 「—」가 남습니다).

그 앞까지는 다 들어가 있습니다 — 0100~0162 **63개**. 0162 는 2026-09-14 원장님이 붙여넣고 「ㅇㅇ완료」 하셨습니다
(제가 실 DB 를 직접 재지는 못했습니다 — 다음에 `0-어디까지-들어갔나.sql` 을 돌리시면 「안 들어간 개수 0 · 실 DB 01xx 63」이어야 합니다).
그 앞 0100~0161 62개는 2026-09-11 원장님이 그 파일로 재신 값입니다(62 · 0161 들어감 1 · 새 함수 1 ·
「시험 몇 주 전 멈춤인가」 **false** — 시험을 낼 수 있는 상태).

## 끝난 것 (2026-09-14)

- (`0164.sql` 은 위 「지금 하실 것」)
- `0163.sql` · 오늘 수업 01 의 「📄 내신 자료」 업무가 읽는 함수 `today_prep`((어21) 3단) · 들어감 9/15
- `0162.sql` — 할 일 05 카드의 까닭 글 「시험 회차 …에서 저절로」 → 「시험 …에서 저절로」((어18) 「회차」는 수업 회차만 · 함수 한 벌 + 이미 선 줄)

## 끝난 것 (2026-09-11)

- `0161.sql` — 시험 회차를 넣는 순간부터 시험을 못 내던 것(「멈췄나」를 앱·DB 가 다르게 판단)

- `0160.sql` — 표-9 아이가 쓰는 자리 DB 잠금(`score`·`file_link`)

실 DB 는 **0147 까지**(01xx 48개) 들어가 있고, **0148~0159 열둘이 통째로 비어** 있었습니다.
(제가 「0155 까지 다 들어갔다」고 적어 둔 것이 틀렸습니다 — 원장님이 「성공함」 하신 것을 확인 없이 받아 적었습니다.)

| 파일 | 무엇 | 확인 |
|---|---|---|
| **`0148-0159.sql`** | 0148 학교별 표 공개 · 0149 단원평가 · 0150 반성문 · 0151 예약 발송 · 0152 시험 날짜 바뀜 · 0153 대시보드 반 · 0154 문자 · 0155 로드맵 조각 · 0156 재원생 달 넘기기 · 0157 문자 갈래 · 0158 🃏 건너뜀 · 0159 🏫 홈페이지 | **눌러보기 DB 를 0147 까지로 되돌려 놓고 실제로 돌려 봤습니다 — COMMIT · 48 → 60** (2026-09-11) |

`0-어디까지-들어갔나.sql` — **읽기만** 합니다(아무것도 안 바꾸고 안 지웁니다). 한 줄에 아홉 칸이 나옵니다 —
**안 들어간 개수 · 안 들어간 번호 · 실 DB 01xx · 재원생 · 도는 반 · 교재 · 정한 권한칸 · 연동 열쇠 갈래 · 앱이 낸 계정.**
다 들어갔으면 「안 들어간 개수 **0**」입니다. 열쇠 값은 한 글자도 안 꺼내고 **갈래 수만** 셉니다.
이 파일은 손으로 안 고칩니다 — `node scripts/build-look-sql.mjs` 가 마이그레이션을 훑어 짓고,
`check-doc-numbers` 가 낡으면 빨개집니다(0159 에서 멈춘 채 「60개 중 48개」라고 말하고 있었습니다 · 2026-09-11).

## 늘 같은 것

- **통째로 한 트랜잭션입니다** — 하나라도 틀리면 아무것도 안 들어갑니다. 실패해도 DB 는 그대로고, 고쳐서 **그대로 다시** 붙여넣으면 됩니다.
- 맨 끝에 `notify pgrst, 'reload schema';` 가 들어 있습니다(표에 칸을 더해도 화면이 그 칸을 못 쓰던 일 — 원장님 9/10 밤).
- **다 들어간 뒤에 또 돌리면 안 됩니다** — 문지기가 「이미 다 들어가 있습니다」라며 먼저 막습니다.
- ⚠️ **SQL Editor 는 맨 마지막 `select` 하나만 보여 줍니다** — 그래서 읽기용 파일은 한 문장으로 만들었습니다.

이 파일은 `supabase/migrations/` 의 원본을 번호 차례대로 이어 붙인 것이고,
`node scripts/build-real-db-sql.mjs <파일이름들>` 로 다시 만듭니다.
