# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# 눌러보기 검사가 쓰는 열쇠 — 검사용이지만 저장소에 둘 이유가 없다
# (없으면 scripts/e2e/up.sh 가 새로 만든다)
/scripts/e2e/jwt-secret.txt

# 검사하며 찍은 화면 — 저장소에 두지 않는다
/*.png

# 원장님께 만들어 드린 엑셀 — 저장소에 두지 않는다
/클로이영어_*.xlsx

# 빌드 산출물
/.next/
