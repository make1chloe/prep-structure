-- ─────────────────────────────────────────────────────────────
-- 0035 · 단어 영역 루틴 (원장님 2026-09-02)
--
--   1. 교재예습
--   2. 클래스카드 필수학습 완료
--
-- **두 줄이 끝이다.** 다른 영역이 5~11줄인데 단어는 2줄이다.
-- ⚠️ 「어디」(학원·숙제·둘 다)는 원장님이 안 말씀하셨다. **숙제**로 두고 —
--    단어는 집에서 외워 오는 것이고, 클래스카드는 플래너로 집에서 돈다 —
--    화면에서 바로 고치실 수 있게 했다(▲▼ ✎ 🗑). **틀렸으면 한 번 누르면 된다.**
-- ⚠️ 「클래스카드 필수학습 완료」는 **확장이 이미 확인해 준다** — 목표·실제 점수가 온다.
--    아이가 체크하는 것이 아니라 **앱이 셈해서 안다.**
-- ─────────────────────────────────────────────────────────────
insert into v2.learn_items(name, method, sort, import_batch) values
  ('교재예습', '다음에 나갈 범위를 미리 보고 온다', 10, 'excel'),
  ('클래스카드 필수학습 완료', '필수로 켠 모드를 목표 점수까지 — 확장이 받아 적는다', 20, 'excel')
on conflict (name) do nothing;

insert into v2.area_routine(area, item_id, place, required, sort, import_batch)
select '단어', i.id, 'home', true, s.n, 'excel'
from (values ('교재예습',1),('클래스카드 필수학습 완료',2)) s(nm,n)
join v2.learn_items i on i.name = s.nm
on conflict (area, item_id) do nothing;

comment on table v2.area_routine is
  '영역 루틴. ⚠️ 「단어」는 **2줄**이다 — 다른 영역(5~11줄)과 크게 다르므로
   하루 총량을 셀 때 단어책을 든 아이는 항목이 훨씬 적다';
