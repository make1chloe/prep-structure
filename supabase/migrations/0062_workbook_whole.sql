-- 원장님 확정 (2026-09-02): **워크북은 대단원 전체를 한 번에 낸다.**
--
-- ⚠️ 이건 이름 문제가 아니라 **배정 규칙**이다. 배정 엔진(routineNext)이 워크북 줄들을
--    소단원마다 하나씩 세면 **한 대단원 워크북이 열 번에 나눠 나간다** — 원장님이 하시는 방식과 다르다.
--    쓰작2 는 대단원당 워크북이 10줄이라, 안 지키면 열 배로 잘게 나간다.
--
-- ⚠️ 원장님은 「워크북은 단원 구분명이 없어서 그냥 워크북만 써놓은듯」이라 하셨는데,
--    **옛 앱 원본에는 이름이 있었다** (「워크북 01 현재진행형」 — 11권 전부, 실측).
--    그래서 0054·0055·0061 이 채운 이름은 **되돌리지 않았다.** 원본과 맞다.
--    이름이 있는 것과 「통째로 낸다」는 따로 간다 — 화면엔 이름이 보이고, 배정은 통째로 나간다.
insert into v2.hold_decision(why_like, decided) values
 ('%워크북 배정%',
  '워크북은 **대단원 전체를 한 번에** 낸다 (원장님 2026-09-02). 배정 엔진이 워크북 줄을 '
  '한 덩어리로 묶는다 — 소단원마다 쪼개지 않는다. ⚠️ 소단원 기준 교재에서도 그런지는 확인 안 됨')
on conflict (why_like) do update set decided = excluded.decided;

comment on column v2.units.is_workbook is
  '워크북 줄인가. ⚠️ **워크북은 대단원 전체를 한 번에 낸다**(원장님 2026-09-02) — '
  '배정이 이 줄들을 한 덩어리로 묶어야 한다. 소단원마다 쪼개면 열 배로 잘게 나간다';
