/**
 * 학부모 화면에 그대로 나가는 **글 한 벌.** 화면·서버 동작·검사가 같은 글을 본다 (원칙 1).
 *
 * ⚠️ 여기 글을 고치면 학부모 20명이 처음 보는 첫 인상이 바뀐다. 원장님 확인 없이 고치지 마라.
 */

/**
 * ⚠️ **원장님이 써 주신 두 줄이다. 글자 하나 바꾸지 마라.**
 *    「남기실 말」 자리에 그대로 둔다.
 */
export const LEAVE_NOTE = Object.freeze([
  "결석 예정을 미리 알려주시면 수업을 준비하는 데에 큰 도움이 됩니다.",
  "병원진료가 아닌 당일결석은 보강이 불가합니다.",
]);

/**
 * 처음 여는 분께 드리는 안내.
 *
 * ⚠️⚠️ **실측 — 학부모 20명이 한 번도 로그인한 적이 없다.** 이 화면은 늘 「처음 여는 사람」의 화면이다.
 * ⚠️⚠️ 둘째 줄을 빼지 마라. **앞으로 할 것도 같이 보인다는 것을 안 밝히면**
 *    다음 주 숙제가 「아직」으로 보여 **「이걸 아직도 안 했나」로 오해**한다.
 */
export const FIRST_TIME = Object.freeze([
  "여기서 보시는 것은 출결 · 과제 · 그날 수업 내용 · 월간 리포트입니다.",
  "⚠️ 지난 것뿐 아니라 **앞으로 할 것도 같이 보입니다.** 다음 시간에 할 숙제가 「아직」으로 뜨는 것은 안 한 것이 아니라 아직 할 때가 안 된 것입니다.",
  "그날 수업 내용은 원장님이 그날 정리를 마쳐야 보입니다. 그전에는 「아직 정리 중이에요」로 뜹니다.",
  "결석·지각을 미리 알려주시려면 아래 달력에서 그 날짜를 눌러 주세요.",
]);

/** 결석·지각 예정을 적을 때 화면에 같이 세우는 글 */
export const PLAN_NOTE = LEAVE_NOTE[1];

/**
 * 결석·지각 예정 한 줄의 **머리말.**
 *
 * ⚠️ **확인 안 됨 — `v2.request` 에는 날짜 칸이 없다**(칸 9개를 다 봤다, 2026-09-02).
 *    (「지각 몇 분」은 이제 **안 받는다** — 원장님 2026-09-02. 그래서 그 칸은 필요 없어졌다.)
 *    그래서 값을 **글로** 남긴다. 원장 화면이 이 글자를 **기계로 읽어야 하는 날**이 오면
 *    그때는 이 상수를 `lib/` 로 올려라 — 두 화면이 각자 글자를 정하면 그날부터 어긋난다.
 *    칸을 만드는 쪽이 낫다 → 보고의 needsDb 에 적었다.
 * ⚠️ `v2.request.kind` 에는 「지각」 갈래가 없다(question·makeup·absence·other).
 *    그래서 둘 다 `absence` 로 남기고 **머리말로 가른다.**
 */
export const PLAN_TAG = Object.freeze({ absent: "[결석 예정]", late: "[지각 예정]" });

/** 학부모가 보낸 것의 갈래 이름 — `v2.request.kind` 의 CHECK 와 짝이다 */
export const REQUEST_NAME = Object.freeze({
  absence: "결석·지각 예정",
  makeup: "보강 요청",
  question: "남기신 말",
  other: "그 밖",
});
